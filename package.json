{
  "name": "courseflow",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=11.16.0 <12"
  },
  "devDependencies": {
    "@courseflow/core": "workspace:*",
    "@courseflow/infrastructure": "workspace:*",
    "@courseflow/test-support": "workspace:*",
    "@eslint/js": "^9.39.0",
    "@playwright/test": "^1.55.0",
    "@testing-library/dom": "^10.4.0",
    "@testing-library/jest-dom": "6.9.1",
    "@testing-library/react": "^16.3.0",
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitest/coverage-v8": "^4.0.0",
    "concurrently": "^9.2.0",
    "dotenv-cli": "^10.0.0",
    "esbuild": "0.28.2",
    "eslint": "^9.39.0",
    "eslint-config-next": "16.2.11",
    "jsdom": "^27.0.0",
    "pg": "^8.16.0",
    "prettier": "^3.6.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "typescript-eslint": "^8.46.0",
    "vitest": "^4.0.0"
  },
  "scripts": {
    "build": "pnpm --filter @courseflow/core build && pnpm --filter @courseflow/contracts build && pnpm --filter @courseflow/infrastructure build && pnpm --filter @courseflow/test-support build && pnpm --filter @courseflow/worker build && pnpm --filter @courseflow/web build",
    "db:migrate": "pnpm --filter @courseflow/infrastructure build:cli && dotenv -e .env -- node packages/infrastructure/dist/migrate.cjs",
    "deps:down": "docker compose down",
    "deps:prepare": "pnpm --filter @courseflow/infrastructure build:cli && dotenv -e .env -- node packages/infrastructure/dist/ensure-object-store.cjs",
    "deps:up": "docker compose up --detach --wait && pnpm deps:prepare",
    "dev": "pnpm deps:up && pnpm db:migrate && dotenv -e .env -- concurrently --kill-others --names web,worker --prefix-colors cyan,magenta \"pnpm --filter @courseflow/web dev\" \"pnpm --filter @courseflow/worker dev\"",
    "format": "prettier --config prettier.config.mjs --write .",
    "format:check": "prettier --config prettier.config.mjs --check .",
    "gate:p0": "pnpm install --frozen-lockfile && pnpm deps:up && pnpm format:check && pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:migration && pnpm build && pnpm test:e2e && pnpm security",
    "gate:p1": "pnpm install --frozen-lockfile && pnpm deps:up && pnpm db:migrate && pnpm format:check && pnpm lint && pnpm typecheck && pnpm test:unit && pnpm test:migration && pnpm test:p1:postgres && pnpm build && pnpm test:e2e && pnpm security",
    "lint": "eslint .",
    "security": "pnpm security:secrets && pnpm security:licenses && pnpm security:audit",
    "security:audit": "pnpm audit --audit-level high",
    "security:licenses": "node scripts/check-licenses.mjs",
    "security:secrets": "node scripts/check-secrets.mjs",
    "test:e2e": "dotenv -e .env -- playwright test",
    "test:migration": "pnpm --filter @courseflow/infrastructure build:cli && dotenv -e .env -- node scripts/verify-empty-migration.mjs",
    "test:p1:postgres": "dotenv -e .env -- tsx scripts/verify-p1-postgres-contract.ts",
    "test:unit": "vitest run",
    "typecheck": "pnpm --recursive --if-present typecheck"
  }
}
