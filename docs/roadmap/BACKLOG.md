# CourseFlow 首个公开版本工作包 Backlog

> 状态：已批准，执行中
> 基线日期：2026-08-21
> 顺序与门禁：[ROADMAP.md](./ROADMAP.md)
> 已批准设计：[Implementation Roadmap Design](../superpowers/specs/2026-08-21-courseflow-implementation-roadmap-design.md)

## 1. 使用规则

本台账登记首发的 53 个工作包及其唯一的 Requirement/TEST 主所有者。Requirement 与 `TEST-*` 的完整语义仍分别由 [PRD.md](../product/PRD.md) 和 [MODULE_CONTRACTS.md](../architecture/MODULE_CONTRACTS.md) 拥有。

状态只允许：

- `Ready`：硬依赖完成，规范与验收可判定，可以领取。
- `In Progress`：执行者已经领取且正在修改。
- `Verification`：实现完成，正在收集包内测试、平台或 Gate 证据。
- `Done`：目标测试和证据全部满足，变更已提交。
- `Blocked`：存在已记录且无法在包内解决的真实外部阻塞。
- `—`：工作包已登记但尚未进入生命周期；不是正式状态。

同一时刻只推进一个主链工作包；只有 Roadmap 明确允许的独立平台包可以并行。状态变更必须在“证据台账”记录提交、命令、结果和未验证项。

## 2. 工作包注册表

### R0 — 实现就绪

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R0-01` | 首发剖面、19 个 UI 表面、五项主导航和“临近截止”规则写回产品语义所有者；移除无功能主题预留位 | — | — | — | — | `Done` |
| `WP-R0-02` | Architecture、Contracts、User Flow 与追溯状态统一为已批准实现基线，测试描述能区分 19/24 表面 | `WP-R0-01` | — | — | — | `Done` |
| `WP-R0-03` | 开发工具/依赖版本复核完成，双平台主机、签名、发布与回退资源均有事实状态 | `WP-R0-01` | — | — | — | `Done` |

### R1 — 可打包 Walking Skeleton

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R1-01` | pnpm、Electron、Forge Vite、React、TypeScript 精确锁定并可重复安装、类型检查和构建 | `WP-R0-02`, `WP-R0-03` | — | — | — | `Done` |
| `WP-R1-02` | Main/preload/renderer 最小安全壳可在开发与打包产物中打开，Renderer 无 Node/Electron 能力 | `WP-R1-01` | — | — | — | `Done` |
| `WP-R1-03` | Main 监督单一 Workspace utility process；精确 `appBuildId` 握手和最小 Query DTO 可验证 | `WP-R1-02` | — | — | — | `Done` |
| `WP-R1-04` | 开发 app identity、稳定开发数据根、单实例和内存 SQLite 运行时探针在打包产物中通过 | `WP-R1-03` | — | — | — | `Done` |
| `WP-R1-05` | Windows 与 macOS 的进程边界、隐私、启动/退出和打包烟测证据登记完成 | `WP-R1-04` | 对应平台主机 | — | — | `Done` |

### R2 — 首次真实保存

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R2-01` | DATA commit、schema/迁移、事务和幂等摘要基础可持久化并在重启后重开 | `WP-R1-04` | `WP-R1-05` | `A-DATA-001` | `TEST-DATA-001`, `TEST-DATA-002`, `TEST-DATA-003`, `TEST-DATA-005` | `Done` |
| `WP-R2-02` | 首次 setup 可创建并选择当前学期，重启后保持稳定身份 | `WP-R2-01` | — | `A-TERM-001`, `A-TERM-002` | — | `Done` |
| `WP-R2-03` | 用户可创建课程和首个 meeting，并保留课程核心字段与 TBA 区分 | `WP-R2-02` | — | `A-COURSE-001`–`A-COURSE-004` 的创建/首个 meeting 切片 | `TEST-PLAN-001`, `TEST-PLAN-002`, `TEST-PLAN-007`, `TEST-DATA-001`–`TEST-DATA-006` | `Done` |
| `WP-R2-04` | setup → 当前学期 → 课程 → meeting → 重启的 UI 纵向切片通过 | `WP-R2-03` | `WP-R1-05` | — | — | `Done` |

### R3 — 可用课表

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R3-01` | 学期生命周期和有效日期范围约束课程/meeting 投影 | `WP-R2-04` | — | `A-TERM-003`, `A-COURSE-007` | — | `Done` |
| `WP-R3-02` | 重复 meeting 产生稳定 occurrence/segment，跨重启身份不漂移 | `WP-R3-01` | — | `A-COURSE-005` | `TEST-PLAN-004`, `TEST-PLAN-005` | `Done` |
| `WP-R3-03` | 时间、TermZone、冲突和 TBA 语义完整且不把未知值默认化 | `WP-R3-02` | — | `A-COURSE-006` | `TEST-PLAN-002` | `Done` |
| `WP-R3-04` | 假期设置与 holiday skip 对课表投影生效，边界日期可判定 | `WP-R3-03` | — | `A-TERM-004`, `A-TERM-005` | — | `Done` |

### R4 — 完整 MVP-A 计划核心

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R4-01` | 当前学期 Course 关联的一次性 Task 创建、编辑、删除、完成与稳定身份闭环 | `WP-R3-04` | — | `A-TASK-001`–`A-TASK-003` | `TEST-PLAN-001` | `Done` |
| `WP-R4-02` | 重复任务、适用范围、假期规则与实例展开闭环 | `WP-R4-01` | — | `A-TASK-004`, `A-TASK-007`, `A-TASK-010` | `TEST-PLAN-003` | `Done` |
| `WP-R4-03` | 实例状态、单次/后续范围、跳过与撤销按事实提交边界工作 | `WP-R4-02` | — | `A-TASK-005`, `A-TASK-006`, `A-TASK-008`, `A-TASK-009` | `TEST-SHELL-003`, `TEST-FLOW-01-COMMIT` | `Done` |
| `WP-R4-04` | Today、Week 与临近截止投影遵循统一计划和已批准日期边界 | `WP-R4-03` | — | `A-VIEW-001`–`A-VIEW-006` | `TEST-WORKSPACE-001`, `TEST-PLAN-006`, `TEST-PLAN-007` | `Done` |
| `WP-R4-05` | Calendar 与 Agenda 共享稳定事件身份并正确呈现冲突/TBA | `WP-R4-04` | — | `A-CALENDAR-001`–`A-CALENDAR-003` | `TEST-PLAN-008` | `Done` |
| `WP-R4-06` | 首次设置与五项主导航的键盘、焦点、非颜色状态和基础可用性通过 | `WP-R4-05` | Windows/macOS 输入环境 | — | `TEST-USABILITY-001` | `Done` |

### R5 — 结构化备份内核

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R5-01` | 备份目的地配置和活动 DATA/资料库/备份三类位置隔离可证明 | `WP-R4-06` | — | `A-DATA-002` | `TEST-PROTECT-001` | `Done` |
| `WP-R5-02` | 正式 DATA commit 可异步产生结构化不可变快照，失败不回滚本地成功 | `WP-R5-01` | — | — | `TEST-PROTECT-002`, `TEST-DATA-004` | `Done` |
| `WP-R5-03` | 最近两份已验证结构化快照、待备份和未配置状态准确持久化 | `WP-R5-02` | — | `A-DATA-003` | `TEST-PROTECT-003` | `Done` |

### R6 — 恢复、迁移与回退内核

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R6-01` | 备份候选分类、安全恢复集和“只整库替换、不自动合并”边界闭环 | `WP-R5-03` | — | `A-DATA-006` | `TEST-WORKSPACE-002`, `TEST-PROTECT-004` | `Done` |
| `WP-R6-02` | 同卷暂存、检查点、外部激活协调记录和确定性继续/回滚状态机闭环 | `WP-R6-01` | 故障注入环境 | — | `TEST-DATA-006` | `Done` |
| `WP-R6-03` | 迁移安全副本、handoff 与中断恢复内核遵循 ADR-04/08/10 | `WP-R6-02` | 精确旧/新开发 build fixture | — | — | `Done` |
| `WP-R6-04` | 精确版本回退入口、影响说明、删除安全副本和恢复导航闭环 | `WP-R6-03` | 精确兼容 build fixture | — | `TEST-SHELL-005`, `TEST-WORKSPACE-007`, `TEST-PROTECT-007` | `Done` |
| `WP-R6-05` | 启动、maintenance、recovery、模块不可用和重启生命周期路由可判定 | `WP-R6-04` | — | — | `TEST-WORKSPACE-004`, `TEST-FLOW-00-LIFECYCLE` | `Done` |

### G-A — MVP-A 内部门

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-GA-01` | 完成已批准桌面窗口 UI 优化后，不含 Attendance/Library 的 A-only 打包剖面通过 G1–G7；其保护证据会在 R11/R12 被替代 | `WP-R6-05` | `WP-R1-05`, 当前双平台 A-only 包、Windows 真实物理鼠标环境 | — | — | — |

#### `WP-GA-01` 桌面窗口 UI 优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. 领取本包后先按 TDD 完成两个独立 UI 切片，再用同一 clean source 的 Windows x64 与 macOS arm64 packaged app 取得真实观察证据，最后运行 A-only G1–G7。不得用模拟截图、DOM 注入或自动生成的指针事件替代人工视觉与物理鼠标验收。

**目标：** 默认恢复窗口为显式 `1280×800`、最小窗口为 `960×640`（2026-08-29 用户直接指令，取代原计划的 `1120×720` 且不再排除最小尺寸），Renderer 只呈现一个贴合窗口客户区的视觉外壳；正常窗口的四边保持一致，内容较长或窗口较矮时底边仍始终可见，右侧不显示垂直滚动条但滚轮、触控板和键盘滚动继续可用。

> **实施状态（2026-08-29）：** 切片 1 与切片 2 的代码已按上述数值在 `WP-UI-01` 切片 2 中实现并通过自动化检查，证据见台账；`WP-GA-01` 本身仍为 `—`，其未完成部分只剩“Packaged 人工验收与 Gate”。

**架构：** Main 继续拥有原生窗口尺寸和 `thickFrame`；Renderer 技术根节点只约束客户区，现有 `.workspace-frame` 拥有唯一视觉边界，现有 `.workspace-content` 拥有唯一纵向滚动。设置对话框和业务页面不改变事实或 DOM 所有权。

**技术栈：** 当前锁定的 Electron、React、TypeScript、Vite 与原生 CSS；不新增依赖。

**批准依据：** 2026-08-25 用户对 Windows packaged app 截图和实际边界观察的诊断审批；本节是该 UI 优化的唯一执行计划，Roadmap 只同步顺序与 Gate。

**实现边界：** 复用现有 `BrowserWindow`、`.workspace-frame`、`.startup-frame` 与 `.workspace-content`；保留 `titleBarStyle: 'hidden'`、`thickFrame: true`、自绘窗口按钮、drag/no-drag 区和平台菜单差异。除用户 2026-08-29 直接批准的 `minWidth`/`minHeight` 两个常量外，不新增依赖、包装组件、窗口状态持久化、透明窗口、业务功能、Workspace/DATA 接口或新 Requirement/TEST 主所有权。

**预计文件：**

- `tests/architecture/runtime-boundaries.test.ts`：锁定显式默认窗口尺寸，并继续锁定原生 resize frame。
- `src/main.ts`：只增加 `width: 1120` 与 `height: 720`。
- `tests/renderer/workspace-shell.test.ts`：锁定单一视口外壳、唯一内容滚动区和隐藏滚动条但不禁用滚动的 CSS 契约。
- `src/renderer/styles.css`：移除可见的 `body` 外围留白和根滚动，约束现有 frame 为客户区高度，并让 `.workspace-content` 独立滚动。
- `docs/roadmap/BACKLOG.md`、`docs/roadmap/ROADMAP.md`：实施完成后只登记实际命令、双平台制品、人工观察、未验证项和状态。预计不修改 `src/renderer/App.tsx`；若既有 DOM 无法承载该布局，先更新本计划并说明可达证据。

##### 切片 1：显式增大默认恢复窗口

- [x] 在 `runtime-boundaries.test.ts` 先增加 RED：`BrowserWindow` 的 `width`/`height`/`minWidth`/`minHeight` 必须为 `1280`/`800`/`960`/`640`，`thickFrame` 仍为 `true`，且仍没有 `frame: false` 或 `transparent: true`。

```typescript
const sizes = { width: '1280', height: '800', minWidth: '960', minHeight: '640' };
for (const [name, text] of Object.entries(sizes)) {
  const property = objectProperty(state, options, name);
  assert.ok(property && state.is.isNumericLiteral(property));
  assert.equal(property.text, text);
}
assert.equal(objectProperty(state, options, 'transparent'), undefined);
```

- [x] 运行该聚焦测试并确认它只因当前缺少显式尺寸而失败。
- [x] 在 `src/main.ts` 的现有唯一 `BrowserWindow` options 中加入四个数值，不增加尺寸计算、显示器探测、持久化或 fallback。

```typescript
const window = new BrowserWindow({
  width: 1280,
  height: 800,
  minWidth: 960,
  minHeight: 640,
  show: options?.show ?? true,
  thickFrame: true,
  title: 'CourseFlow',
  titleBarStyle: 'hidden',
  webPreferences: {
    preload: path.join(__dirname, 'preload.js'),
    contextIsolation: true,
    sandbox: true,
    nodeIntegration: false,
    webSecurity: true,
  },
});
```

- [x] 重跑聚焦测试并确认通过。

##### 切片 2：单一视口外壳与内部滚动

- [x] 在 `workspace-shell.test.ts` 先增加 RED，锁定：`body` 无外围 padding 且不再拥有文档滚动；`html`、`body`、`#root` 与现有 frame 受窗口客户区高度约束；`.workspace-content` 具有 `min-height: 0` 和 `overflow-y: auto`；可见垂直 scrollbar 被隐藏但不得使用 `overflow-y: hidden` 禁止内容滚动。

```typescript
const bodyRule = /body\s*\{[^}]*\}/s.exec(styles)?.[0] ?? '';
const contentRule = /\.workspace-content\s*\{[^}]*\}/s.exec(styles)?.[0] ?? '';
assert.match(bodyRule, /padding:\s*0;/);
assert.match(bodyRule, /overflow:\s*hidden;/);
assert.match(contentRule, /min-height:\s*0;/);
assert.match(contentRule, /overflow-y:\s*auto;/);
assert.match(contentRule, /scrollbar-width:\s*none;/);
assert.match(styles, /\.workspace-content::\-webkit-scrollbar\s*\{[^}]*display:\s*none;/s);
```

- [x] 运行该聚焦测试，确认失败分别指向当前 `body` 外围 padding、frame 仅有 `min-height`、以及根页面承担滚动。
- [x] 在 `styles.css` 只重分配布局所有权：技术根节点占满客户区且不滚动，现有 frame 成为唯一可见外壳，顶部栏保持可达，`.workspace-content` 成为唯一纵向滚动区，内容底部 padding 保证最后一项完整可见。

```css
html,
body,
#root {
    height: 100%;
}

body {
    overflow: hidden;
    padding: 0;
}

#root {
    min-height: 0;
}

.workspace-frame,
.startup-frame {
    height: 100%;
    min-height: 0;
}

.workspace-frame {
    display: grid;
    grid-template-rows: auto minmax(0, 1fr);
    width: 100%;
    margin: 0;
}

.startup-frame {
    display: grid;
    grid-template-rows: auto minmax(0, 1fr);
}

.startup-surface {
    min-height: 0;
}

.workspace-content {
    min-height: 0;
    overflow-y: auto;
    scrollbar-width: none;
}

.workspace-content::-webkit-scrollbar {
    display: none;
}
```

- [x] 为 `.workspace-content` 同时提供 Chromium/WebKit 与标准 scrollbar 隐藏规则；保留 wheel、touchpad、`PageUp`/`PageDown`、`Home`/`End`、焦点跟随和 skip-link 导航。
- [x] 重跑聚焦测试；再运行 `pnpm test`、`pnpm typecheck` 与 `git diff --check`，确认既有窗口控制、键盘、forced-colors、对比度、透明度和 reduced-motion 回归仍通过。

##### Packaged 人工验收与 Gate

- [ ] 从同一 clean source 分别在 Windows x64 与 macOS arm64 运行 `pnpm package`、`pnpm smoke:packaged`，记录精确 commit、平台、制品身份和结果；单平台不得推断另一平台通过。
- [ ] 在两平台真实 packaged app 的首次默认打开中观察恢复窗口约为 `1120×720`；桌面只出现在原生窗口之外，客户区内没有第二层灰白外框或不对称外围留白。
- [ ] 使用真实长内容分别在默认高度和约 `600px` 的较矮恢复窗口检查页面顶部、中段、末尾：四边属于同一外壳，底边始终留在窗口底部，最后一项可完整到达，最右侧没有可见垂直滚动条。
- [ ] 用真实鼠标滚轮/触控板和键盘 `PageUp`、`PageDown`、`Home`、`End` 验证内容仍可双向滚动；键盘焦点、skip link、Today/Courses/Calendar/Tasks/Files 与设置对话框不得被内部滚动区困住或遮挡。
- [ ] Windows 使用真实物理鼠标重验标题栏拖拽、左侧 Snap、右侧 Snap、最大化与恢复；macOS 重验标题栏拖拽、自绘最小化/最大化/关闭和全局菜单。Snap/最大化时服从系统贴边形状，不强制制造圆角空隙。
- [ ] 在 Windows High Contrast/透明与动画设置、macOS Increase Contrast/Reduce Transparency/Reduced Motion 下抽查单一边界、内容滚动和焦点可见性；只记录真实观察。
- [ ] 上述 UI 验收全部通过后运行 A-only G1–G7，并将命令、真实观察、未验证项和最终 commit 写入证据台账；`TEST-USABILITY-001` 只作为既有回归重验，不改变其 `WP-R4-06` 主所有权。

### R7 — 出勤

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R7-01` | 可点名 occurrence、点名窗口和 eligibility 使用稳定身份且边界准确 | `WP-GA-01` | — | `A-ATTEND-001`, `A-ATTEND-002` | `TEST-ATTEND-001` | — |
| `WP-R7-02` | 出勤标记、统计与课程覆盖层保持未知/未标记/零的区分 | `WP-R7-01` | — | `A-ATTEND-003`–`A-ATTEND-005` | `TEST-ATTEND-002`, `TEST-ATTEND-003`, `TEST-FLOW-06-DERIVED-RESULTS` | — |
| `WP-R7-03` | Attendance 降级不阻塞 Plan；统一计划流和失败隔离通过 | `WP-R7-02` | 故障注入环境 | `A-ATTEND-006` | `TEST-ATTEND-004`, `TEST-FLOW-02-UNIFIED-PLAN` | — |

### R8 — 资料库身份与索引

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R8-01` | 单一资料库根身份、稳定本地边界和重定位语义闭环 | `WP-R7-03` | Windows/macOS 文件系统 | `B-FILE-001`, `B-FILE-013` | `TEST-LIBRARY-001` | — |
| `WP-R8-02` | 课程/分类目录、待归类与根内合法路径规则闭环 | `WP-R8-01` | Windows/macOS 文件系统 | `B-FILE-002`, `B-FILE-003` | `TEST-WORKSPACE-006`, `TEST-LIBRARY-005` | — |
| `WP-R8-03` | 全量扫描、watcher hint、FileId 对账和外部变更恢复闭环 | `WP-R8-02` | Windows/macOS 文件系统与 watcher | `B-FILE-005`, `B-FILE-006` | `TEST-LIBRARY-003` | — |
| `WP-R8-04` | 自定义标签、目录派生标签和组合搜索只使用真实索引 | `WP-R8-03` | — | `B-FILE-007`, `B-FILE-008` | — | — |

### R9 — 可恢复文件操作

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R9-01` | copy-in 导入和 journal 在中断/重启后确定性完成或回滚 | `WP-R8-04` | Windows/macOS 文件系统 | `B-FILE-004` | `TEST-LIBRARY-002` | — |
| `WP-R9-02` | 重命名、移动、系统回收站和系统打开请求不报告虚假成功 | `WP-R9-01` | Windows/macOS 原生适配器 | `B-FILE-009` | `TEST-PLATFORM-002` | — |
| `WP-R9-03` | 同名冲突的保留两份、替换、取消及逻辑身份规则闭环 | `WP-R9-02` | Windows/macOS 文件系统 | `B-FILE-011` | `TEST-LIBRARY-004` | — |

### R10 — 预览与系统打开

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R10-01` | 受验证资源描述符、lease 生命周期和预览数据面不泄露真实路径 | `WP-R9-03` | — | — | — | — |
| `WP-R10-02` | 支持格式只读预览、限制、解析失败和高风险文件政策闭环 | `WP-R10-01` | 恶意/超限 fixture | `B-FILE-010` | `TEST-LIBRARY-007` | — |
| `WP-R10-03` | 打包产物中的预览、定位、系统打开请求和 Library recovery 流在双平台通过 | `WP-R10-02` | Windows/macOS 打包产物 | — | `TEST-SHELL-002`, `TEST-PLATFORM-003`, `TEST-FLOW-03-LIBRARY-RECOVERY` | — |

### R11 — 完整数据保护

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R11-01` | 每次 DATA/Library 提交触发的快照完整包含结构化数据、根身份和所有必需文件 | `WP-R10-03` | 可控云盘目录 fixture | `A-DATA-004`, `B-FILE-012` | `TEST-FLOW-04-BACKUP-FAILURE` | — |
| `WP-R11-02` | 完整恢复在重新打开 DATA、全量资料库对账和启动路由后才报告成功 | `WP-R11-01` | 故障注入与完整快照 fixture | `A-DATA-005` | `TEST-LIBRARY-006`, `TEST-PROTECT-005`, `TEST-PROTECT-006`, `TEST-FLOW-05-RESTORE-RECOVERY` | — |
| `WP-R11-03` | Shell/Workspace 模块健康、降级隔离、19 表面边界和完整首发 G1–G7 通过；冻结公开 schema/fixture | `WP-R11-02` | Windows/macOS 完整首发包 | — | `TEST-SHELL-001`, `TEST-SHELL-004`, `TEST-WORKSPACE-003`, `TEST-WORKSPACE-005` | — |

### R12 — 双平台公开发布

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-R12-01` | macOS 与 Windows 在共享领域契约下完成完整首发闭环 | `WP-R11-03` | 两个平台真实主机 | `A-PLATFORM-001` | `TEST-PLATFORM-001` | — |
| `WP-R12-02` | 离线、无账户、无远程后端/AI、无应用内更新和无生产诊断边界通过 | `WP-R12-01` | 断网与制品检查环境 | `A-PLATFORM-002` | `TEST-PLATFORM-004`, `TEST-PRIVACY-001`, `TEST-RELEASE-004` | — |
| `WP-R12-03` | 稳定数据根、已安装升级/迁移、精确版本回退和卸载保留数据在双平台通过 | `WP-R12-02` | 旧/新签名候选制品 | `A-DATA-007`, `A-PLATFORM-003` | `TEST-DATA-007`, `TEST-PLATFORM-005`, `TEST-FLOW-07-UPDATE-ROLLBACK` | — |
| `WP-R12-04` | 签名、notarized 的 macOS arm64 原生制品通过安装、替换与 Gate | `WP-R12-03` | macOS arm64 主机、Apple 签名/notary | — | `TEST-RELEASE-002` | — |
| `WP-R12-05` | 签名的 Windows x64 WiX 原生制品通过 per-machine 安装、升级、卸载与 Gate | `WP-R12-03` | Windows x64 主机、Windows 代码签名 | — | `TEST-RELEASE-003` | — |
| `WP-R12-06` | GitHub Release manifest、双平台资产、重新下载验证和 G8 全部通过 | `WP-R12-04`, `WP-R12-05` | GitHub Releases 发布权限与干净下载环境 | `A-PLATFORM-004` | `TEST-RELEASE-001`, `TEST-RELEASE-005` | — |

### RF — 维护性重构（非首发交付链）

本节工作包不属于 §1 所述 53 个首发工作包，不拥有任何 Requirement/TEST 主所有权，也不改变首发剖面；它只登记行为保持的仓库结构维护，遵守与主链相同的状态与证据规则。

| WorkPacket | 可验证结果 | 硬依赖 | 证据依赖 | 主 Requirement | 主 TEST | 状态 |
|---|---|---|---|---|---|---|
| `WP-RF-01` | 按[已批准重构计划](../superpowers/plans/2026-08-28-courseflow-physical-architecture-refactor.md)完成 `src/` 物理模块化：`plan`/`workspace` 新包、data store 与 schema 内部拆分、renderer/protect/shared 拆分、依赖方向架构测试；全程行为保持，现有测试逐名等值通过 | `WP-R6-05` | 用户 Windows 主机复验 `pnpm test`/`typecheck`/`package`/`smoke:packaged` | — | — | `Verification` |
| `WP-UI-01` | 按用户 2026-08-29 直接指令与参考图对齐桌面视觉，并修复用户逐项列出的桌面显示缺陷：窗口控件伪影、卡片圆角白斑、多余外缘与根滚动、低分辨率滚动条与错位、首次设置页多余窗口按钮、进度卡黑卡位置，以及把「设置」改为含分类导航的独立设置表面；验收以用户在运行应用中的人工评审为准 | `WP-RF-01` | 用户运行应用人工评审 | — | — | `In Progress` |
| `WP-RF-02` | 把 2,848 行的 `src/renderer/styles.css` 按表面拆成多个样式文件并保持行为不变：新增一条 CSS 契约测试断言拆分后按固定顺序拼接的结果与拆分前逐字节相同，`workspace-shell`/`setup-ui` 既有 CSS 断言逐名等值通过 | `WP-UI-01` | 用户 Windows 主机复验 `pnpm test`/`typecheck` | — | — | `—` |

## 3. 首发 TEST 主所有权校验

注册表必须满足：

- 65 个首发适用 `TEST-*` 各出现且只作为一个工作包的主 TEST；包括 Attendance 分支的 `TEST-FLOW-06-DERIVED-RESULTS`。
- `TEST-GRADE-001`–`TEST-GRADE-007` 不进入任何首发工作包。
- 工作包可以重跑上游测试作为回归证据，但不得改变其主所有者；重跑记录写入证据台账。

按族计数为：Shell 5、Workspace 7、Plan 8、Attendance 4、Library 7、Protect 7、Data 7、Platform 5、Privacy 1、Release 5、Flow 8、Usability 1，共 65。

## 4. 首发 Requirement 主所有权校验

注册表必须满足：

- 42 条 MVP-A、6 条 MVP-A-P、13 条 MVP-B Requirement 各有且只有一个主工作包，共 61 条。
- `C-GRADE-001`–`C-GRADE-014` 与 `C-TARGET-001`–`C-TARGET-007` 明确排除，且不得由无 Requirement 的基础工作包暗中实现。
- 没有主 Requirement 的工作包只建设当前纵向切片必需的部署、契约、测试或安全边界，不拥有新的产品行为。

## 5. 发布资源矩阵

`WP-R0-03` 必须通过实际检查更新本表。`未验证` 是事实状态，不等于可用；任何依赖它的 Gate 在证据产生前保持开放。

| 资源 | 当前事实状态 | 关闭条件 | 影响工作包 |
|---|---|---|---|
| Windows x64 构建/安装主机 | 构建主机已验证、安装环境未验证（2026-08-22：Windows 11 `10.0.26200` / build `26200` / `AMD64` 从 clean commit `cd3a2fd66bca0fbd7e51c9eadef4dac1393484a2` 完成 package、packaged smoke 与可见窗口启动/退出；未执行 WiX 安装、干净机器或 SAC-On 验证） | 记录 OS/架构，并实际运行打包与安装烟测 | `WP-R1-05`, `WP-R12-01`, `WP-R12-05` |
| macOS arm64 构建/安装主机 | 构建主机已验证、安装环境未验证（2026-08-23：macOS `26.5.2` / build `25F84` / `arm64` / `Mac15,12` 从同一 clean commit `cd3a2fd66bca0fbd7e51c9eadef4dac1393484a2` 完成 package、packaged smoke 与可见窗口启动/退出；未执行 Developer ID 签名、公证、DMG 安装或 Gatekeeper 验证） | 记录 OS/架构，并实际运行打包与安装烟测 | `WP-R1-05`, `WP-R12-01`, `WP-R12-04` |
| Apple Developer ID 与 notarization | 未验证（2026-08-22：未在 macOS 对候选制品执行 Developer ID 签名、notarize、staple 或 Gatekeeper 检查） | 候选制品签名、notarize、staple 和 Gatekeeper 验证通过 | `WP-R12-04` |
| Windows 代码签名能力 | 未验证（2026-08-22：当前 development package 未签名且只在保持 SAC Off 的开发环境运行；未对 WiX 制品执行证书签名、SAC-On 或干净机器验证，当前主机未发现 `signtool`） | WiX 制品签名和干净机器验证通过 | `WP-R12-05` |
| GitHub Releases 发布权限 | 未验证（2026-08-22：`gh auth status` 已确认登录，但未实际创建候选 Release、上传资产/manifest 或重新下载） | 能创建候选 Release、上传资产/manifest 并从公开端重新下载 | `WP-R12-06` |
| 旧版 → 新版安装/迁移条件 | 未验证（2026-08-22：尚无精确旧/新候选制品、独立测试数据或完整升级/迁移矩阵） | 保留精确旧候选制品与独立测试数据，完整升级/迁移矩阵通过 | `WP-R12-03` |
| 新版 → 精确旧版回退条件 | 未验证（2026-08-22：尚无精确兼容制品、迁移安全副本或独立回退测试数据） | 精确兼容制品、迁移安全副本和独立测试数据齐备并通过 | `WP-R12-03` |

工具和依赖版本的复核结果也由 `WP-R0-03` 记录，但最终发布候选必须在 `WP-R12-04`/`05` 再次按 ADR-10 复核，不能沿用过期查询。

## 6. 证据台账

每次状态变化在下表追加一行；不得覆盖失败或未验证事实。

| 日期 | WorkPacket | 状态变化 | 提交/制品 | 实际运行的验证 | 结果与未验证项 |
|---|---|---|---|---|---|
| 2026-08-21 | `WP-R0-01` | `— → Ready` | Roadmap 基线提交 | 文档链接、ID/计数、`git diff --check` | 等待执行 R0 产品校准 |
| 2026-08-22 | `WP-R0-01` | `Ready → In Progress` | 本工作包 | `rg -n "GAP-PRODUCT-01|固定浅色|外观预留|六个主导航|六项主导航|临近截止|即将到期" docs/product docs/superpowers/specs/2026-08-18-courseflow-ui-wireframes-page-spec-design.md docs/architecture/MODULE_CONTRACTS.md` | 命中 `GAP-PRODUCT-01` 的未定义 near-due 阈值、UI 的三处固定浅色/外观预留位和六个主导航；开始产品语义校准。 |
| 2026-08-22 | `WP-R0-01` | `In Progress → Done` | `23497eaed1e1b2924fb75326bb925e28986730be` (`docs: calibrate first release profile`)；后续验收修正 `b688645d578499a33e362a41024a60f49e3f25bf` (`docs: clarify near-due acceptance`) | `rg -n "today \+ 1|today \+ 7|today \+ 8|TermZone|61 条|19 个|Today.*Courses.*Calendar.*Tasks.*Files" docs/product/PRD.md docs/product/MVP_SCOPE.md docs/superpowers/specs/2026-08-18-courseflow-ui-wireframes-page-spec-design.md`；`rg -n "固定浅色|外观预留|六个主导航|六项主导航" docs/superpowers/specs/2026-08-18-courseflow-ui-wireframes-page-spec-design.md`；`git diff --check`；`git diff -- docs/product docs/superpowers/specs docs/roadmap/BACKLOG.md` | 首条命中 near-due 全部边界、61/19/5 与五项导航；第二条无输出（exit 1，表示无残留）；`git diff --check` 通过且最终 diff 已审阅，确认仅更新产品/UI 语义所有者和 Backlog，五个 `UI-GRADE-*` 页面仍保留且标为 C1 未来设计。 |
| 2026-08-22 | `WP-R0-02` | `Ready → In Progress` | 本工作包 | `rg -n "待用户审阅书面规格|候选架构基线|候选规范|GAP-PRODUCT-01|24 个正式页面|24 个 UI 表面" docs/superpowers/specs/2026-08-17-user-flow-design.md docs/architecture` | 命中三份待终审状态、开放 `GAP-PRODUCT-01` 和首发验收硬编码 24 个表面；开始实现基线与首发追溯校准。 |
| 2026-08-22 | `WP-R0-02` | `In Progress → Done` | `913a60dd99cf606db8efcf0ae8d2d6205199cf3b` (`docs: approve implementation baseline`)；后续契约修正 `0122ab2398aa4714657e79753a7d644d8bdab5f7` (`docs: clarify release profile contracts`) | `rg -n "已批准实现基线|首发 19|完整.*24|GAP-PRODUCT-01.*已解决" docs/superpowers/specs/2026-08-17-user-flow-design.md docs/architecture`；`rg -n "候选架构基线|候选规范|待用户审阅书面规格|24 个正式页面只使用" docs/superpowers/specs/2026-08-17-user-flow-design.md docs/architecture`；稳定 ID 前后集合比较；`git diff --check` | 首条定位三份已批准状态、已解决 GAP、首发 19 / 完整设计 24；第二条无输出（exit 1，表示无旧状态或旧首发硬编码）；User Flow/Architecture/Contracts 的稳定 ID 集合分别保留 38/56/264 项；`git diff --check` 通过。 |
| 2026-08-22 | `WP-R0-03` | `Ready → In Progress` | 本工作包 | `Get-Command node,npm,pnpm -ErrorAction SilentlyContinue`；临时添加 Workspace dependency runtime 的 Node `bin` 后运行 `node --version`、`pnpm --version`、`git --version`、`[System.Environment]::OSVersion`、`$env:PROCESSOR_ARCHITECTURE`；`Get-Command signtool,codesign,notarytool,gh -ErrorAction SilentlyContinue`；`gh auth status` | 初始 PATH 只有 Workspace runtime 的 `pnpm`，没有 `node`/`npm`；临时 runtime 得到 Node `v24.19.0`、pnpm `11.19.0`、Git `2.55.0.windows.4`、Microsoft Windows NT `10.0.26200.0`、`AMD64`。仅发现 `gh`；已登录状态不是候选 Release 创建/上传/重新下载证据，未将其推断为发布权限。 |
| 2026-08-22 | `WP-R0-03` | `In Progress → Done` | 本工作包 | 运行 10 条指定 `pnpm view` 查询（Electron `43.4.1`、Forge CLI/Vite `7.11.2`、React/React DOM `19.2.8`、TypeScript `7.0.2`、Vite `8.2.2`、`@types/node` `24.13.3`、`@types/react` `19.2.18`、`@types/react-dom` `19.2.4`）；查阅 [Electron releases](https://releases.electronjs.org/)、[Forge Vite plugin](https://www.electronforge.io/config/plugins/vite)、[utilityProcess](https://www.electronjs.org/docs/latest/api/utility-process)、[parentPort](https://www.electronjs.org/docs/latest/api/parent-port)、[Node test runner](https://nodejs.org/api/test.html) 和 [Node SQLite](https://nodejs.org/api/sqlite.html)；`git diff --check` | 全部精确版本可解析。Registry：Electron `>=22.12.0`/MIT；Forge CLI `>=16.4.0`/MIT；Forge Vite 无 peerDependencies/MIT；React/React DOM MIT；TypeScript `>=16.20.0`/Apache-2.0；Vite `^20.19.0 || >=22.12.0`/MIT；Node `24.19.0` 满足所有已声明 engine。官方资料确认 Electron `43.4.1` 为 stable、Forge Vite 构建 main/renderer、`utilityProcess`/`parentPort` 支持主进程与 utility 通信、`node:test` 稳定、`node:sqlite` 为 RC；资源矩阵按实际证据更新，macOS、签名、Release、升级和回退 Gate 仍开放。 |
| 2026-08-22 | `WP-R1-01` | `Ready → In Progress` | 本工作包 | `Test-Path package.json`；`Test-Path pnpm-lock.yaml` | 均为 `False`，证明尚无实现工具链；开始建立精确锁定的最小桌面工具链。 |
| 2026-08-22 | `WP-R1-01` | `In Progress → Blocked` | 未提交 | `pnpm exec tsc --ignoreConfig --noEmit --target ES2023 --module ESNext --moduleResolution Bundler --types node build/read-development-build-id.ts forge.config.ts vite.node.config.ts vite.renderer.config.ts`；检查 `@electron-forge/plugin-vite@7.11.2` 的 `src/Config.ts`、`src/ViteConfig.ts` | 简报强制的 `{ entry: 'src/workspace.ts', config: 'vite.node.config.ts', target: 'workspace' }` 与精确锁定的 Forge Vite `7.11.2` 不兼容：其类型只允许 `main`/`preload`，运行时对 `workspace` 直接抛出 `Unknown target`。不能在不改变锁定版本、配置语义或添加不安全类型断言的情况下通过配置编译；等待上游澄清。 |
| 2026-08-22 | `WP-R1-01` | `Blocked → In Progress` | 未提交 | pnpm `11.19.0` CHANGELOG、`@electron-forge/plugin-vite@7.11.2` 的 `Config.ts`/`ViteConfig.ts`；干净重装后的 `pnpm config get --location project --json nodeLinker/saveExact/blockExoticSubdeps`；配置编译门 | 根因已裁定并单变量验证：pnpm 11 只从 workspace YAML 读取 linker/save 策略；Forge Vite 的 Workspace entry 使用 `main` Node/Worker 模板。`nodeLinker: hoisted` 恢复 `@electron-forge/shared-types` 可见性。`blockExoticSubdeps: false` 为整个 workspace 关闭 exotic transitive dependency 拦截；当前锁文件唯一此类条目是 Forge 固定 commit 的官方 `@electron/node-gyp` tarball，任何未来依赖或锁文件变更都必须重新审查 exotic sources；配置编译通过。 |
| 2026-08-22 | `WP-R1-02` | `Ready → In Progress` | 本工作包 | 创建 bootstrap DTO 的失败测试 | 开始定义 `IF-WORKSPACE` bootstrap Query 的最小只读 DTO；尚未实现 production code。 |
| 2026-08-22 | `WP-R1-01` | `In Progress → Done` | `build: establish desktop toolchain` | `pnpm install --frozen-lockfile`；`pnpm list --depth 0`；`pnpm exec tsc --version`；`pnpm exec electron --version`；三个 `pnpm config get --location project --json`；修正后的配置编译门；禁止直接依赖核对；`git diff --check` | 冻结安装、顶层精确版本、pnpm workspace 策略、配置编译与禁止依赖核对均通过。未运行完整 `pnpm typecheck`/`pnpm package`：Task 5 尚未创建 Main/preload/renderer 入口；未验证 Vite generator（本机 Application Control 阻断 rolldown 原生绑定）、Windows/macOS 打包/安装、签名与发布资源，未将这些事项推断为已通过。 |
| 2026-08-22 | `WP-R1-02` | `In Progress` | 未提交 | 新基线 `74e95a3` 上先运行 `pnpm test`（RED：仅缺少 `src/shared/bootstrap-contract.ts`）；实现后运行 `pnpm test`、`pnpm typecheck`、`git diff --check` | Task 4 的 `build: use supported Node test resolution` 已消除先前 TS5108；bootstrap 契约 4/4 测试通过，完整类型检查和 diff 检查通过。工作包保持 In Progress，尚未验证开发/打包壳、Windows/macOS 或签名资源。 |
| 2026-08-22 | `WP-R1-02` | `In Progress → In Progress` | 本工作包 | 新增 Renderer/Main 静态架构边界测试，待运行 RED | 领取安全 desktop shell 切片；测试将分别捕获 Renderer 重新获得 Node/Electron/IPC/路径能力，以及 Main 取消窗口隔离、sandbox 或关闭 Node integration 的回归。 |
| 2026-08-22 | `WP-R1-02` | `In Progress → Done` | Windows package `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe` | `pnpm test`（6/6）；`pnpm typecheck`；`pnpm package`；启动 packaged EXE（PID 35760，窗口标题 `CourseFlow`）并通过该 PID 正常关闭 | 当前 Windows x64 package/launch 已验证，无开发服务器启动；Renderer 静态边界和 Main 窗口安全设置受测试保护。macOS、安装、签名、发布及 Workspace utility/Query 仍未验证或未实现；`WP-R1-03` Ready。 |
| 2026-08-22 | `WP-R1-02` | `Done → In Progress` | 独立审查 round 1/5；上游 `98bcf7d` | 审查发现先前 packaged 证据只验证窗口 handle/title，未验证 asar 内 renderer 内容；且缺少 ADR-01 CSP、navigation/new-window/permission 硬化，原顶层 Renderer 守卫不能覆盖嵌套或所有导入形式 | 原 Done 证据被审查推翻并重开。本轮先在测试/计划中登记 RED 和修复门；`98bcf7d` 已修正 renderer output layout，仍须以本轮 package 的 asar 清单和真实窗口内容复验，`WP-R1-03` 在验收前不 Ready。 |
| 2026-08-22 | `WP-R1-02` | `In Progress → Done` | Windows x64 package / asar / 内容 QA | `pnpm test` 10/10、`pnpm typecheck`、`pnpm package`；实际 `app.asar` 清单含 Main/preload、`main_window/index.html`、renderer CSS/JS，并与 Main loadFile、Forge name、Vite outDir 一致；无 dev server 的 handle 截图实际显示三项壳文案 | 审查重开项已修复：CSP、navigation/new-window/session permission 拒绝和递归 AST Renderer 边界均受测试保护，nested side-effect import mutation 确认会失败。截图 run 的 PID `9928` 在关闭调用前已退出；独立有界关闭 smoke PID `17904` 的 `CloseMainWindow()` 返回 True、无残留精确 EXE 进程。仅当前 Windows x64 证据；不推断 macOS、安装、签名或发布。`WP-R1-03` 恢复 `Ready`。 |
| 2026-08-22 | `WP-R1-03` | `Ready → In Progress` | 本工作包 | 新增 Workspace supervisor 与进程边界测试，待运行 RED | 开始单一 Workspace utility process、精确 bootstrap handshake 和窄 Renderer capability 切片；不实现 SQLite、真实数据根或重启策略。 |
| 2026-08-22 | `WP-R1-03` | `In Progress → Done` | Windows x64 package `out/CourseFlow Dev-win32-x64` | `pnpm test` 16/16；`pnpm typecheck`；`pnpm package`；`rg -n 'utilityProcess\\.fork' src` 仅 Main 一处；Renderer 特权 IPC/import query 无命中 | Main/preload 使用 shared `WORKSPACE_QUERY_CHANNEL`，Renderer 只使用冻结 query capability；supervisor 覆盖 ready、malformed response、5 秒 timeout、child exit 和 dispose。Forge 产出 Workspace entry。打包 EXE 的真实 GUI handshake 未验证：Computer Use `ShellExecuteW` 错误 5，随后精确 EXE 启动被 Windows 应用控制策略阻断；不将其或 SQLite/真实数据根标为已验证。`WP-R1-04` Ready。 |
| 2026-08-22 | `WP-R1-03` | `Done → Blocked` | 独立审查 round 1/5；Windows application control | Electron `UtilityProcess.error` 处理测试 RED 后修复为 GREEN；现有 package asar 清单含 Main/preload/renderer/workspace，并嵌入 `development:c522feb87d9…`。已存在 Electron runtime 启动该精确 asar（PID `49408`）后无窗口/无子进程，随后仅停止该 PID | `error` 已安全收敛为 unavailable；但 asar runtime 入口未产生可见 ready/build handshake，不能视为最终 unsigned wrapper EXE 的等价启动。wrapper 的 Computer Use `ShellExecuteW` 错误 5 和精确 `Start-Process` 应用控制策略阻断仍在。mandatory GUI 证据未达成，故 WP-R1-03 Blocked、WP-R1-04 不得 Ready；未声称 SQLite/数据根。 |
| 2026-08-22 | `WP-R1-03` | `Blocked → Done` | Windows x64 final wrapper `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe`；截图 `.superpowers/sdd/2026-08-21-courseflow-r0-r1-implementation/task-7-packaged-handshake.png` | 用户手动关闭 Smart App Control 后，clean `38e177ad0ee565d2078d25b337715aecef57dd99` 的 `pnpm package` 通过；asar 含 Main/preload/workspace/renderer，三端 build ID 精确为 `development:38e177…` 且无 `:dirty`；Computer Use 从唯一最终 wrapper 窗口读取 `Workspace 进程已就绪` 和 `Build 38e177ad0ee5`，Alt+F4 后无残留 wrapper/utility 进程 | 真实最终 wrapper GUI handshake 已验证，先前 application-control blocker 被此证据取代；未将 unsigned/SAC compatibility 推断为发行签名验收，SQLite、真实数据根、macOS、安装和发布仍不在 WP-R1-03 已验证范围。`WP-R1-04` Ready。 |
| 2026-08-22 | `WP-R1-04` | `Ready → In Progress` | 本工作包 | 跨平台开发根、SQLite 版本和严格 probe DTO 测试已加入，待运行 RED | 仅实现开发根、单实例和内存 SQLite probe；不创建正式 DATA/schema/migration/文件数据库。 |
| 2026-08-22 | `WP-R1-04` | `In Progress → Done` | Windows x64 final wrapper `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe` | `pnpm test` 25/25、`pnpm typecheck`、`pnpm package` 均通过；真实 `--courseflow-smoke` stdout 恰有一个非空 JSON 行（其余仅 Electron pre-JS CRLF 空白）：`{"kind":"courseflow.smoke","ok":true,"appBuildId":"development:ee5a32169bc421b0fd07d69a2e501c548ce1aad4:dirty","sqliteVersion":"3.53.1","dataRootClass":"verified-local"}`、exit 0、stderr 空；正常模式 stdout 仅 Electron pre-JS `0D 0A` 空白（2 bytes）、stderr 空且 exit 0 | Main 在 ready 前仅创建 `CourseFlow Dev` 下 ActivityControl、DataSlots、Chromium/Session 开发根，设置开发 AppUserModelId 和单实例锁；probe 经真实 preload/IPC/utility/`node:sqlite` 内存连接返回版本与 `verified-local`，无路径进入 DTO。两次运行均无残留 exact wrapper/utility 子进程。当前 Windows unsigned 测试环境的 SAC 已由用户暂时关闭；这不是签名或 SAC compatibility 证据，后者仍属于 `WP-R1-05`/Task 9。macOS、签名、安装和发布仍未验证。`WP-R1-05` 与 `WP-R2-01` Ready（前者的平台/签名证据依赖仍开放）。 |
| 2026-08-22 | `WP-R1-05` | `Ready → In Progress` | 未提交 | 新增 `tests/architecture/runtime-boundaries.test.ts` 后运行 `pnpm test`（RED：34 项中 33 通过、1 失败） | 进程/Renderer/preload/BrowserWindow/依赖与开发身份边界全部通过；唯一失败为 `scripts/run-packaged-smoke.mjs must exist`，证明 smoke runner 尚未实现且 RED 原因精确。 |
| 2026-08-22 | `WP-R1-05` | `In Progress → Blocked` | clean source `812acba1b14cf72803ede651a19b160b82606466`；Windows x64 final wrapper `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe` | Windows 11 `10.0.26200` / build `26200` / `AMD64`；Node `v24.19.0`、pnpm `11.19.0`、Electron `v43.4.1`、SQLite `3.53.1`；`pnpm install --frozen-lockfile`、`pnpm test` 34/34、`pnpm typecheck`、`pnpm package`、`pnpm smoke:packaged` 均通过，runner 报 `PASS packaged smoke win32/x64 development:812acba1b14cf72803ede651a19b160b82606466 SQLite 3.53.1 verified-local`；独立 raw capture 为前置 `0D 0A` 空白、唯一非空 JSON 行、结尾 `0A`，exit 0/stderr 0 bytes；唯一可见 `CourseFlow` 窗口实际显示 ready、Build `812acba1b14c`、SQLite `3.53.1` 与开发数据根已验证，定向 Alt+F4 后窗口列表为空且 exact wrapper/utility process count 为 0 | Windows development package 的进程/隐私/启动/退出证据通过；唯一 R1 blocker 是没有真实 macOS arm64 主机和同 commit 实际结果。`WP-R2-01` 保持 `Ready`，但其 `WP-R1-05` evidence dependency 与最终 release Gate 保持开放。当前制品未安装、未签名且 SAC 保持 Off，不是公开候选；macOS 签名/notarization、Windows WiX/签名/SAC-On/干净机器及发布均未验证。 |
| 2026-08-22 | `WP-R1-05` | `Blocked → Blocked` | clean source `591542d3a27cabdd2c9b12b6049901dd5950076c`；Windows x64 final wrapper `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe`；可见窗口截图 `.superpowers/sdd/2026-08-21-courseflow-r0-r1-implementation/final-head-591542d-gui.png` | Node `v24.19.0`、pnpm `11.19.0`、Electron `v43.4.1`、SQLite `3.53.1`；root 在该 clean source 上 fresh 运行 `pnpm install --frozen-lockfile`、`pnpm test` 37/37、`pnpm typecheck`、`pnpm package`、`pnpm smoke:packaged` 均通过，runner 报 `PASS packaged smoke win32/x64 development:591542d3a27cabdd2c9b12b6049901dd5950076c SQLite 3.53.1 verified-local`；raw stdout 为 166 bytes，前缀 `0D 0A`，恰有一个非空 JSON 行，完整 build ID 精确为 `development:591542d3a27cabdd2c9b12b6049901dd5950076c`、不含序列化路径，stderr 为 0 bytes；截图已由 root 实际查看，显示 `CourseFlow`、`Workspace is ready`、Build `591542d3a27c`、SQLite `3.53.1` 与 verified development root；对 exact window 发送 `WM_CLOSE` 后 exit 0，正常模式 stdout 仅 `0D 0A`、stderr 为 0 bytes，最终 exact wrapper/utility process count 为 0 | 本行为追加证据，不覆盖 clean source `812acba1b14cf72803ede651a19b160b82606466` 的历史行。Task 9 仅覆盖 development platform 的进程/隐私/启动/退出/package smoke；macOS 签名/notarization 与 Windows WiX/签名/SAC-On/干净机器/安装/发布属于 `WP-R12-03`–`WP-R12-06`，均未验证。`WP-R1-05` 仍为 `Blocked`，唯一原因是缺少同一 source commit `591542d3a27cabdd2c9b12b6049901dd5950076c` 的真实 macOS arm64 结果；`WP-R2-01` 保持 `Ready` 且 evidence dependency 保持开放。该制品 unsigned、uninstalled 且 SAC 保持 Off，不是 public candidate。 |
| 2026-08-22 | `WP-R1-05` | `Blocked → Blocked` | clean source `cd3a2fd66bca0fbd7e51c9eadef4dac1393484a2`；Windows x64 final wrapper `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe` | Windows 11 `10.0.26200` / build `26200` / `AMD64`；Node `v24.19.0`、pnpm `11.19.0`、Electron `v43.4.1`、SQLite `3.53.1`；在该 clean source 上运行 `pnpm install --frozen-lockfile`、`pnpm test` 37/37、`pnpm typecheck`、`pnpm package`、`pnpm smoke:packaged` 均通过，runner 报 `PASS packaged smoke win32/x64 development:cd3a2fd66bca0fbd7e51c9eadef4dac1393484a2 SQLite 3.53.1 verified-local`；独立 raw stdout 为 166 bytes，前缀 `0D 0A`、恰有一个非空 JSON 行、结尾 `0A`，完整 build ID 精确为 `development:cd3a2fd66bca0fbd7e51c9eadef4dac1393484a2`、不含序列化路径，stderr 为 0 bytes；唯一可见 `CourseFlow` 窗口实际显示 Workspace 进程已就绪、Build `cd3a2fd66bca`、SQLite `3.53.1` 与本地开发数据根已验证；对 exact window 定向 Alt+F4 后 exit 0，正常模式 stdout 仅 `0D 0A`、stderr 为 0 bytes；最终 exact package process、fixture process 和 fixture temp directory count 均为 0 | 这是 drive-qualified Windows root 修复后的最新 Windows evidence，保留 clean source `812acba1b14cf72803ede651a19b160b82606466` 与 `591542d3a27cabdd2c9b12b6049901dd5950076c` 的历史证据。`WP-R1-05` 仍为 `Blocked`，唯一原因是缺少同一 source commit `cd3a2fd66bca0fbd7e51c9eadef4dac1393484a2` 的真实 macOS arm64 结果；`WP-R2-01` 保持 `Ready` 且 evidence dependency 保持开放。该制品 unsigned、uninstalled 且 SAC 保持 Off，不是 public candidate；macOS 签名/notarization 与 Windows WiX/签名/SAC-On/干净机器/安装/发布属于 `WP-R12-03`–`WP-R12-06`，均未验证。 |

| 2026-08-23 | `WP-R2-01` | `Ready → In Progress` | 本工作包 Task 1 | 新增 TEST-DATA-002 canonical digest golden vector 测试，待实现 `courseflow-canonical-json-v1` 与 SHA-256 digest | 先固定 canonical DTO、受限编码和 digest；不实现 DATA store/schema/迁移或后续工作包。 |
| 2026-08-23 | `WP-R2-01` | `In Progress → Done` | 完整 Task 1–6 source range `54a02f6857c3ec7862ece1e5d6d429f474ef222c`..`5b5bd2717b3311e1f7950550e5bc0703af01db77`；实现/修正 commits：`279061644595e24629c48e2837b41dd464f71820`、`787285dee8b0c83cae396d58bc0c2f48cec58d2c`、`df979838b43bed41358f2433775c95572486e048`、`15ecdb02c39fb44db66ba299e2bdf31bf3eb1a94`、`421c7c944e845d761b060b2bdafc6fec4b802349`、`ebf46bb660b1fd7dbdb8b2c94f7d26bca1bc7123`、`e0ba8c283bc47a9517a9b0713b0b53e33c8a2f45`、`b7907f92bb942b06076632eb88077423550761c7`、`621a42de43ce4cc9c71f74c8476de36a9547593e`、`fde1298d83764b3522ee10c9de244f340232043f`、`5b5bd2717b3311e1f7950550e5bc0703af01db77` | `pnpm test` 73/73 PASS（四个 `TEST-DATA-001`/`002`/`003`/`005` 均命名 PASS）、`pnpm typecheck` PASS、clean source 上 `pnpm package` Windows x64 PASS、`pnpm smoke:packaged` PASS：`PASS packaged smoke win32/x64 development:5b5bd2717b3311e1f7950550e5bc0703af01db77 SQLite 3.53.1 verified-local`；runner 验证唯一精确 smoke JSON、空 stderr、无路径/WorkspaceId/revision 字段泄露。schema level 1 且仅六张共同表：`workspace_state`、`setup_state`、`command_receipts`、`receipt_effects`、`durable_followups`、`protection_watermarks`。canonical `courseflow-canonical-json-v1` digest golden/replay、同 ID 不同语义拒绝、semantic pre-COMMIT failpoint 全不改变事实、post-COMMIT response loss 仅经 reopen/restart receipt 收敛、stale EntityVersion/queued writer snapshot 边界、read-only/recovery/wrong ID/missing manifest/FK/future/corrupt/nonempty-level-0 与无 reset 分类均通过。protocol v2 的 path-free `workspaceData` 只经单一 Workspace utility owner 集成；`node:sqlite` 仅 DATA schema/store，package/lockfile 无 diff、无 ORM/连接池（禁止扫描唯一 `protection_watermarks`/`term` 词法命中已人工排除）。`WP-R2-02` 因此 Ready；但 `WP-R1-05` 仍 Blocked，真实 macOS arm64 证据未验证，Done 不关闭最终双平台 release Gate，也不交付 C1/C2、未来模块、迁移、备份或恢复。 |
| 2026-08-23 | `WP-R2-01` | `Done → Done` | final-review fix `736bbb9eab70cb881f6112442749db24185de16f` | 先证实 `TEST-DATA-003` 64-bit exhaustion RED 为 0/3，再在修复后通过 focused 3/3、sqlite-data-store 26/26、`pnpm test` 76/76、`pnpm typecheck` 与 `git diff --check`；clean source `736bbb9eab70cb881f6112442749db24185de16f` 的 `pnpm package` Windows x64 PASS，`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:736bbb9eab70cb881f6112442749db24185de16f SQLite 3.53.1 verified-local`；独立 scoped re-review 为 1 addressed / 0 open，Critical/Important/Minor 均无 | Revision 或 setup EntityVersion 到 `9223372036854775807` 时在任何事实写入前 rollback 并终止当前 DATA owner，queued/future 写拒绝且持久事实、receipt、follow-up 与 watermark 不变。该修正不改变 level-1 六表范围、DTO/digest/failpoint/open classification，不新增依赖、ORM、连接池、C1/C2、未来模块、迁移、备份或恢复；`WP-R2-02` 保持 Ready，`WP-R1-05` 的真实 macOS arm64 阻塞与 release Gate 仍开放。 |
| 2026-08-23 | `WP-R1-05` | `Blocked → Done` | clean source `cd3a2fd66bca0fbd7e51c9eadef4dac1393484a2`；macOS arm64 final wrapper `out/CourseFlow Dev-darwin-arm64/CourseFlow Dev.app` | macOS `26.5.2` / build `25F84` / `arm64` / `Mac15,12`；Node `v24.19.0`、pnpm `11.19.0`、Electron `v43.4.1`、SQLite `3.53.1`；在隔离 worktree 的该 clean source 上运行 `pnpm install --frozen-lockfile`、`pnpm test`（36 pass、0 fail、1 个 Windows 专属 skip）、`pnpm typecheck`、`pnpm package`、`pnpm smoke:packaged` 均通过，runner 报 `PASS packaged smoke darwin/arm64 development:cd3a2fd66bca0fbd7e51c9eadef4dac1393484a2 SQLite 3.53.1 verified-local`；正常模式以 `open -n` 启动同一 app，CoreGraphics 观察到 owner `CourseFlow Dev` 的 layer 0 on-screen 窗口 `CourseFlow`，随后通过应用 quit 正常退出，最终 exact wrapper/helper process count 为 0 | macOS 与既有 Windows x64 证据绑定同一 clean source commit，进程/隐私/启动/退出/package smoke 的双平台 R1 Gate 因此关闭。当前 app unsigned、unnotarized、uninstalled，不是 public candidate；macOS 签名/公证/DMG、Windows WiX/签名/SAC-On/干净机器、安装、迁移/回退与发布继续由 `WP-R12-03`–`WP-R12-06` 验证。屏幕捕获权限与 Accessibility 未授予自动化进程，因此没有把截图像素或辅助功能树内容列为证据；真实 on-screen window、完整 smoke handshake 与无残留进程均已独立观察。 |

| 2026-08-23 | `WP-R2-02` | `Ready → In Progress` | 本工作包 | 新增 Term DTO、DATA、迁移和 Workspace restart 测试后运行 `pnpm run clean:test`、`pnpm run test:compile`（RED，exit 2） | 测试编译精确失败于尚不存在的 `workspace-term-contract`、`WorkspaceApplication`、`openWorkspaceDataWithMigrations` 与 `readSetupProjection`；开始首次 setup 当前学期纵向切片，未实现课程、meeting 或完整导航。 |
| 2026-08-23 | `WP-R2-02` | `In Progress → Done` | clean implementation source `c14310457052ef1f27ab7a9bb0f32f6478b187e1`；Windows x64 package `out/CourseFlow Dev-win32-x64` | Windows 11 `10.0.26200` / build `26200` / x64；Node `v24.19.0`、pnpm `11.19.0`、Electron `v43.4.1`、SQLite `3.53.1`；targeted 52/52 PASS；`pnpm test` 101/101 PASS；`pnpm typecheck`、`git diff --check`、clean source `pnpm package` 均 PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:c14310457052ef1f27ab7a9bb0f32f6478b187e1 SQLite 3.53.1 verified-local` | level 2 仅交付 Term/Current Term：规范化名称/LocalDate/IANA zone、随机稳定 TermId、唯一 current、同一 ReadSnapshot、Revision/plan EntityVersion、canonical digest/receipt/follow-up/watermark 和 pre/post-COMMIT 收敛均有证据；level 1 receipt/follow-up 经已验证 safety copy 和独立事务迁移保留，迁移中断不重建/重置；read-only/recovery 拒绝写入。首次 setup UI 只经冻结 preload capability → Main → 单一 Workspace utility → DATA，DTO/Renderer 无数据库、路径、Node/Electron/platform 类型。未实现课程、meeting、完整 Today/导航、深色模式或后续模块；macOS 本轮未重新打包/烟测，不从既有平台证据推断本提交通过。dirty source 的首次 smoke 尝试按 clean build ID 门禁被预期拒绝，随后在上述 clean implementation source 重新 package/smoke 通过。 |
| 2026-08-23 | `WP-R2-02` | `Done → Done` | clean implementation source `c14310457052ef1f27ab7a9bb0f32f6478b187e1` 的 Windows x64 packaged app；隔离的临时 `LOCALAPPDATA` | 真实可见窗口通过键盘/日期选择器填写名称 `2026 Fall`、`2026-09-08`、`2026-12-19`、`America/Toronto` 并执行“创建并继续”；完成面显示 canonical UUID `TermId`。关闭窗口、确认无 CourseFlow 窗口后，以同一隔离数据根重新启动；名称、日期、时区与同一个 `TermId` 逐字一致 | packaged Renderer → preload → Main → Workspace → DATA 的首次设置和进程重启持久化取得实际 Windows E2E 证据；没有触碰既有开发数据根。macOS packaged 交互 E2E 仍未在本提交上执行。 |
| 2026-08-23 | `WP-R2-03` | `— → Ready` | `WP-R2-02` clean implementation source `c14310457052ef1f27ab7a9bb0f32f6478b187e1` | `WP-R2-02` registry/证据已为 `Done`；`WP-R2-03` 无额外证据依赖 | `WP-R2-03` 成为唯一 `Ready` 工作包；课程与 meeting 仍未实现。 |
| 2026-08-23 | `WP-R2-03` | `Ready → In Progress` | 本工作包 | Course/首个 Meeting 的 contract、DATA、迁移、Workspace 与 setup UI 失败测试待写入和运行 | 开始当前学期下 Course 与首个 Meeting 的原子持久化纵向切片；不实现 occurrence、规则分段、冲突检测、假期 skip、Today/Calendar 或完整导航。 |
| 2026-08-23 | `WP-R2-03` | `In Progress → In Progress` | TDD RED | `pnpm run clean:test` 后 `pnpm run test:compile` exit 2：缺少 `workspace-course-contract`，`SetupProjection.courses` 与 Course/Meeting 双 effect command 类型尚不存在；随后定向 `setup-ui.test` 为 1 pass / 1 fail，精确失败于缺少课程字段和 `createCourseWithMeeting` UI capability | RED 先固定 PLAN/DATA/schema/Workspace/IPC/UI 义务，再在共同语义所有者处实现；未用模拟数据或扩大到 occurrence/分段/冲突/假期。 |
| 2026-08-23 | `WP-R2-03` | `In Progress → Verification` | clean implementation source `dcc068ef944b54f56be35c17b3521a5294bf42e6` | Windows 11 `10.0.26200` / x64；Node `v24.19.0`、pnpm `11.19.0`、Electron `v43.4.1`、SQLite `3.53.1`；`pnpm test` 125/125 PASS；`pnpm typecheck`、`git diff --check` PASS；独立只读复审最终为 Critical/Important/Minor 均无、Ready Yes | schema level 3 交付 Course、MeetingSeries 与 WP-R2-03 单 segment 映射；level 1/2 经同一已验证 safety copy 链式迁移且中断保持旧级事实；Course 与首个 Meeting 在一个事务、一个 revision、一个 receipt 的两个有序 effect 中原子提交，CommandId 重放、pre/post-COMMIT、read-only/recovery、同一 ReadSnapshot、TBA/known、LEC/TUT/PRA、人类可读名称、有效日期和重启稳定身份均有测试。审阅发现并修复 Course/Term post-COMMIT 结果误报：Workspace 重开 DATA 后按持久 receipt 收敛，无法确认时只报告 `dataEffect: unknown`。Renderer 仍只经冻结 preload capability → Main → 单一 Workspace utility → DATA；未新增依赖或绕过 Shell/Workspace 边界。 |
| 2026-08-23 | `WP-R2-03` | `Verification → Done` | clean source `dcc068ef944b54f56be35c17b3521a5294bf42e6`；Windows x64 package `out/CourseFlow Dev-win32-x64`；隔离临时 `LOCALAPPDATA` | clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:dcc068ef944b54f56be35c17b3521a5294bf42e6 SQLite 3.53.1 verified-local`。真实可见 packaged Renderer 输入 Term `2026 Fall` / `2026-09-08`–`2026-12-18` / `America/Toronto`，Course `CSC240` / `Enriched Introduction to the Theory of Computation` / `LEC0101` / `Grace Hopper` / blue / `3.5`，Meeting `TUT` / `WED` / `14:00`–`15:30` / 同学期有效期 / TBA；完成面返回 TermId `846c97a9-3e8d-494b-811e-781ce2721b83`、CourseId `cdd0822f-526e-4137-b5c6-2ff55dbfe449`、MeetingSeriesId `9f463a86-c487-4528-aa3b-985328928a3e`。正常关闭并确认精确目标进程为 0 后，以同一隔离根重启；页面无 setup form，全部字段与三个 ID 逐字一致；再次正常关闭后精确目标进程和本机调试端口均为 0 | packaged UI 提交实际越过 Renderer → preload → Main → Workspace → DATA 并在进程重启后由 DATA 投影恢复；Computer Use 可访问树确认两个 setup 步骤，本机 `SendInput` 返回 Windows error 87 后，字段与 submit 改由同一可见 packaged Renderer 的 localhost-only Electron DevTools 协议触发，未直接写数据库或调用 preload 能力。未触碰既有开发数据根。此工作包只关闭 `A-COURSE-001–004` 的创建/首个 Meeting 切片；不声称交付 Course 编辑/归档、多个 Meeting、occurrence、规则分段、冲突检测、假期 skip、Today/Calendar 或完整导航。macOS 未在本提交上重新 package、smoke 或执行交互 E2E，不从既有证据推断通过。`WP-R2-04` 成为唯一 `Ready` 工作包。 |
| 2026-08-23 | `WP-R2-04` | `Ready → In Progress` | baseline `77dd5c6943c50be83df98e3ee9404d0ca6e12448`；本工作包 TDD RED | 目标 setup/Workspace/DATA/contract 组合 52/52 PASS，基线 `pnpm test` 125/125 PASS；新增 Renderer 行为测试后 `pnpm run clean:test`、`pnpm run test:compile` exit 2，精确缺少 `src/renderer/setup-state` | 开始只验证现有 setup → Current Term → Course → 首个 Meeting → 重启闭环；不新增 occurrence/segment、冲突、假期、任务、Today/Calendar 或完整导航。审查确认唯一当前包缺陷为：已有完整 projection 处于 read-only DATA 时仍会显示“设置完成”。 |
| 2026-08-23 | `WP-R2-04` | `In Progress → Verification` | clean implementation source `d41bf57e6548538ae5e3113c0e4c2ce2fee87c82` | `pnpm test` 126/126 PASS；`pnpm typecheck`、`git diff --check` PASS。将修复临时反向突变为 read-only → `complete` 后，聚焦测试 2/3 PASS、1 FAIL（actual `complete` / expected `problem`）；恢复提交后 3/3 PASS 且源码 diff 为零。三个独立只读审计确认 DATA/Workspace 纵向子集、UI/可访问性基础与 Windows E2E 路径均无本包阻塞项 | Renderer 路由现在只有 `dataMode === ready` 且已有 Course/Meeting 时才显示完成；read-only 完整 projection 明确显示问题状态，未误报成功。改动只提取一个 Renderer 纯映射并增加真实行为测试，无新依赖、公共 DTO、领域事实或进程边界变化。完整 `ProjectionEnvelope`/`IF-WORKSPACE` 仍按 `WP-R4-04` 验证，不把本包描述为完整接口交付。 |
| 2026-08-23 | `WP-R2-04` | `Verification → Done` | clean source `d41bf57e6548538ae5e3113c0e4c2ce2fee87c82`；Windows x64 package `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe`；隔离 `%LOCALAPPDATA%/CourseFlow-WP-R2-04-ae911b8a8b4b4a0891521d43f379a757` | Windows 11 `10.0.26200` / build `26200` / x64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:d41bf57e6548538ae5e3113c0e4c2ce2fee87c82 SQLite 3.53.1 verified-local`。真实可见 packaged Renderer 仅以 Computer Use 键盘/可访问元素输入 Term `2027 Winter WP-R2-04` / `2027-01-04`–`2027-04-23` / `America/Toronto`，Course `CSC299` / `WP-R2-04 Integration Seminar` / `LEC0101` / `Ada Lovelace` / red / `3.5`，Meeting `TUT — Tutorial` / `WED` / `14:00`–`15:30` / 同学期有效期 / TBA；完成面返回 TermId `8367a762-1bf5-4f4e-a522-30ca1cb5db35`、CourseId `33b0d3d2-339b-46b6-979d-5d679df053e2`、MeetingSeriesId `cf4f730c-507b-4d11-92aa-afd24a5723d8`。第一次启动 PID 为 wrapper `2408`、Workspace utility `11280`、renderer `10536`、network utility `17872`、GPU `17860`；正常关闭后 exact EXE 与全部记录 PID 均为 0。同一隔离根重启 PID 为 wrapper `30124`、Workspace utility `44792`、renderer `25556`、network utility `5480`、GPU `50976`；20/20 必需字段匹配、0 项 setup form 文本出现，全部字段和三个稳定 ID 逐字一致。第二次正常关闭后 exact EXE、全部记录 PID、这些 PID 的 TCP 连接与调试进程/端口均为 0；隔离根已移入 Windows 回收站且原路径不存在 | packaged UI 的两次提交实际越过 Renderer → frozen preload → Main → 单一 Workspace utility → DATA，重启只由 DATA 投影恢复；未直接调用 preload、读写 SQLite 或启用 DevTools/debug 端口。实际截图与可访问树确认浅色布局、标题/标签/fieldset/radio/status 文字、可见键盘焦点、TBA 非颜色表达和完成页字段；这只是本纵向切片的基础验收，不声称提前完成 `WP-R4-06`。未触碰既有开发数据根；隔离测试数据仍可从回收站恢复。macOS 未在本提交上重新 package、smoke 或执行交互 E2E，不从既有 R1 平台证据推断本提交通过。 |
| 2026-08-23 | `WP-R3-01` | `— → Ready` | `WP-R2-04` clean implementation source `d41bf57e6548538ae5e3113c0e4c2ce2fee87c82` 与本次证据提交 | `WP-R2-01`–`WP-R2-04` 注册表状态均为 `Done`；`WP-R3-01` 硬依赖满足且无额外证据依赖 | `WP-R3-01` 成为唯一 `Ready` 工作包；本次关闭不实现 `A-TERM-003`、`A-COURSE-007` 或任何 R3 投影行为。 |
| 2026-08-23 | `WP-R3-01` | `Ready → In Progress` | baseline `135bc471bba391559b44fe39f55d1ae62add8e07`；本工作包 TDD RED | 新增 Term 生命周期、Course/Meeting 范围、迁移与 Workspace 重启测试后运行测试编译；初始失败于尚无 lifecycle Intent/ClockPort、range intent、归档投影和 schema level 4 | RED 先固定 end date 当天/次日、TermZone、归档与修正恢复、精确继承/合法缩短/one-day 越界、非法命令不变、pre/post-COMMIT、幂等、只读/恢复态和重启稳定性；未扩展到 occurrence/segment、单次课节、冲突、假期或任务。 |
| 2026-08-23 | `WP-R3-01` | `In Progress → Verification` | clean implementation source `85ec2a3df2e91309fb6e4cd31e14bc73018019e1` | 聚焦回归 36/36 PASS；`pnpm test` 147/147 PASS；`pnpm typecheck`、`git diff --check` PASS；27 个改动 TypeScript/TSX 路径的 FECS `@file` 门禁 PASS；独立只读复审首轮 3 个 Important 均修复，复审最终 Critical/Important/Minor 均无、Ready Yes | Workspace 以注入的 `ClockPort` 和显式 IANA `TermZone` 判断本地日期：等于结束日只读，次日才发起正式 `workspace.reconcile-lifecycle` Intent；归档保留全部 Term/Course/Meeting 事实和历史查询，修正结束日后可用同一 `TermId` 显式恢复。Course 默认继承 Term，Meeting 默认继承 Course，显式范围只能缩短；schema level 4、v1/v2/v3 链式迁移、历史 schema-1 Course receipt replay、事务/revision/entity version/receipt/follow-up/watermark、pre/post-COMMIT、read-only/recovery 和 reopen/restart 均有回归。复审补齐 Workspace 跨实体投影校验、跨时钟 restore replay 的稳定摘要，以及旧 receipt 可达性；无新增依赖。 |
| 2026-08-23 | `WP-R3-01` | `Verification → Done` | clean source `85ec2a3df2e91309fb6e4cd31e14bc73018019e1`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:85ec2a3df2e91309fb6e4cd31e14bc73018019e1 SQLite 3.53.1 verified-local` | `A-TERM-003` 与 `A-COURSE-007` 的目标纵向切片关闭，历史事实、稳定 `TermId`/`CourseId`/`MeetingSeriesId` 和重启语义保持。未在本提交上重新运行 macOS package/smoke 或 packaged 交互 E2E，不从既有平台证据推断通过；签名、安装和公开发布 Gate 仍开放。`WP-R3-02` 成为唯一 `Ready` 主链工作包。 |
| 2026-08-23 | `WP-R3-02` | `Ready → In Progress` | baseline `5fd81a0a90576bf12e106cb3ede7e9a534b6e2b6`；本工作包 TDD RED | 先增加 Meeting occurrence 契约、DATA、schema/migration、Workspace 与进程边界测试；初始失败精确指向缺少稳定 `MeetingOccurrenceId`、override/split Intent、schema level 5 与纵向调用链 | RED 固定 `(MeetingSeriesId, originalLogicalAnchor)` 身份、only-this 隔离、this-and-future 历史不变/分段不重叠、重算/普通编辑/重启稳定，以及事务、receipt/revision、幂等、pre/post-COMMIT、只读/恢复和迁移语义；不纳入冲突、假期、任务 occurrence 或广泛 UI。 |
| 2026-08-23 | `WP-R3-02` | `In Progress → Verification` | clean implementation source `9a71b6e10f5cc98f0278e6e3f61c88e7eced6b60` | 聚焦测试 46/46 PASS；`pnpm test` 177/177 PASS；`pnpm typecheck`、`git diff --check` PASS；独立只读复审最终 Critical 0、Important 0、Ready Yes | 普通 occurrence 由 series 与原始 logical anchor 确定性派生且不落表；only-this 只持久化目标 `OccurrenceOverride`；this-and-future 保留 `MeetingSeriesId`，从目标 anchor 以独立 `SegmentId` 原子分割，旧段与新段不重叠且历史不变。schema level 5、v1–v4 链式迁移、影响预览/确认 token、事务/revision/entity version/receipt/follow-up/watermark、pre/post-COMMIT、幂等、read-only/recovery、reopen/restart 与 Renderer → preload → Main → Workspace → DATA 窄调用链均有回归；无新增依赖。 |
| 2026-08-23 | `WP-R3-02` | `Verification → Done` | clean source `9a71b6e10f5cc98f0278e6e3f61c88e7eced6b60`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:9a71b6e10f5cc98f0278e6e3f61c88e7eced6b60 SQLite 3.53.1 verified-local` | `A-COURSE-005` 与 `TEST-PLAN-004/005` 的 Meeting occurrence/segment 切片关闭；两项 TEST 主所有权由尚未开始的 `WP-R4-03` 移至本包且全表仍保持唯一，未来任务 occurrence 仍归 R4。macOS 未在本提交上重新 package、smoke 或执行交互 E2E，不从既有平台证据推断通过；冲突、假期、任务 occurrence 和广泛 UI 未提前实现。 |
| 2026-08-23 | `WP-R3-03` | `— → Ready` | `WP-R3-02` clean implementation source `9a71b6e10f5cc98f0278e6e3f61c88e7eced6b60` 与本次证据提交 | `WP-R3-02` 的 `A-COURSE-005`、`TEST-PLAN-004/005`、Windows package/smoke 和独立复审均通过；`WP-R3-03` 硬依赖满足且无额外证据依赖 | `WP-R3-03` 成为唯一 `Ready` 主链工作包；本次关闭不实现 `A-COURSE-006` 的冲突、完整时间或 TBA 语义。 |
| 2026-08-24 | `WP-R3-03` | `Ready → Verification` | clean implementation source `d13ece4f14b43d993d61bd4b292112abe01d514d` | 聚焦回归 41/41 PASS；`pnpm test` 194/194 PASS；`pnpm typecheck`、`git diff --check` PASS；把进程默认时区改为 `Pacific/Auckland` 后显式 TermZone 测试 4/4 PASS；25 个改动 TypeScript/TSX 路径的 FECS `@file` 与 120 字符门禁均 PASS | schema level 6 为 Meeting segment/override 持久化显式同日/次日 offset，level 5 经既有 verified safety-copy/逐级事务协议迁移且旧 receipt/digest 可重放。Meeting occurrence 只通过显式 TermZone 和可替换 ZoneRules 解析 Instant，采用确定性的 DST gap/overlap 规则和半开区间精确边界；冲突在同一 Term 内应用 segment/only-this override、排除 cancelled 与归档事实后计算。warning 携带拟议/既有 Course、MeetingSeries/Occurrence 稳定对象和双方/交集 Instant；review 不写事实，continue 在同一 expected revision/version 下重算并按原时间提交。TBA 仅是 location 状态，星期、时间格与冲突判断保留；单一 ReadSnapshot、稳定身份、事务、幂等、pre/post-COMMIT、重启和现有分段语义均有回归，无新增依赖。 |
| 2026-08-24 | `WP-R3-03` | `Verification → Done` | clean source `d13ece4f14b43d993d61bd4b292112abe01d514d`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | Windows `10.0.26200` / build `26200` / x64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:d13ece4f14b43d993d61bd4b292112abe01d514d SQLite 3.53.1 verified-local` | `A-COURSE-006`、`Q-TIME-01`、`Q-STATE-01` 与本包 `TEST-PLAN-002` 切片关闭；正常周、精确边界、跨日、DST、only-this override、this-and-future segment、cancelled、TBA、历史 Term 排除和明确继续保存均有定位测试。macOS 未在本提交上重新 package/smoke，packaged 交互 E2E、签名、安装和公开发布不从本证据推断通过；假期、任务和广泛 UI 未实现。 |
| 2026-08-24 | `WP-R3-04` | `— → Ready` | `WP-R3-03` clean implementation source `d13ece4f14b43d993d61bd4b292112abe01d514d` 与本次证据提交 | `WP-R3-03` 注册表为 `Done`，目标测试、Windows package/smoke 与文档追溯均通过；`WP-R3-04` 硬依赖满足且无额外证据依赖 | `WP-R3-04` 成为唯一 `Ready` 主链工作包；本次状态推进不实现 HolidayRange、holiday skip、任务或后续计划投影。 |
| 2026-08-24 | `WP-R3-04` | `Ready → Verification` | clean implementation source `e5109a4036c6ee9c86dd0352b1c51afdd4e4b7c0` | HolidayRange/ADR-04/Workspace 聚焦回归 23/23 PASS；`pnpm test` 212/212 PASS；`pnpm typecheck`、`git diff --check` PASS；25 个改动 TypeScript 路径的 FECS `@file` 与 120 字符门禁均 PASS | schema level 7 以每个命名连续范围一行保存 inclusive LocalDate、稳定 `HolidayRangeId`、EntityVersion 与 tombstone；level 6 经 verified safety copy 和逐级事务迁移，既有 receipt/follow-up 保留。创建、编辑和删除均原子推进 revision/PLAN version、持久 receipt/effect/follow-up/watermark，并覆盖摘要固定向量、幂等、stale、pre/post-COMMIT、只读、恢复和重启。Meeting occurrence 先按 active HolidayRange 抑制，再应用 only-this replaced/cancelled override；编辑/删除确定性重派生且身份、segment、override 和历史事实不被重写。冲突只比较变更前后并返回新引入的 warning，review 不写事实、continue 明确保存。Workspace/preload/Main 仅增加窄 HolidayRange DTO 与调用链，无新增依赖。 |
| 2026-08-24 | `WP-R3-04` | `Verification → Done` | clean source `e5109a4036c6ee9c86dd0352b1c51afdd4e4b7c0`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | Windows `10.0.26200` / build `26200` / x64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:e5109a4036c6ee9c86dd0352b1c51afdd4e4b7c0 SQLite 3.53.1 verified-local` | `A-TERM-004/005` 的 Meeting/课表假期切片关闭，R3 里程碑关闭。范围首尾、多个范围、编辑/删除重派生、稳定 occurrence identity、单次 override、历史 tombstone/revision、新增冲突 warning、迁移/事务/摘要幂等、持久化重启、只读与恢复均有定位测试。macOS 未在本提交上重新 package/smoke，packaged 交互 E2E、签名、安装和公开发布不从本证据推断通过；Task、`followTeachingWeek`、Attendance 与广泛 UI 未实现。 |
| 2026-08-24 | `WP-R4-01` | `— → Ready` | `WP-R3-04` clean implementation source `e5109a4036c6ee9c86dd0352b1c51afdd4e4b7c0` 与本次证据提交 | `WP-R3-01`–`WP-R3-04` 注册表均为 `Done`，R3 的目标测试和 Windows package/smoke 通过；`WP-R4-01` 硬依赖满足且无额外证据依赖 | `WP-R4-01` 成为唯一 `Ready` 主链工作包；重复任务和 `followTeachingWeek` 假期逻辑仍由 `WP-R4-02` 拥有，本次关闭未提前实现 Task、Attendance 或广泛 UI。 |
| 2026-08-24 | `WP-R4-01` | `Ready → Verification` | contract/source commits `ed43983be7aa7a1a567658e81669261936d0ee5b`、`5f2ce8551bc50b3073c836e6f005f53071857881` | Task contract/schema/Workspace 聚焦回归 21/21 PASS；`pnpm test` 229/229 PASS；`pnpm typecheck`、`git diff --check` PASS；21 个改动 TypeScript 路径的 FECS `@file` 与 120 字符门禁 PASS；三个独立只读复审最终 Critical/Important/Minor 均无、Ready Yes。TDD 与复审回归覆盖 segmentless TaskSeries、writer-busy ProblemCode 丢失及历史 Task 重挂，并分别在 schema、Workspace problem mapper 和事务内 Current Course 校验处修复为 GREEN | schema level 8 以一条 TaskSeries、一条 once TaskSegment 和可选 completed occurrence-state 保存最小正式事实，level 7 经既有 verified safety-copy/逐级事务协议迁移且旧 receipt/follow-up 保留。small/large 与 date-only/timed/TBA 使用精确 union；创建、编辑、删除和完成原子推进 revision/PLAN/Task version、receipt/effect/follow-up/watermark，并覆盖摘要幂等、stale、pre/post-COMMIT、只读/恢复、当前来源/目标 Course 范围、并发 writer-busy、重启与稳定 `TaskSeriesId`/`TaskOccurrenceId`。Renderer 仅得到冻结的窄 preload capability，无新增依赖或广泛 UI。 |
| 2026-08-24 | `WP-R4-01` | `Verification → Done` | clean source `5f2ce8551bc50b3073c836e6f005f53071857881`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | Windows `10.0.26200` / build `26200` / x64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:5f2ce8551bc50b3073c836e6f005f53071857881 SQLite 3.53.1 verified-local` | `A-TASK-001–003` 与本包 `TEST-PLAN-001` 的当前学期 Course 关联 once Task 切片关闭。weekly、`followTeachingWeek`、only-this/this-and-future、skipped/撤销/进度等完整实例状态、Today/Week/Calendar 和广泛 UI 均未提前实现；macOS 未在本提交上重新 package/smoke，packaged 交互 E2E、签名、安装和公开发布不从本证据推断通过。 |
| 2026-08-24 | `WP-R4-02` | `— → Ready` | `WP-R4-01` clean implementation source `5f2ce8551bc50b3073c836e6f005f53071857881` 与本次证据提交 | `WP-R4-01` 注册表为 `Done`，目标测试、Windows package/smoke、文档追溯和独立复审均通过；`WP-R4-02` 硬依赖满足且无额外证据依赖 | `WP-R4-02` 成为唯一 `Ready` 主链工作包；只推进重复任务、适用范围、`followTeachingWeek` 假期规则与实例展开，完整实例状态/范围编辑仍由 `WP-R4-03` 拥有。 |
| 2026-08-24 | `WP-R4-02` | `Ready → Verification` | clean implementation source `3e876916532f903afed4cea471f3313a2d76e07c` | Task contract/schema/DATA/Workspace/进程边界聚焦回归 99/99 PASS；`pnpm test` 249/249 PASS；`pnpm typecheck`、`git diff --check` PASS；19 个改动 TypeScript 路径的 FECS `@file`、120 字符、tab 与新增单参数箭头门禁 PASS；三个独立只读复审最终 Critical/Important/Minor 均无、Ready Yes | schema level 9 保存精确 once/weekly schedule union，level 8 经 verified safety copy 和逐级事务协议迁移，旧 receipt/effect/follow-up/occurrence state 与 watermark 保留。weekly 规则要求用户确认的 inclusive `startDate`/`confirmedEndDate` 全部位于 Course 教学范围且至少产生一个实例；实例由 `TaskSeriesId` 与原始 LocalDate anchor 确定性派生，不保存普通 weekly occurrence。`followTeachingWeek=true` 仅抑制落入 active HolidayRange 的 weekly 实例，`false` 保留，once 始终保留；TermZone/DST、边界年份与 canonical ZoneId/Instant、重算/重启、事务/摘要幂等、pre/post-COMMIT、只读与恢复均有定位回归。Renderer 仅增加冻结的有界查询 capability，无新增依赖或广泛 UI。 |
| 2026-08-24 | `WP-R4-02` | `Verification → Done` | clean source `3e876916532f903afed4cea471f3313a2d76e07c`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:3e876916532f903afed4cea471f3313a2d76e07c SQLite 3.53.1 verified-local` | `A-TASK-004/007/010` 与本包 `TEST-PLAN-003` 的 weekly Task、适用范围、假期规则及稳定实例展开切片关闭。完整实例状态、only-this/this-and-future、跳过/撤销/进度仍由 `WP-R4-03` 拥有；Today/Week/Calendar 与广泛 UI 未提前实现。macOS 未在本提交上重新 package/smoke，packaged 交互 E2E、签名、安装和公开发布不从本证据推断通过。 |
| 2026-08-24 | `WP-R4-03` | `— → Ready` | `WP-R4-02` clean implementation source `3e876916532f903afed4cea471f3313a2d76e07c` 与本次证据提交 | `WP-R4-02` 注册表为 `Done`，目标测试、Windows package/smoke、文档追溯和独立复审均通过；`WP-R4-03` 硬依赖满足且无额外证据依赖 | `WP-R4-03` 成为唯一 `Ready` 主链工作包；只推进完整实例状态、only-this/this-and-future、跳过/撤销/进度及其事实提交边界，本次状态推进不实现这些行为、后续视图或广泛 UI。 |
| 2026-08-24 | `WP-R4-03` | `Ready → Verification` | baseline `281add5e1e8e89b33a74881a4b7387da026557a2`；本行所在本地提交 | WP-R4-03 聚焦回归 88/88 PASS；`pnpm test` 283/283 PASS；`pnpm typecheck`、`git diff --check` PASS；24 个改动 TypeScript 路径的 FECS `@file`、120 字符与 tab 门禁 PASS；多轮独立只读复审发现的 preview 投影边界、Shell requery/Undo、schema 外键顺序、fresh token 绑定、跨重启 Undo、终态历史保留、Holiday override 和 stale toast 问题均经定位回归修复，最终 Critical 0、Important 0 | schema level 10 保存 only-this override、独立 occurrence state、完成前进度与一次性 Undo history；level 9 经既有 verified safety copy/事务协议迁移且可在 failpoint 后确定性重启。pending/completed/skipped、large 可选 0–100%、恢复、only-this/this-and-future 编辑与删除、whole-series 删除、稳定 segment/occurrence identity、终态历史保留、完整 ImpactPreview、确认/冲突/幂等、pre/post-COMMIT、follow-up、只读/恢复与 Workspace reopen 均有直接断言。Renderer 只增加纯 Task command state，preload/Main/Workspace 只增加有界 Task occurrence DTO 与调用链，无新增依赖或广泛 UI。 |
| 2026-08-24 | `WP-R4-03` | `Verification → Done` | clean source 为本行所在本地提交；Windows x64 package `out/CourseFlow Dev-win32-x64` | Windows `10.0.26200` / x64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` PASS，packaged build identity 与该 clean source commit 一致且报告 `verified-local` | `A-TASK-005/006/008/009`、`TEST-SHELL-003` 与 `TEST-FLOW-01-COMMIT` 切片关闭。删除、completed、skipped 保持不同事实；完成显示 100%，恢复还原完成前进度；分段不改写终态历史。Today/Week/Calendar/Agenda 和广泛 UI 未提前实现。macOS 未在本提交上重新 package/smoke，packaged 交互 E2E、签名、安装和公开发布不从本证据推断通过。 |
| 2026-08-24 | `WP-R4-04` | `— → Ready` | `WP-R4-03` 本行所在 clean source 与本次证据提交 | `WP-R4-03` 注册表为 `Done`，目标测试、Windows package/smoke、文档追溯和独立复审均通过；`WP-R4-04` 硬依赖满足且无额外证据依赖 | `WP-R4-04` 成为唯一 `Ready` 主链工作包；只推进 Today、Week 与临近截止投影，本次关闭不实现这些视图、Calendar/Agenda 或广泛 UI。 |
| 2026-08-24 | `WP-R4-04` | `Ready → Verification` | clean implementation source `6ae07ad1d8cca588c0dace00f96b9251894bfab1` | TDD RED 先固定缺失的 PLAN evaluator、单 ReadSnapshot DATA source 和 `workspace.plan.query`；Reading Week 可见性复审再以 2 个失败测试固定 holiday-suppressed 实例不得进入 Week。GREEN 后聚焦 PLAN/DATA/Workspace/IPC 与既有 SAVEPOINT 回归通过；`pnpm test` 292/292 PASS，`pnpm typecheck`、`git diff --check` PASS，13 个改动 TypeScript 路径的 FECS `@file`、120 字符与 tab 门禁 PASS | `MOD-PLAN` 单点产生 overdue/today/near-due/future/completed/skipped/TBA、Monday–Sunday Today/Week、确定性 next-small/next-large 与真实空状态；`near-due` 精确覆盖 TermZone `today + 1` 至 `today + 7`。DATA 在一个 SQLite ReadSnapshot 中组合 Current Term 的稳定 Task/Meeting occurrence、HolidayRange 与同一 revision；Workspace 只读取一次 Clock 并形成单一 EvaluationContext，preload 仅增加经 exact validator 的窄查询能力。ATTEND unavailable/disabled 显式使用 meeting end-state fallback，Today 返回 Task/Meeting 贡献和排除明细；学期进度含首尾日且不扣假期。无 schema、迁移、依赖、Calendar/Agenda 或 Renderer 页面改动。 |
| 2026-08-24 | `WP-R4-04` | `Verification → Done` | clean source `6ae07ad1d8cca588c0dace00f96b9251894bfab1`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:6ae07ad1d8cca588c0dace00f96b9251894bfab1 SQLite 3.53.1 verified-local` | `A-VIEW-001–006`、`TEST-WORKSPACE-001`、`TEST-PLAN-006/007` 的 Today、Week、倒计时、临近分类、计数与学期日期进度切片关闭；Week 不显示 holiday-suppressed 周期课节，根投影仍保留其稳定身份和文字分类供排除明细。Calendar/Agenda、广泛 UI 与 ATTEND overlay 仍由后续工作包拥有；macOS 未在本提交上重新 package/smoke，packaged 交互 E2E、签名、安装和公开发布不从本证据推断通过。 |
| 2026-08-24 | `WP-R4-05` | `— → Ready` | `WP-R4-04` clean implementation source `6ae07ad1d8cca588c0dace00f96b9251894bfab1` 与本次证据提交 | `WP-R4-04` 注册表为 `Done`，目标测试、Windows package/smoke 与文档追溯均通过；`WP-R4-05` 硬依赖满足且无额外证据依赖 | `WP-R4-05` 成为唯一 `Ready` 主链工作包；只推进 Calendar/Agenda 对现有统一 PLAN occurrence identity、冲突、TBA 和 HolidayRange 的消费，不重写 WP-R4-04 分类或 Today/Week 计数公式。 |
| 2026-08-24 | `WP-R4-05` | `Ready → Verification` | clean implementation source 为本行所在本地提交 | TDD RED 先精确失败于尚无 Calendar/Agenda/TBA 字段，后续分别以 6/7、6/7、7/8、7/8 失败锁定 terminal TBA 分组、exact array、4096 warning 上限和 nested DTO descriptor；GREEN 后 PLAN/DATA/Workspace exact validator 聚焦回归 20/20 PASS，`pnpm test` 294/294 PASS，`pnpm typecheck`、`git diff --check` PASS；6 个改动 TypeScript 路径的 FECS `@file`、120 字符、tab 与新增单参数箭头门禁 PASS；独立只读复审最终 Critical/Important/Minor 均为 0、Ready Yes | 同一 PLAN evaluator 在同一 revision/EvaluationContext 上复用 WP-R4-04 分类后的 Meeting 与 once/weekly Task；Calendar 将 timed 事项、date-only 全天事项和每可见周最多一个 HolidayRange 连续片段分层，Agenda 按日期/语义层/精确时间/稳定身份确定性排序，每个假期范围仅一项。重叠 warning 携带双方分类对象与 Instant 交集，排除 cancelled/holiday-suppressed、不修改事实并复用既有 4096 有界 DTO 上限。TBA 按 Deadline union 独立分组，completed/skipped 仍保留终态分类。无 schema、迁移、依赖或 Renderer 页面改动；clean package/smoke 待本源码提交后执行，广泛 UI 仍属 `WP-R4-06`。 |
| 2026-08-24 | `WP-R4-05` | `Verification → Done` | clean source `a52688bd95e69e95e17917d8ff2d116380cc2b4f`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | Windows `10.0.26200` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:a52688bd95e69e95e17917d8ff2d116380cc2b4f SQLite 3.53.1 verified-local` | `A-CALENDAR-001–003` 与 `TEST-PLAN-008` 的 Calendar/Agenda/TBA 投影切片关闭；package 仅有现有 Vite 配置加载和 `inlineDynamicImports` 弃用 warning。macOS 未在本提交上重新 package/smoke，packaged 交互 E2E、签名、安装和公开发布不从本证据推断通过；广泛 UI 仍归 `WP-R4-06`。 |
| 2026-08-24 | `WP-R4-06` | `— → Ready` | `WP-R4-05` clean implementation source `a52688bd95e69e95e17917d8ff2d116380cc2b4f` 与本次证据提交 | `WP-R4-05` 注册表为 `Done`，目标测试、Windows package/smoke、文档追溯和独立复审均通过；`WP-R4-06` 硬依赖满足，Windows/macOS 输入环境证据依赖保持开放 | `WP-R4-06` 成为唯一 `Ready` 主链工作包；本次状态推进不实现首次设置、五项主导航、键盘/焦点、非颜色状态或广泛 UI。 |
| 2026-08-25 | `WP-R4-06` | `Ready → Verification` | implementation commit `6d50db96a4e59d9695286347912719fa425a5328`；checkpoint-status fix `508f905ea1101b96e47d7303316ffdd53197efe5` | TDD 回归先以 9/10 PASS、1 FAIL 精确捕获成功保存后状态文字未收敛，修复后 setup 聚焦回归 22/22 PASS；最终 `pnpm test` 382/382 PASS，`pnpm typecheck`、`git diff --check` PASS；44 个改动 TS/TSX 路径与 `styles.css` 的适用 FECS、120 字符与 tab 门禁 PASS；三轴独立只读复审最终 Critical/Important/Minor 均为 0、Ready Yes | 首次设置以 Shell draft checkpoint 支持退出/继续和提前进入 Today，最低条件为 Current Term、Current Course 与 Meeting 或 Task；字段级错误保留输入并聚焦首个错误，写入 unknown 保留原 command ID 精确重试。五项导航按固定顺序支持 Tab、方向键、Home/End；Today/Courses/Calendar/Tasks/Files 只消费 Setup/PLAN 真实投影并区分 empty、TBA、unavailable、read-only 与已结束学期。视觉固定浅色晨光渐变、半透明白卡和白卡内深色强调层，包含响应式、双层焦点、非颜色文字、对比度、forced-colors 与 reduced-motion CSS。未新增依赖、模拟数据、R5+ 后端或 Shell 绕过。 |
| 2026-08-25 | `WP-R4-06` | `Verification → Verification` | clean source `508f905ea1101b96e47d7303316ffdd53197efe5`；Windows x64 package `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe`；隔离临时 `LOCALAPPDATA` | Windows `10.0.26200` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:508f905ea1101b96e47d7303316ffdd53197efe5 SQLite 3.53.1 verified-local`。真实可见 packaged app 创建 Current Term `2026 Current WP-R4-06` / `2026-08-25`–`2026-08-31` / `America/Toronto`、Course `CSC301` / `Software Engineering` 和 date-only Task `Read chapter` / `2026-08-25`，最低条件达到 100% 后提前进入 Today；右方向键、End、Home 分别到 Courses、Files、Today，Calendar 七日列和 Tasks 显示同一真实 Task，Files 明确报告资料库投影不可用。任务完成只在持久化重查后变为已完成并出现 6 秒 Undo 状态文字；自动化往返超过 6 秒，未把 Undo 点击误报为 packaged 通过。正常退出并用同一隔离根重启后直接进入 Today，Term/Course/Task 与 completed 状态逐字保留 | 初始 implementation build 还实际验证了部分 Term 草稿“保存进度并退出”后从 Today 继续且名称逐字恢复，以及非法 IANA 时区保留输入、显示字段旁错误并聚焦该字段；该轮发现的过期“正在保存设置草稿…”状态已由后续 fix 与最新 package 中“正式数据已保存；设置进度已刷新”证据关闭。`WP-R4-06` 仍不得 `Done`：未取得 macOS arm64 同源码 package/smoke/交互、Windows Narrator 与 macOS VoiceOver、系统 High Contrast/Increase Contrast/Reduce Transparency/Reduced Motion 的真实运行、以及真实首次用户约 20 分钟完成参考设置的观察证据。Windows packaged Undo 点击本轮也未在 6 秒内完成；极长合法 Term 的 PLAN 分块性能边界未做平台基准。`WP-R5-01` 保持 `—`。隔离 E2E 根仍位于系统 Temp；删除请求被执行策略拒绝，未触碰既有开发数据根。 |
| 2026-08-25 | `WP-R4-06` | `Verification → Verification` | clean implementation source `7ae3368a2a9526616d75c7ddd8b7404e30b5b113`；window-frame commits `41d5dfc27644d113ec96a741003860de7d8f46c0`、`7ae3368a2a9526616d75c7ddd8b7404e30b5b113`；Windows x64 package `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe`；截图 `.superpowers/sdd/2026-08-25-window-frame/packaged-windows-setup-titlebar.png`、`.superpowers/sdd/2026-08-25-window-frame/packaged-windows-workspace-titlebar.png`、`.superpowers/sdd/2026-08-25-window-frame/packaged-windows-native-window-menu.png` | Windows `10.0.26200` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、SQLite `3.53.1`；BrowserWindow/Menu/window-control/drag-no-drag 回归包含在 `pnpm test` 396/396 PASS，`pnpm typecheck`、`git diff --check`、clean `pnpm package` 均 PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:7ae3368a2a9526616d75c7ddd8b7404e30b5b113 SQLite 3.53.1 verified-local`；独立只读复审最终 Critical/Important/Minor 均无、Ready Yes。真实可见 packaged app 无 File/Edit/View/Window 应用菜单，setup modal 与主 topbar 均显示三枚独立窗口按钮及可见键盘焦点；可访问树逐项暴露“最小化窗口”“最大化或还原窗口”“关闭窗口”。`Alt+Space` 原生系统菜单仍提供移动、大小、最小化、最大化和关闭；原生最大化/还原在 `813×601` 与 `3440×1392` 间切换，原生移动将窗口原点从 `(1319,396)` 移至 `(1308,688)`，原生 resize 将窗口从 `802×601` 调至 `813×601`。键盘激活自绘按钮后，最小化使窗口进入系统 minimized 状态，最大化/还原产生相同边界切换，关闭后目标窗口与进程均不存在 | Main 继续使用 `titleBarStyle: 'hidden'` 与 `thickFrame: true`，未使用 `frame: false`；Windows 仅移除应用菜单，macOS 全局菜单路径未改且以平台守卫隐藏 traffic lights。topbar、启动/问题面和 setup modal header 为 drag 区，全部交互控件为 no-drag；仅新增一个固定 channel、精确 action validator 的 Shell 窗口控制 IPC，未改变业务、DATA、Workspace 领域接口或 R5，也未新增依赖。Computer Use 的鼠标 `SendInput` 持续返回 Windows error 87，因此本源码的指针拖拽、标题栏双击和边缘 Snap 未取得真实操作证据；系统原生移动、resize、最大化/还原已由键盘系统菜单真实验证。按用户确认的独立自绘按钮取舍，最大化按钮 hover 不提供 Windows 11 Snap Layout 弹层。macOS 同源码 package/smoke、全局菜单/traffic lights 与交互仍未运行；既有 Narrator/VoiceOver、系统对比度/透明度/动效、约 20 分钟参考设置、Undo 与性能未验证项不变。`WP-R4-06` 保持 `Verification`，`WP-R5-01` 保持 `—`。 |
| 2026-08-25 | `WP-R4-06` | `Verification → Verification` | clean verification baseline `14f7228055c034abf7841971c2698d6114984692`（相对 packaged implementation `7ae3368a2a9526616d75c7ddd8b7404e30b5b113` 仅新增证据文档）；Windows x64 package `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe`；隔离系统 Temp 数据根 `CourseFlow-WP-R4-06-A11y-1787668815682`；本行所在本地提交 | Windows `10.0.26200` / AMD64。Narrator 与键盘从空隔离根创建 Term `WP-R4-06 Narrator 2026` / `2026-08-25`–`2026-08-31` / `America/Toronto`、Course `CSC406` / `Accessibility Validation` 和 TBA small once Task `Narrator keyboard validation`；三个正式保存检查点分别显示 33%、67%、100%，Narrator 播报“正式数据已保存；设置进度已刷新”，随后通过“进入今天”进入真实 Today。空必填提交把焦点送回 Term 名称，Narrator 逐项播报“学期名称”“编辑”“请输入学期名称。”“必需”“无效”；setup 全程可按既有顺序遍历 required 输入、选择、保存与退出控件，无焦点陷阱。主导航在 scan mode 关闭后以方向键按 Today → Courses → Calendar → Tasks → Files 顺序移动，Home/End 到首尾；Narrator 对当前项播报“按钮，当前页面”，五页分别显示同一真实 Course/Task、七日 Calendar、TBA 分组和 Files unavailable 非颜色文字状态。设置按钮播报“打开设置，按钮”；Return 打开“完成首次设置，对话框”，Escape 关闭后焦点和播报回到“打开设置，按钮”。窗口控件依次暴露“最小化窗口”“最大化或还原窗口”“关闭窗口”的按钮角色与动作说明；Enter 激活最小化后窗口立即离开前台且工具仍列出其句柄，测试会话随后中断，故不把持续 minimized 状态作为本轮新增证据；同一隔离根重启后数据逐字保留。Enter 激活最大化/还原在 `802×601` / `(1319,396)` 与 `3440×1392` / `(0,0)` 间往返，Enter 激活关闭后目标窗口和进程均不存在 | 系统设置修改前后逐值一致：High Contrast `Flags=126`、scheme 空，`EnableTransparency=1`、`MinAnimate=1`、`UserPreferencesMask=9E1E078012000000`。Contrast theme 从“无”切到“水生”并应用；forced-colors 下黑底、白字/边框、青色当前项/焦点及文字状态可读，键盘 Courses → Tasks 仍工作，再恢复“无”。透明效果从开切到关时玻璃效果消失但文字、卡片与双层焦点仍可读；动画效果从开切到关时页面内容、焦点和键盘换页仍完整；两项均恢复为开。Computer Use 本轮可发送真实双击：标题栏双击完成 `802×601 → 3440×1392 → 802×601`；两次标题栏 drag 返回成功但窗口原点不变，向屏幕边缘的 Snap drag 被工具以端点超出目标窗口边界拒绝，因此真实指针拖拽与 Snap 如实保持未验证。未发现实测产品缺陷，按约束未新增失败测试、未改代码，也未重复 package/smoke；本轮使用的 package/smoke 身份仍由上一行 `7ae3368` 证据拥有。macOS arm64 同源码 package/smoke/交互、VoiceOver、macOS Increase Contrast/Reduce Transparency/Reduced Motion、真实首次用户约 20 分钟参考设置观察、Windows packaged 6 秒内 Undo 点击、极长合法 Term 的平台性能基准，以及真实指针标题栏拖拽/Snap 仍未完成。`WP-R4-06` 保持 `Verification`，`WP-R5-01` 保持 `—`。 |
| 2026-08-25 | `WP-R4-06` | `Verification → Verification` | clean implementation source `7ae3368a2a9526616d75c7ddd8b7404e30b5b113`；macOS arm64 package `out/CourseFlow Dev-darwin-arm64/CourseFlow Dev.app`；隔离 worktree `/tmp/courseflow-wp-r4-06.qpCzxq/source`；隔离数据根 `/tmp/courseflow-wp-r4-06.qpCzxq/isolated-home`；本行所在本地提交 | macOS `26.5.2` / build `25F84` / arm64 / `Mac15,12`（Apple M3）；Node `24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、SQLite `3.53.1`。精确 detached source `7ae3368` 上 `pnpm install --frozen-lockfile`、`pnpm typecheck`、`pnpm package` 均 PASS；`pnpm test` 为 395 pass、0 fail、1 个 Windows 专属 skip；`pnpm smoke:packaged` 报 `PASS packaged smoke darwin/arm64 development:7ae3368a2a9526616d75c7ddd8b7404e30b5b113 SQLite 3.53.1 verified-local`。真实 packaged app 以 `HOME` 与 `CFFIXED_USER_HOME` 同时指向隔离根启动，helper `--user-data-dir`、ActivityControl、DataSlots 与 Chromium 均实际位于该根；从空根创建 Term `WP-R4-06 macOS 2026` / `2026-08-25`–`2026-08-31` / `America/Toronto`、Course `CSC406` / `macOS Accessibility Validation` 和 TBA small once Task `VoiceOver keyboard validation`，三个正式保存检查点分别为 33%、67%、100%。五项页面显示同一真实事实，方向键、Home/End 按固定顺序工作，Files 以文字报告资料库事实不可用；正常 quit 后 exact process count 为 0，同根重启直接进入 Today，Term/Course/Task 逐字保留，最终再次正常 quit 且 packaged process count 为 0。macOS 全局菜单实际保留 Apple、CourseFlow Dev、File、Edit、View、Window；Window 菜单实际展开并提供 Minimize、Zoom、全屏和 Bring All to Front。原生 traffic lights 在真实窗口中不可见，自绘三按钮可见；自绘最大化/还原为 `800×600 ↔ 1470×833`，自绘最小化使 on-screen 主窗口数变为 0 而 5 个 packaged 进程保留，Dock 点击恢复 `800×600`。VoiceOver 首次系统欢迎层保持“不再显示此信息”未选中并实际选择“使用旁白”；字幕面板逐项读出“最小化窗口”“最大化或还原窗口”“关闭窗口”的按钮角色，Today 为“当前页面，按钮”，主导航按 Today → Courses → Calendar → Tasks → Files 遍历。VoiceOver 以 VO-Space 打开“完成首次设置”对话框并读出标题级别 2，Escape 后焦点回到“打开设置”；Tasks 的真实项被读为 `TBA VoiceOver keyboard validation CSC406 · 小任务 TBA`，并暴露为 1/1 状态操作组。VoiceOver 关闭后主进程为 0。系统设置实测：Increase Contrast 为 `{increaseContrast=1, reduceTransparency=1}`，边界、文字和焦点仍清晰；关闭后两者均为 0。Reduce Transparency 单独为 `{increaseContrast=0, reduceTransparency=1}`，表面变为实色而内容与焦点保留；关闭后为 0。Reduced Motion 单独为 `{reduceMotion=1}`，键盘 Tasks → Files 与 unavailable 文字状态完整；关闭后为 0。四个原先不存在的偏好键经 macOS 规范化为显式 false；最终 `increaseContrast=0`、`reduceTransparency=0`、`reduceMotion=0`、`voiceOverOnOffKey=0`，输入源恢复 `Pinyin – Simplified`。首次仅改 `HOME` 的准备尝试未隔离 helper，立即退出且未提交 UI 领域命令，但既有开发根的 Chromium/session 文件与 active SQLite 被进程打开或触及；该尝试废弃且不计入上述证据，未删除或改写既有根 | 未发现实测产品缺陷，按约束未新增失败测试、未改实现；当前 package 是 development ad-hoc 制品，不是 R12 签名、公证或安装证据。macOS package/smoke、真实交互/重启、全局菜单/traffic lights、VoiceOver、Increase Contrast、Reduce Transparency 与 Reduced Motion 阻塞项由本行关闭；真实首次用户约 20 分钟完成参考设置观察、Windows packaged 6 秒内 Undo 点击、极长合法 Term 的平台性能基准及真实指针标题栏拖拽/Snap 仍未完成。`WP-R4-06` 保持 `Verification`，`WP-R5-01` 保持 `—`。 |
| 2026-08-25 | `WP-R4-06` | `Verification → Verification` | clean verification baseline `adc71ccf77784b5f455e4fcc6ac68a249b9c4e41`；Windows x64 package `out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe`；隔离系统 Temp 数据根 `CourseFlow-WP-R4-06-WinRemaining-1787689933747`；本行所在本地提交 | Windows 11 Home Chinese `10.0.26200` / build `26200` / x64；Intel Core Ultra 9 275HX / 24 logical processors / 32 GB RAM；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、SQLite `3.53.1`。clean `pnpm package` PASS（仅既有 Vite 配置原生加载与 `inlineDynamicImports` warning）；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:adc71ccf77784b5f455e4fcc6ac68a249b9c4e41 SQLite 3.53.1 verified-local`；bundled Node runtime 下 `pnpm test` 396/396 PASS、`pnpm typecheck` PASS。真实 packaged app 从空隔离根创建最大合法 Term `WP-R4-06 Maximum Legal Term` / `0001-01-01`–`9999-12-31` / `America/Toronto`、Course `PERF406` / `Maximum Term Performance` 和 TBA small once Task `Undo performance task`。完成任务后 Toast 在 `873.11 ms` 可操作，`942.84 ms` 内完成 Undo 并回到 pending；鼠标 hover 后再等待 `6.5 s` Toast 仍在，键盘在 `989.77 ms` 聚焦 Undo 后至 `8302.58 ms` 仍在，Enter 后 `940.25 ms` 回到 pending。Undo 后 Tasks 显示 pending，Calendar/Agenda 显示同一 TBA task 且无 completed 标记，Today 显示 `1` 项 TBA；同根重启直接进入 Today，Term、`739853 / 3652059` 学期日进度和 pending Task 在 Today/Tasks/Calendar 均保持，正常关闭后 exact packaged process count 为 0 | 最大合法 Term 的真实导航样本为 Today/PLAN `182.78 ms`、Calendar + Agenda `172.01 ms`，两者均在第 2 次可访问树观察收敛；规范尚未拥有参考硬件、Workspace 规模或毫秒门槛，因此仅记录环境与数值，不自创性能 PASS/FAIL。真实系统指针在同一 topbar drag 区双击可完成 `802×601 → 3440×1392`，但三次 `drag` 虽移动指针，窗口原点均保持 `(382,360)`；输入驱动未提供可独立确认的按键保持语义，窗口未进入 native move，故标题栏拖拽与边缘 Snap 仍未验证，不据此判定产品缺陷。未发现其余实测产品缺陷，按约束未新增失败测试或修改实现；隔离根保留在系统 Temp。Windows packaged `<6 s` Undo、hover/focus 暂停、跨视图/重启状态与极长合法 Term 数值测量缺口由本行关闭；真实首次用户约 20 分钟完成参考设置观察及真实指针标题栏拖拽/Snap 仍开放。`WP-R4-06` 保持 `Verification`，`WP-R5-01` 保持 `—`。 |
| 2026-08-25 | `WP-R4-06` | `Verification → Done` | existing Windows x64 package identity `adc71ccf77784b5f455e4fcc6ac68a249b9c4e41`；`out/CourseFlow Dev-win32-x64/CourseFlow Dev.exe`；clean documentation baseline `1499fae5c7d577d891f395a79b26c7e0ba9a83b9`；空隔离系统 Temp 数据根 `CourseFlow-WP-R4-06-Human-1787692730636`；真实首次用户报告、可见 Today 截图与窗口边界观察；本次证据提交 | Windows 11 Home Chinese `10.0.26200` / build `26200` / x64。packaged app 从首次设置 `0%` 启动；真实首次用户仅依据界面完成参考最低设置并进入显示 `2026秋季学期` / `2026-09-08`–`2027-01-04` / `America/Toronto` 的真实 Today，开始 `2026-08-25 17:20:21.547 -04:00`，结束 `17:24:29.344 -04:00`，耗时 `4 分 07.797 秒`，在约 20 分钟目标内，报告阻塞点为“无”。真实物理鼠标从 topbar 空白 drag 区移动窗口后，观察边界从 `(1425,189) 1088×802` 变为恢复态 `(1411,276) 1088×802`；用户按指定步骤实际确认左、右屏幕边缘 Snap 预览与释放后贴靠均通过 | 最后两项人工证据关闭，`TEST-USABILITY-001` 与 Windows 自绘标题栏真实指针拖拽/Snap 验收通过；结合前序同源码双平台 package/smoke、packaged 交互、重启持久化、键盘、读屏和系统视觉设置证据，`WP-R4-06` 关闭。未发现可复现产品缺陷，未触发 TDD 修复且未改实现。真实用户提出“适当增大默认窗口”和“移除右侧可见垂直滚动条”两项非阻塞 UI 后续改进；按用户要求仅记录，留待后续独立工作包处理，不纳入本次关闭改动。 |
| 2026-08-25 | `WP-R5-01` | `— → Ready` | `WP-R4-06` 最终人工验收与 `Verification → Done` 证据；本次证据提交 | `WP-R4-06` 注册表为 `Done`，R4 全部工作包关闭，R5 进入条件满足；`WP-R5-01` 的 `A-DATA-002`、`TEST-PROTECT-001` 与 ADR-03/05/07 数据位置边界保持既定 | `WP-R5-01` 成为唯一 `Ready` 主链工作包；本次只推进生命周期，不实现备份目的地、快照或 R5 代码。 |
| 2026-08-25 | `WP-R5-01` | `Ready → Verification` | clean implementation source 为本行所在本地提交 | TDD RED 先后锁定缺失 PROTECT 契约/存储/Workspace 路由、别名重叠、活动 DATA sibling、Windows device namespace、同 CommandId 改选目录、提交前失败残留、根与 Workspace 目录身份替换、超长 repository marker 和 disjoint mount-style identity；GREEN 后目标回归 56 项中 55 PASS、1 个 Windows file-link 权限 skip，`pnpm test` 426 项中 425 PASS、同 1 项 skip，`pnpm typecheck`、`git diff --check` PASS；28 个改动 TypeScript 路径的适用 FECS `@file`、120 字符、tab、禁用语法与新增单参数箭头门禁 PASS；独立只读复审最终 Critical/Important/Minor 均为 0、Ready Yes | schema level 12 持久化合法 `unconfigured` 或单一 BackupConfiguration/稳定 BackupSetId，并保持 revision、entity version、receipt、follow-up、watermark、pre/post-COMMIT、重放与重启语义。Shell 只发送 path-free Intent，Main 原生目录选择后交给 Workspace；PROTECT 通过窄 DATA/PLATFORM port 验证活动 DataSlotsRoot、可选 LibraryRoot 与目的地的 canonical path、volume identity 和 ancestor 隔离，只验证既有精确 marker/身份树并执行可回收写探针，配置提交不创建 repository、Workspace 或 BackupSet 目录。两个 BackupSet 只派生到各自 `CourseFlow/<WorkspaceId>/<BackupSetId>`，不枚举、选择、计数或清理其他集合。未实现 snapshot 生成、发布、保留、清理或恢复；clean package/smoke 待本源码提交后执行。当前 Windows 主机不允许创建 file symlink，因此 marker-link negative case 未实际执行；macOS 未在本提交上重新验证。 |
| 2026-08-25 | `WP-R5-01` | `Verification → Done` | clean source `708fa4febbc20cf6b498f952b288e7048955c299`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | Windows `10.0.26200` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:708fa4febbc20cf6b498f952b288e7048955c299 SQLite 3.53.1 verified-local` | `A-DATA-002` 与 `TEST-PROTECT-001` 的目的地配置、合法未配置态、三位置隔离及 repository/Workspace/BackupSet 身份隔离切片关闭。package 只有既有 Vite native config 与 `inlineDynamicImports` 弃用 warning。当前 Windows 主机不允许创建 file symlink，故 marker-link negative case 保持 skip；macOS arm64 未在本提交上重新 package/smoke，packaged 目录选择交互、签名、安装和公开发布不从本证据推断通过。snapshot 生成、发布、保留、清理和恢复均未实现。 |
| 2026-08-25 | `WP-R5-02` | `— → Ready` | `WP-R5-01` clean implementation source `708fa4febbc20cf6b498f952b288e7048955c299`、`Verification → Done` 证据与本次证据提交 | `WP-R5-01` 注册表为 `Done`，目标测试、Windows package/smoke、规范追溯、FECS 与独立复审均通过；`WP-R5-02` 硬依赖满足且无额外证据依赖 | `WP-R5-02` 成为唯一 `Ready` 主链工作包；本次只推进生命周期，不实现 snapshot 生成、发布、保留、清理或恢复。 |
| 2026-08-25 | `WP-R5-02` | `Ready → In Progress` | 本工作包（待提交） | `git status --short --branch` 确认工作树干净且 `main` 仅领先远端 2 个既有提交；定位 `TEST-PROTECT-002`、`TEST-DATA-004`、`FLOW-04`、ADR-04/07 与现有 DATA/PROTECT/PLATFORM 调用链 | 开始实现正式 DATA commit 的 DurableFollowUp 异步结构化 snapshot 切片；范围仅含 watermark、actual revision、staging/manifest/验证/发布/final 重验/成功登记和重启收敛，不实现 R5-03 保留/清理、状态 UI 或恢复。 |
| 2026-08-25 | `WP-R5-02` | `In Progress → Verification` | clean implementation source 为本行所在本地提交 | TDD RED 先后锁定 manifest、DATA operation、publisher/coordinator 与 Workspace wake 缺口；GREEN 后目标集合 31/31 PASS，`pnpm test` 453 项中 452 PASS、1 个既有 Windows file-link 权限 skip，`pnpm typecheck`、`git diff --check` PASS。全量首轮仅 packaged-smoke 后代进程清理时序用例抖动，独立重跑 3/3 与随后全量重跑均 PASS。改动 TypeScript 路径的 FECS `@file`、120 字符、tab、函数说明及新增语法辅助门禁 PASS；以用户合同和 `MODULE_CONTRACTS`/ADR-03/04/07 为 Spec 轴、AGENTS/FECS/Ponytail 为 Standards 轴的最终自审均无未解决 finding | schema level 13 持久化/合并 `backupNeededThrough` 与幂等 DurableFollowUp，按 Online Backup actual revision 串行执行同父 staging、同步关闭、逐成员 SHA-256、canonical manifest、staging full validation、持久 publishing intent、原子 rename、final fresh full validation 和 DATA success transaction；15 个阶段 failpoint、PostCommit 丢失/重复、重启与更高 revision 均收敛。未实现 R5-03 保留/清理、状态 UI、候选恢复或 Library-present 闭包；clean package/smoke 待本源码提交后执行。当前 Windows 主机不允许创建 file symlink，相关 link negative case保持 skip；macOS 未在本提交上重新验证。 |
| 2026-08-25 | `WP-R5-02` | `Verification → Done` | clean source `e2745ebea8909820344270d89d21a4a877f753ef`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | Windows `10.0.26200.0` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、packaged SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:e2745ebea8909820344270d89d21a4a877f753ef SQLite 3.53.1 verified-local` | `TEST-PROTECT-002`、`TEST-DATA-004` 与本切片 `FLOW-04` 关闭：正式 DATA commit 的本地成功不等待或回滚异步备份，持久 operation/watermark/follow-up 与结构化不可变 snapshot 在通知丢失、重复、阶段中断和重启后收敛。package 只有既有 Vite native config 与 `inlineDynamicImports` 弃用 warning。当前 Windows 主机不允许创建 file symlink；macOS arm64、真实 iCloud/OneDrive 同步器、packaged 目的地交互、签名、安装和公开发布未在本提交上验证。未实现 R5-03 保留/清理/状态、Library-present 闭包、候选恢复或 Restore。 |
| 2026-08-25 | `WP-R5-03` | `— → Ready` | `WP-R5-02` clean implementation source `e2745ebea8909820344270d89d21a4a877f753ef`、`Verification → Done` 证据与本次证据提交 | `WP-R5-02` 注册表为 `Done`，目标测试、全量测试/typecheck、FECS/规范自审与 Windows package/smoke 均通过；`WP-R5-03` 硬依赖满足且无额外证据依赖 | `WP-R5-03` 成为唯一 `Ready` 主链工作包；本次只推进生命周期，不实现最近两份保留、清理、状态 UI、候选恢复或 Restore。 |
| 2026-08-26 | `WP-R5-03` | `Ready → In Progress` | clean baseline `5c3cb9c484e88b8a10f0b7d9e27e498753b0b17e`；本工作包（待提交） | `git status --short --branch` 确认 `main` 与 `origin/main` 同步且工作树干净；定位 `A-DATA-003/004`、`TEST-PROTECT-003`、ADR-07 §9/10、schema level 13、现有 DATA/PROTECT/PLATFORM 调用链和 R5/R6 边界。Stop That Shit Guard 为 `OBSERVING / unconfirmed`，按用户的 change 合同只处理本包及必要 schema/调用者/测试/证据，不新增依赖 | 开始以 TDD 实现合法 unconfigured、backup pending/current、last success、最近两份 fresh verified snapshot 与 restartable cleanup；明确排除 R6 Restore、Library-present 完整闭包和 `WP-GA-01` 视觉优化。 |
| 2026-08-26 | `WP-R5-03` | `In Progress → Verification` | clean implementation source 为本行所在本地提交 | TDD RED/GREEN 覆盖 schema 13→14、持久 cleanup journal、七个 quarantine/delete 边界、fresh verified projection、最近两份保留、失败后本地成功/旧好快照/水位不回滚、unknown/other BackupSet/unregistered/corrupt/identity-conflict 不删除、父树与 snapshot 大小写别名、atomic publish/final record 两个重启及同 pass 窗口、newer snapshot 再验证、最旧 corrupt 或 missing registration 不阻塞后续保护，以及首个 member 删除后 surviving manifest 在重启或同 pass 被替换。聚焦 DATA/PROTECT/Workspace 回归 88/88 PASS；`pnpm test` 482 项中 481 PASS、0 FAIL、1 个既有 Windows file-link 权限 skip；`pnpm typecheck`、`git diff --check` PASS。全量首轮仅既有 packaged-smoke 后代进程的 500ms 退出观察窗抖动，隔离 3/3 与全量重跑均 PASS；19 个改动 TypeScript 路径的 FECS `@file`、120 字符、tab、禁用语法与新增单参数箭头门禁 PASS。独立只读复审提出的 projection freshness、真实 level-13 fixture、父级及发布 exact identity、verified candidate 选择与 member-level crash/同 pass 身份问题均已补红测并修复；对当前最终树复审为零个剩余 Critical/Important finding，`Ready to merge: Yes` | schema level 14 保存单一 `planned → quarantined → deleting` cleanup operation；DATA 原子登记/完成，PROTECT 在 exact read-only BackupSet 树中 fresh full-validate 后选择最旧 verified candidate，PLATFORM 只执行同父 quarantine 和非递归精确删除。删除固定为 database 后 manifest，每次 unlink 后重新经过 PROTECT 的 journal/manifest/root digest 和 newer-snapshot 验证；任何额外、替换、alias、冲突或不足两份 newer verified 均停止。公开 Workspace projection 不泄露路径，保留 durable last success，同时只列当前重新验证通过的最近两份。未新增依赖，未进入 Restore、Library-present 闭包或 UI；clean Windows package/smoke 待本源码提交后执行。 |
| 2026-08-26 | `WP-R5-03` | `Verification → Done` | clean source `274b86a1a2a15623ee78801c0a74aa8fb78aefba`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | Windows `10.0.26200.0` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、packaged SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:274b86a1a2a15623ee78801c0a74aa8fb78aefba SQLite 3.53.1 verified-local` | `A-DATA-003/004` 与 `TEST-PROTECT-003` 的结构化 A-only 切片关闭：合法 unconfigured、持久 pending/current、last success、最近两份 fresh verified snapshot 及 restartable exact quarantine/delete 均成立；清理失败不回滚本地或 snapshot 成功，未知、其他集合、未登记、损坏与身份冲突条目保持不删除。package 只有既有 Vite native config 与 `inlineDynamicImports` 弃用 warning。当前 Windows 主机不允许创建 file symlink，相关 negative case 保持 skip；macOS arm64、真实 iCloud/OneDrive 同步器、packaged 目的地交互、签名、安装和公开发布未在本提交上验证。Library-present 完整闭包留在 R11，候选分类、安全恢复集、Restore 与激活留在 R6，`WP-GA-01` UI 未进入本包。 |
| 2026-08-26 | `WP-R6-01` | `— → Ready` | `WP-R5-03` clean implementation source `274b86a1a2a15623ee78801c0a74aa8fb78aefba`、`Verification → Done` 证据与本次证据提交 | `WP-R5-03` 注册表为 `Done`，目标测试、全量测试/typecheck、FECS、独立复审与 clean Windows package/smoke 均通过；`WP-R6-01` 硬依赖满足且无额外证据依赖 | `WP-R6-01` 成为唯一 `Ready` 主链工作包；本次只推进生命周期，不实现候选分类、RestoreSafetySet、影响预览、整库替换、自动合并或任何 R6 代码。 |
| 2026-08-26 | `WP-R6-01` | `Ready → Verification` | feature source `9d9df5790640ca3ce80fff4b19657aaa5ddc1a52`；schema-14 migration fix `f7af771c31a30846f019abd2eb910f20230016c0`；本次证据提交 | TDD RED/GREEN 覆盖 `verified`、`incomplete`、`corrupt`、`incompatible`、`unknown` 五类 fresh candidate revalidation、已登记 operation staging 排除、仅 verified 可开始、原 snapshot 不变、独立 candidate-validation copy/migration、持久 RestoreSession/CommandId 重放/并发确认、健康 current 的 A-only RestoreSafetySet、完整替换预览及 candidate/current revision/Library root/target/impact 每项绑定失效。最终目标集合 54/54 PASS；默认并发 `pnpm test` 首轮 503 项中 501 PASS、1 个 Windows file-link 权限 skip、1 个既有 packaged-smoke 进程树压力场景因 `taskkill.exe` 在 100 ms/1 s 窗口内 `ETIMEDOUT`；同一测试产物串行全量 503 项中 502 PASS、0 FAIL、同 1 项 skip，四个进程树压力场景全部 PASS；负载回落后精确 `pnpm test` 复跑同为 502 PASS、0 FAIL、1 skip。`pnpm typecheck`、`git diff --check` PASS；26 个改动 TypeScript 路径的适用 FECS `@file`、120 字符、tab 与尾随空白门禁 PASS。打包 smoke 首轮捕获 level 14 未进入 14→15 迁移；新增测试先 RED，再修正允许级别和迁移安全副本验证分支后 schema 22/22 PASS。Stop That Shit Guard 保持 `OBSERVING / unconfirmed`，未扩张用户 change 合同 | schema level 15 保存 path-free RestoreSession 与幂等 receipt。PROTECT 只从 exact repository/Workspace/BackupSet 树重新验证候选；验证副本位于 ActivityControlRoot 的 session 私有区域，不修改原 snapshot、不使用或冒充 activation staging。完整预览明确 `replacement=complete`、`automaticMerge=false`，绑定候选原件和验证副本、current revision、Library root identity/generation、target binding version 与影响摘要；任一事实变化均使确认回到 waiting-decision。健康且 Library absent 的 current 在确认时生成、同步发布并 fresh 验证独立 RestoreSafetySet；Shell → Workspace → PROTECT 与公开 DTO 均不泄露路径。未新增依赖，未实现 R6-02 激活/checkpoint/maintenance/继续/回滚，未实现 R11 Library-present 完整闭包或 `WP-GA-01`。 |
| 2026-08-26 | `WP-R6-01` | `Verification → Done` | clean source `f7af771c31a30846f019abd2eb910f20230016c0`（含 feature source `9d9df5790640ca3ce80fff4b19657aaa5ddc1a52`）；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | Windows `10.0.26200.0` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、packaged SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:f7af771c31a30846f019abd2eb910f20230016c0 SQLite 3.53.1 verified-local`；smoke 后只读复核活动 DATA 为 schema level 15，并保留一份 level 14 migration safety copy | `A-DATA-005/006`、`TEST-PROTECT-004` 与 `TEST-WORKSPACE-002` 的 pre-activation 恢复切片关闭：五类候选、verified-only session、健康 current RestoreSafetySet、完整替换/不自动合并预览、全部绑定事实失效和 path-free 边界均有证据。package 只有既有 Vite native config 与 `inlineDynamicImports` 弃用 warning。当前 Windows 主机不允许创建 file link，相关 negative case 保持 skip；macOS arm64、真实云盘同步器、签名、安装和公开发布未在本提交上验证。`WP-R6-02` 保持 `—`，其故障注入证据依赖和激活/继续/回滚状态机未进入；Library-present RestoreSafetySet 完整闭包仍属于 R11，`WP-GA-01` 未进入。 |
| 2026-08-26 | `WP-R6-02` | `— → Verification` | clean implementation source `3616fbf591af741d6515db2d0216d3bbcbeb9ab2`；本次证据提交 | TDD RED/GREEN 覆盖同卷 sibling staging、精确容量与关闭后重验、ActivityControlRoot 外部 immutable plan/write-ahead journal、armed checkpoint、前检查点取消、普通打开门禁、DATA commit-last、成功/回滚重开、重启证据重建、并发 action 串行、丢失外部镜像发布、连续终态链及严格 schema/receipt 校验；forward 11 个与 rollback 10 个物理阶段 failpoint、plan/journal/receipt 发布 failpoint 均收敛。目标恢复集合 128/128 PASS；`pnpm test` 585 项中 584 PASS、0 FAIL、1 个既有 Windows file-link 权限 skip；`pnpm typecheck`、`git diff --check` PASS；26 个改动 TypeScript 路径的 FECS `@file`、120 字符、tab、尾随空白、禁用语法与 ESNext 草案单参数箭头门禁 PASS。Stop That Shit Guard 保持 `OBSERVING / unconfirmed`，未扩张用户 change 合同 | schema level 16 保存 pre-checkpoint cancel 与 typed completion receipt；PLATFORM 只处理 DataSlotsRoot 同父 sibling，PROTECT 独占外部 activation plan/journal 与证据状态机，Workspace 在 checkpoint 后只暴露 path-free `resume`/`rollback` 并阻止普通打开。每个物理动作均先持久 intent，执行后重新观察并持久 observed；任何前检查点失败/取消保持旧活动 DATA，checkpoint 后只由 journal 与实际 slot 证据决定继续或回滚，候选 DATA 最后提交，成功与回滚都以重开验证为边界。未新增依赖，未进入 R6-03/04/05、Library-present 闭包或 `WP-GA-01`。 |
| 2026-08-26 | `WP-R6-02` | `Verification → Done` | clean source `3616fbf591af741d6515db2d0216d3bbcbeb9ab2`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本次证据提交 | Windows `10.0.26200.0` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、packaged SQLite `3.53.1`；clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:3616fbf591af741d6515db2d0216d3bbcbeb9ab2 SQLite 3.53.1 verified-local` | ADR-08、`FLOW-05` 与 `TEST-DATA-006` 的 A-only 激活切片关闭。package 仅有既有 Vite native config 与 `inlineDynamicImports` 弃用 warning。macOS arm64、真实 OS 进程强杀/掉电、真实跨卷拒绝、packaged Restore 交互 E2E、签名、安装与发布未在本提交上验证；现有阶段 failpoint 不冒充真实掉电证据。Library-present 完整闭包仍属于 R11；`WP-R6-03` 的精确旧/新开发 build fixture 尚未满足，因此保持 `—`，R6-03/04/05 与 `WP-GA-01` 均未进入。 |
| 2026-08-26 | `WP-R6-03` | `— → Ready` | pinned old source `2361554e7e0a18c11ed0ce3b4b1da7bab52a6940`；clean fixture/new source `6ed1c23f70d282338d824df553d67a8e8dca20bb`；[`courseflow-development-build-fixture-v1`](../../tests/scripts/development-build-migration.fixture.v1.json)；本次证据提交 | Windows `10.0.26200.0` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、packaged SQLite `3.53.1`。TDD 先以缺失 runner/stage 取得 RED，再以 5/5 输入与动作白名单测试 GREEN；精确新端 `pnpm test` 590 项中 589 PASS、0 FAIL、1 个既有 Windows file-link 权限 skip，`pnpm typecheck` PASS。fixture 在两个 clean detached worktree 以 frozen lockfile 固定 protocol 2、schema `15 → 16` 及 snapshot/repository/RestoreSafetySet/Restore activation V1 范围，并显式保持 MigrationSafetyCopy/MigrationRollbackHandoff 范围为空；旧 DATA owner 实际生成 schema 15 与 revision 1 setup 事实，新端在 `migration.after-safety-copy` 停止时验证一份 schema 15 safety copy且活动数据库哈希/事实不变，随后独立进程迁移为 schema 16/revision 2并再次独立重启重验；旧端打开新 schema 返回 `incompatible-version` 且活动数据库哈希不变，旧/新 Bootstrap reader 双向拒绝对方 `AppBuildId`。两端 clean `pnpm package` PASS；smoke 分别报告 `development:2361554e7e0a18c11ed0ce3b4b1da7bab52a6940` 与 `development:6ed1c23f70d282338d824df553d67a8e8dca20bb`、SQLite `3.53.1`、`verified-local` | 所有 worktree、DATA 与 smoke 根均位于可丢弃 OS 临时目录，完成后删除且未记录路径；未读取或写入真实数据，未新增依赖。JavaScript 与 ESNext 草案 FECS 原文门禁通过：3 个实现/测试路径具备 `@file`，4 空格、无 tab/尾随空白、单行不超过 120 字符、严格比较、无 `var`/直接 `eval`、单参数箭头与语句门禁通过；JSON/Markdown 无适用内容分支。模块/目录原文的历史 AMD 与单数目录条款和已批准 Node/ESM 及既有 `scripts`/`tests`/`docs` 结构冲突，按项目约束优先且本次未新增一级目录。package 仅保留既有 Vite native config 与 `inlineDynamicImports` warning。Stop That Shit Guard 为 `OBSERVING / unconfirmed`；本次只解除证据依赖并将 `WP-R6-03` 置 `Ready`，未实现 R6-03/04/05。macOS arm64、真实 OS 强杀/掉电、真实跨卷、签名/安装制品及 MigrationSafetyCopy/Handoff 运行时仍未验证，不从本证据推断通过。 |
| 2026-08-27 | `WP-R6-03` | `Ready → In Progress` | baseline `183a79ca269ad4a69106afa2d828b352a7c80688`；用户确认的 `WP-R6-03` change 合同 | 定位 ADR-04/08/10、`MODULE_CONTRACTS` 中 `MigrationSafetyCopyV1`、`MigrationRollbackHandoffV1`、`FLOW-07`、`TEST-DATA-007`、`TEST-PROTECT-007`、`TEST-WORKSPACE-005/007` 及 R6-02 activation/Restore mutex 调用链；确认只修改 DATA/PROTECT/Workspace/PLATFORM 必要契约、持久状态、同卷 write-ahead 物理切换、fixture 与测试 | 开始本包内核切片；保持 Restore 全局互斥、copy-before-write、最多一份已验证副本、exact source/target build、owner 内路径和无部分成功。未授权 R6-04 UI/影响说明/显式删除/恢复导航、R6-05 生命周期路由、依赖或发布流程。 |
| 2026-08-27 | `WP-R6-03` | `In Progress → Verification` | implementation source `f3b46238008f87fc3d812af44d5ec47e80a2f761` | TDD 覆盖 schema 15→16 首写前 safety copy、关闭/同步/重开验证、重启复用、单份替换、source revision 与稳定 DataSlot/database identity 绑定、copy 消费；handoff 覆盖 closed canonical write-ahead 状态、同卷 staged slot、source/target/other build 分类、持久 action actor、重启继续/取消、终态 receipt、Restore 全局互斥、256 条上界与第 257 条写前拒绝。最终 `pnpm test` 639 项中 638 PASS、0 FAIL、1 个既有 Windows file-link 权限 skip；`pnpm typecheck`、`git diff --check`、fixture 脚本 `node --check` 及适用 FECS 门禁 PASS。三个独立只读审计发现的 3 个 P1 均以回归测试修复，最终复审为 P0–P2 皆无、`Ready to commit: Yes` | DATA 在任何 schema 写入前发布并重验唯一安全副本；PROTECT 保存 path-free、build-bound handoff 和确定性恢复状态；PLATFORM 只执行 owner 内同卷 sibling 物理动作；Workspace 在 DATA open 前协调 Restore/handoff mutex，普通启动不自动重命名。无新增依赖，未实现 R6-04/05 或发布能力。 |
| 2026-08-27 | `WP-R6-03` | `Verification → Done` | implementation source `f3b46238008f87fc3d812af44d5ec47e80a2f761`；[`courseflow-development-build-fixture-v1`](../../tests/scripts/development-build-migration.fixture.v1.json)；本行所在证据提交 | Windows `10.0.26200.0` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、packaged SQLite `3.53.1`。clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:f3b46238008f87fc3d812af44d5ec47e80a2f761 SQLite 3.53.1 verified-local`。精确 fixture 在 clean detached worktree 固定 old `2361554e7e0a18c11ed0ce3b4b1da7bab52a6940` 与 new `f3b46238008f87fc3d812af44d5ec47e80a2f761`，两端 frozen install/package/smoke、新端全量 test/typecheck、schema `15 → 16`、copy-before-write、唯一 safety copy/restart reuse、wrong/mixed build stop、handoff write-ahead interruption/restart 与 source/other classification 全部 PASS；临时 worktree/data/smoke 根已删除且未记录路径 | `TEST-DATA-007`、`TEST-PROTECT-007`、`TEST-WORKSPACE-005/007` 与 `FLOW-07` 的本包内核切片关闭。fixture 明确证明 pinned old build 没有 HandoffV1 reader，不以新 reader 搭配旧 `AppBuildId` 冒充 compatible target；运行时 target exact-build 行为由注入的 closed build descriptor 覆盖，真实精确兼容目标制品仍是 `WP-R6-04` 的证据依赖。因此 `WP-R6-04` 保持 `—`。macOS arm64、真实 OS 强杀/掉电、真实跨卷、签名/安装/发布未在本提交上验证；不从阶段 failpoint 或 Windows 证据推断通过。 |
| 2026-08-27 | `WP-R6-04` | `— → Ready` | baseline `0ba54b4d9afa0690ede6b39fe731fec765ac56e6`；clean rollback target `5b18515eae0f9f80e1f7a2fcafbf008bb3326979`；clean source `9822ab770d295d086bd6db85ca1ed227abdea9e6`；[`courseflow-compatible-build-fixture-v1`](../../tests/scripts/compatible-development-build-rollback.fixture.v1.json)；本行所在证据提交 | Windows `10.0.26200.0` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、packaged SQLite `3.53.1`。TDD 先取得 7 PASS / 4 RED，再以 11/11 输入与 stage 白名单测试 GREEN。fixture 在两个 clean detached worktree frozen install；各 endpoint 自身 stage 从其 commit 与 compiled modules 生成并逐字段匹配 full commit、完整 `AppBuildId`、protocol/schema/repository 与 HandoffV1/safety reader descriptor，target 不借用 source reader。两个独立可丢弃 DATA/control 场景实际完成 schema `15 → 16`、write-ahead handoff、exact source cancel、exact target continue、两端 other stop、双向 mixed bootstrap stop 与终态跨进程重启；target 在 closed-byte consumption 前只读重开验证，成功后由 fresh target 进程普通可写重开 schema 15/revision 1。source 全量 `pnpm test` 642 项中 641 PASS、0 FAIL、1 个既有 Windows file-link 权限 skip；source/target `pnpm typecheck` PASS。两端 clean `pnpm package` PASS；smoke 分别报告 `development:5b18515eae0f9f80e1f7a2fcafbf008bb3326979` 与 `development:9822ab770d295d086bd6db85ca1ed227abdea9e6`、SQLite `3.53.1`、`verified-local` | 所有 worktree、DATA/control 与 smoke 根均位于 OS 临时目录，完成后删除且未记录路径；未读取或写入真实 DATA，未新增依赖。适用 JavaScript、ESNext 草案与模块 FECS 原文门禁通过；JSON/Markdown 无适用内容分支，历史 AMD 条款与已批准 Node/ESM 冲突时按项目约束优先。Stop That Shit Guard 为 `OBSERVING / unconfirmed`。本次只解除证据依赖并将 `WP-R6-04` 置 `Ready`；fixture-owned Library/FLOW-00 completion ports 不冒充 R6-04/05 实现。package 仅有既有 Vite native config 与 `inlineDynamicImports` warning。macOS arm64、真实 OS 强杀/掉电、真实跨卷、签名/安装/发布和 packaged 回退交互 E2E 未验证。 |
| 2026-08-27 | `WP-R6-04` | `Ready → In Progress` | baseline `c5673306e5c65409c52ecb686c86f607d42dd1b7`；用户确认的 `WP-R6-04` change 合同 | 定位 ADR-10、`FLOW-07`、`TEST-SHELL-005`、`TEST-WORKSPACE-007`、`TEST-PROTECT-007` 及既有 Shell → Workspace → PROTECT/DATA/PLATFORM 调用链；范围固定为 ApplicationBuildStatus、MigrationSafetyCopy 展示/显式删除、绑定 copy/current DATA/Library/source-target build/影响摘要的 preview-confirm，以及 maintenance/recovery 的 source cancel、target continue、other/mixed stop | 开始本包纵向切片；继续使用 path-free DTO、外部手动换包、键盘/焦点/状态公告、Restore 全局互斥与无部分成功。Stop That Shit Guard 为 `OBSERVING / unconfirmed`；未授权新增依赖、R6-05、R11/R12、GA、签名或发布。 |
| 2026-08-27 | `WP-R6-04` | `In Progress → Verification` | implementation sources `a51e960fe51d66aa198292b437305d182a6d0551`、`ae3bd3c8571aec31b4dd27a9ff7a8bd590e3c1bb` | TDD 覆盖 build status 封闭 DTO、copy 显式删除与响应丢失重放、preview 全事实绑定失效、confirm/handoff、maintenance/recovery 全状态导航、exact source cancel、exact target continue、other/mixed stop 和错误 build 不变。目标聚合初次运行 134 项中 132 PASS、2 个 CSS media query 位置断言失败；修正测试定位后精确复跑 2/2 PASS，随后 fresh `pnpm test` 660 项中 659 PASS、0 FAIL、1 个既有 Windows file-link 权限 skip；`pnpm typecheck`、`git diff --check` PASS。两个独立代码审查分别发现删除物理清理中断可能伪报失败和 BuildStatus 重复工具版本；均先补失败测试再修复，删除以同父 quarantine 作为逻辑提交边界并允许同 CommandId 恢复清理，BuildStatus 只保留应用/运行时事实 | Renderer 只经 preload/Workspace 读取 path-free 状态；preview token 绑定安全副本、当前 DATA、Library、source/target build 与影响摘要；confirm 成功后才进入 maintenance 并退出等待外部换包；source/target 只有在 DATA 重开、Library absent 门或全量对账端口及 `FLOW-00` 完成后才报告终态。适用 FECS JavaScript、ESNext/React 草案、HTML、CSS 与模块原文已逐分支复核；新函数 JSDoc、颜色缩写和组件 media query 同位已落实。历史四空格/默认导出条款与既有 TypeScript/TSX 文件约定及最小 diff 冲突时维持项目约束；forced-colors 系统色按可访问性平台要求保留。无新增依赖。 |
| 2026-08-27 | `WP-R6-04` | `Verification → Done` | clean implementation source `ae3bd3c8571aec31b4dd27a9ff7a8bd590e3c1bb`；clean rollback target `5b18515eae0f9f80e1f7a2fcafbf008bb3326979`；[`courseflow-compatible-build-fixture-v1`](../../tests/scripts/compatible-development-build-rollback.fixture.v1.json)；本行所在证据提交 | Windows `10.0.26200.0` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、packaged SQLite `3.53.1`。更新后的 fixture 在两个 clean detached worktree frozen install，逐端验证自身 commit、完整 AppBuildId、protocol/schema/format reader；source full test 为 660 项中 659 PASS、0 FAIL、1 skip，source/target typecheck PASS。两端 clean package/smoke PASS：target `development:5b18515eae0f9f80e1f7a2fcafbf008bb3326979`、source `development:ae3bd3c8571aec31b4dd27a9ff7a8bd590e3c1bb`，均为 SQLite `3.53.1` / `verified-local`；两个独立 DATA/control 场景的 source cancel、target continue、两端 other stop、双向 mixed stop 与跨进程终态重启全部 PASS | `TEST-SHELL-005` 的文字、键盘、焦点、状态公告和 planned/prepared/awaiting-target/wrong-build/completing/recovery 导航由 Shell 测试关闭；`TEST-WORKSPACE-007` 与 `TEST-PROTECT-007` 的 exact-build 动作、持久重放、冲突停止和无部分成功由目标/全量测试及兼容 fixture 关闭。所有 fixture 临时根均已删除且未记录路径；未读取或写入真实 DATA。package 仅有既有 Vite native config 与 `inlineDynamicImports` warning。macOS arm64、真实 OS 强杀/掉电、真实跨卷、目标 packaged UI 的实际回退交互、签名、安装与公开发布未验证，分别保留给后续平台/发布门禁，不从 Windows 开发 fixture 推断通过。`WP-R6-05`、R11/R12、`WP-GA-01` 仍为 `—`，均未进入。 |
| 2026-08-27 | `WP-R6-05` | `— → In Progress` | baseline `10b4349729e9ee4f098ee5068955595293dc47da`；用户确认的 `WP-R6-05` change 合同；[`WP-R6-05 implementation plan`](../superpowers/plans/2026-08-27-wp-r6-05-implementation.md) | 已定位 `FLOW-00`、`TEST-WORKSPACE-003/004/005`、`TEST-FLOW-00-LIFECYCLE` 及 Shell → Workspace → PROTECT/DATA 调用链。基线 `pnpm test` 为 660 项中 657 PASS、2 FAIL、1 skip；两处失败均来自 Windows packaged-smoke timeout 清理在根进程退出后同步启动 PowerShell descendant discovery，满载时 discovery 耗尽既有 grace，实际 `pnpm package` 与 `pnpm smoke:packaged` happy path 均 PASS | 开始 TDD 纵向切片：统一 pre-DATA PROTECT 判定、DATA/Operation/follow-up 恢复、mode/capability/health/route 聚合、显式 welcome 初始化及事件驱动 smoke 清理。Stop That Shit Guard 为 `OBSERVING / unconfirmed`；不新增依赖，不进入 `WP-GA-01`、R7、R11/R12，不放宽 timeout 或省略 residue postcondition。 |
| 2026-08-27 | `WP-R6-05` | `In Progress → Verification` | clean implementation source `c6c7a351cc2771998a1085e8b6a68b05cff54d1f`；本次证据提交 | TDD RED/GREEN 覆盖 protocol 3 与 path-free lifecycle DTO、统一 pre-DATA PROTECT 分类、welcome/setup/today/maintenance/recovery、自动归档后再次重启、read-only、peripheral unavailable/degraded/recovering、pending follow-up、Restore/MigrationRollback 中间态与终态 Operation；显式 source cancel 终态曾先暴露旧 `waiting-decision`，ApplicationBuildStatus 曾仍报告 protocol 2，均以失败测试定位后修正。最终目标集合 100/100 PASS；fresh `pnpm test` 676 项中 675 PASS、0 FAIL、1 个既有 Windows file-link 权限 skip；`pnpm typecheck` 与 `git diff --check` PASS | packaged-smoke 根因为 timeout 回调才同步启动 PowerShell descendant discovery，阻塞 libuv root-exit 回调并耗尽既有 grace；改为 root `exit` 时异步启动并缓存 WMI discovery，所有 helper 共享绝对 deadline，异步 taskkill 后轮询 exact PID 无残留。生产 `20_000/1_000 ms` 及所有测试 timeout/grace 均未放宽，单测与全量负载下五个真实进程树场景均 PASS。FECS 逐路径路由 34 个改动文件中的 32 个 JS/TS/TSX：tab、尾随空白、120 字符、松散等号、动态执行均 0 违规，7 个新增代码文件均有 `@file`；历史 AMD/default-export 条款与已批准 ESM/TypeScript 及既有最小 diff 冲突时按项目约束优先。无新增依赖。 |
| 2026-08-27 | `WP-R6-05` | `Verification → Done` | clean implementation source `c6c7a351cc2771998a1085e8b6a68b05cff54d1f`；Windows x64 package `out/CourseFlow Dev-win32-x64`；本行所在证据提交 | Windows `10.0.26200.0` / AMD64；Node `v24.19.0`、pnpm `11.19.0`、Electron `43.4.1`、packaged SQLite `3.53.1`。clean `pnpm package` PASS；`pnpm smoke:packaged` 报 `PASS packaged smoke win32/x64 development:c6c7a351cc2771998a1085e8b6a68b05cff54d1f SQLite 3.53.1 verified-local` | `FLOW-00`、`TEST-WORKSPACE-003/004/005` 与 `TEST-FLOW-00-LIFECYCLE` 的 A-only 切片关闭：PROTECT 统一判定先于 DATA open，普通路由后才唤醒 backup；启动不执行物理 resume/rollback/continue/cancel；Shell 只消费 Workspace lifecycle，仍经 preload/Main/单一 Workspace utility，DTO 不含路径。package 仅有既有 Vite native config 与 `inlineDynamicImports` warning。macOS arm64、真实 OS 强杀/掉电、真实跨卷、packaged 生命周期人工交互、签名、安装与公开发布未验证；当前没有 Library watcher，故 watcher 前置顺序只有架构边界而无 R11/R12 运行时证据。`WP-GA-01`、R7、R11/R12 均未进入。 |
| 2026-08-28 | `WP-RF-01` | `— → Ready` | 本行所在提交；[已批准重构计划](../superpowers/plans/2026-08-28-courseflow-physical-architecture-refactor.md) | 计划经用户逐项确认（动机、全物理架构深度、全区域范围、main + Backlog 证据工作流）后批准 | 行为保持重构；不实施 `WP-GA-01` 与 2026-08-28 新模型；验证协议与环境边界见计划 §6。 |
| 2026-08-28 | `WP-RF-01` | `Ready → In Progress` | 本行所在提交 | Linux x64 / Node `v22.22.2` 容器基线：`tsc --noEmit`（主/测试配置）通过；78 文件 676 用例 = 667 通过 / 5 跳过 / 4 环境性失败（2 个 packaged-smoke 后代进程清理、1 个大小写不敏感文件系统假设、1 个 `ApplicationBuildStatus` darwin/win32 平台守卫按设计在 Linux 抛出） | 重构全程要求失败集合 ⊆ 该 4 项且用例总数不减；Windows x64 / Node 24 复验留给用户，登记为未验证项。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage B） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 用例 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合与基线一致）；`git diff --check` | 计划 Stage B：从 `sqlite-data-store.ts` 抽出模块级纯领域函数 932 行入 `src/plan/`（local-date、anchors、meeting-occurrences、meeting-overlap、confirmation-tokens、task-schedule 六个文件，含新增导入合计 1,046 行），store 改为从 `../plan/*` 导入；实际抽取行数低于计划 §5 预估（2.3–2.5k 行），其余部分属于 Stage C 的类内拆分，不影响范围。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage C1） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage C1：`sqlite-data-store.ts` 模块级辅助（公共类型与错误、行映射、结果/冲突构造器、命令守卫、数据库/failpoint 辅助，共约 1,600 行）拆入 `src/data/store/{types,guards,rows,results,database}.ts`，facade 显式聚合导出，公共导出集合不变；`runtime-boundaries` 的 `node:sqlite` 导入者断言由精确三文件改为"仅限 `src/data/` 数据层（含 `store/`、`schema/` 子目录）且必含 `sqlite-data-store.ts`"，语义（sqlite 只属于 DATA 层）不变。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage C2） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage C2：11 个 `commit*Synchronously` 命令族、调度器与 `writeSetupDraftSynchronously`（约 3,900 行）自 store 类迁出为 `src/data/store/commits/{dispatch,setup,backup-configuration,term,course,meeting,holiday,task}.ts` 的自由函数；新增 `StoreContext` 端口（`store/context.ts`）由类以闭包构造，函数体仅做 `this.*` → 端口调用的机械改写；`currentVersions`/`readReceiptOutcome`/`advanceSetupMinimumMilestone`/`insertRestoreCommandReceipt`/`readActiveHolidayRanges`/`readConflictMeetingOccurrences` 改为以 `DatabaseSync` 为参的共享函数（`store/context.ts`、`store/conflict-reads.ts`）；facade 由 8,914 行降至约 4,700 行，公共 API 不变。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage C3） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage C3：八个投影读取/预览方法（setup、plan source、task detail/preview、meeting detail/preview，约 1,600 行）迁至 `src/data/store/reads/{setup,plan,task,meeting}.ts`；`StoreContext` 增加 `requireOpen()`；类保留同签名公共委托方法，公共 API 与调用序（含嵌套 requireOpen）不变；facade 降至约 3,100 行。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage C4） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage C4：备份/清理/恢复操作持久化与保护投影（33 个公共方法与 6 个行辅助，约 1,350 行）迁至 `src/data/store/protection/{follow-ups,backup-operations,backup-cleanup,projection,restore-store,restore-candidates}.ts`；`StoreContext` 增加 `requireBackupMutationAllowed()`；类保留同签名委托方法；`runtime-boundaries` 的数据层 `node:sqlite` 白名单正则允许 `store/`、`schema/` 子目录嵌套（仍禁止数据层之外导入）；facade 降至约 1,750 行。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage C5，Stage C 完成） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage C5：store 内核类迁至 `store/kernel.ts`（队列/事务/终态与公共委托面），DATA 槽位检查、初始化、打开与前向迁移入口迁至 `store/open.ts`；`sqlite-data-store.ts` 成为约 70 行的纯公共聚合导出（导出集合与 C 阶段开始前一致）。Stage C 合计把 11,230 行单文件重组为 1 个 facade + 22 个内聚单元。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage C5 验收修正） | 本行所在提交 | 修正后重跑全套门禁：`tsc --noEmit`（主/测试配置）、全量 `node --test` 676 = 667 通过 / 5 跳过 / 4 已知环境性失败、`git diff --check` 全部通过 | 如实登记：上一提交（Stage C5）在提交时 `runtime-boundaries` 有一项红断言——facade 变为纯聚合导出后不再直接导入 `node:sqlite`，C1 修改中"导入者必含 `sqlite-data-store.ts`"的断言失效；执行侧验证脚本管道尾随 `tail` 掩盖了门禁退出码，导致带红提交。本提交把断言更正为"导入者非空且必含 `store/kernel.ts`（实际连接持有者），仍全部限于数据层"，并已修复验证脚本的退出码传播；此后所有阶段按未掩盖退出码执行。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage D） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；实测拆分前后 63 个 `LEVEL_*` 常量逐一 `===` 相等（编译产物字节比对：identical 63/63、diff 0、missing 0）；`git diff --check` | Stage D：`schema.ts`（4,566 行）按语义拆为 `schema/{base,tables,validation,migrations,create}.ts` 与 `schema/levels/level-01..16.ts`，facade 保留原全部导出；DDL 字符串、迁移与验证语义零改动（ADR-04 协议不受影响）。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage E1） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage E1：`workspace-application.ts`、`workspace-lifecycle.ts` 以 `git mv` 迁入新 `src/workspace/` 包（`application.ts`、`lifecycle.ts`），同步全部 `src`/`tests` 导入路径与 `workspace-entry-boundary` 的固定路径；内容零改动。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage E2） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage E2：`WorkspaceApplication`（3,563 行单类）按语义拆为 `src/workspace/{host,projections,outcomes,build-status,migration-boot,migration-rollback,protection,restore,lifecycle-routing,setup-commands,plan-queries,plan-commands}.ts`；类保留 `open/handle/close/waitForDurableBackups` 与请求路由，新增 `WorkspaceHost` 端口以闭包暴露可变状态；两处与数据层同名函数（`deleteMigrationSafetyCopy`、`configureBackupDestination`）以别名消除自遮蔽；为恢复 TS 窄化引入的本地变量在门禁中暴露一处过期引用（`workspaceLifecycle` 在回执恢复重开后仍用旧 store），已改回即时读取并对全部恢复触发路径完成同类审计（`querySetup` 同修，其余无过期引用）；该缺陷由本阶段门禁在提交前拦截，未进入任何提交。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage E3，Stage E 完成） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage E3：七个 `*CommandOutcome` 装配器去重为 `singleEffectCommandOutcome`/`committedEnvelope` 两个共享助手（`outcomes.ts` 476→358 行，各族允许的 effect code、实体 kind、领域文案与 undo 语义保持逐一显式）；`main.ts` 手工枚举的 27 个请求 kind 移交契约所有：`workspace-setup-contract.ts` 新增 `WORKSPACE_SETUP_VALIDATION_REQUEST_KINDS`，`main.ts` 改为查表；`runtime-boundaries` 两个分类测试改为断言"分类来自契约清单 + 每个 kind 在清单内"，语义（已知 kind 一律 `validation`）不变。 |
| 2026-08-28 | `WP-RF-01` | `In Progress`（Stage F1） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage F1：`workspace-pages.tsx`（1,980 行）拆为 `renderer/pages/{shared,widgets,today,courses,calendar,tasks,files}`；`workspace-pages.tsx` 保留 `WorkspacePage` 与四个 Props 类型并聚合导出五个页面与 `TaskActionNotice`，renderer 测试导入路径不变。 |
| 2026-08-29 | `WP-RF-01` | `In Progress`（Stage F2） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage F2：`SetupDialog.tsx`（3,387 行）拆为 `renderer/setup/{mutation,checklist,field-errors,labels,progress,forms}`；`SetupDialog.tsx` 保留对话框组合层（807 行）并聚合导出既有公共名（含 `tests/renderer/setup-dialog.test.ts` 的命名空间导入面）；`setup-ui` 架构测试的原文扫描改为"入口文件 + 对应子目录并集"，切片标记与断言语义不变。 |
| 2026-08-29 | `WP-RF-01` | `In Progress`（Stage F3，Stage F 完成） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage F3：`App.tsx`（1,575 行）的装载与任务动作运行时拆入 `renderer/app/{load-workspace,task-actions}.ts`；`App.tsx` 保留 `App`/`WorkspaceShell`/`WelcomeSurface` 组合层（约 900 行）并聚合导出既有公共名；`setup-ui` 的 `app` 原文扫描同步为"入口 + `renderer/app/` 并集"。Renderer 三个超大文件拆分完成：1,980/3,387/1,575 → 组合层 146/807/902 + 15 个内聚模块。 |
| 2026-08-29 | `WP-RF-01` | `In Progress`（Stage G） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage G：两个数据安全内核按协议层拆分——`restore-activation.ts`（2,621 行）拆为 `restore-activation/{protocol,records,evidence,activation}`，`migration-rollback-handoff.ts`（2,306 行）拆为 `migration-rollback-handoff/{protocol,records,evidence,handoff}`；两个原模块路径保留为公共聚合导出（各约 100 行），公共导出集合、控制流与记录协议零改动。 |
| 2026-08-29 | `WP-RF-01` | `In Progress`（Stage H） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：676 = 667 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage H：四个超大契约（course 2,257 / setup 2,216 / task 2,029 / plan 1,271 行）各拆为 `<contract>/{types,guards,makers}.ts`（plan 无 makers），原模块路径保留为聚合导出（38–101 行），公共导出集合不变；请求 kind 清单已在 Stage E3 归契约所有。 |
| 2026-08-29 | `WP-RF-01` | `In Progress → Verification`（Stage I） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：677 用例（新增依赖方向守卫）= 668 通过 / 5 跳过 / 4 已知环境性失败（集合不变）；`git diff --check` | Stage I：新增 `tests/architecture/module-dependency.test.ts`，对 `src/` 全部相对导入强制执行计划 §4 依赖方向矩阵（当前实际图为其严格子集：workspace→{protect,data,platform,shared}、protect→{data,platform,shared}、data→{plan,platform,shared}、renderer/main/preload/plan/platform→shared）；`AGENTS.md` §项目状态 移除过期执行点描述，指向 Backlog/Roadmap 事实源。容器内九个阶段全部完成；`WP-RF-01` 进入 `Verification`，等待用户在 Windows x64 / Node 24 复验 `pnpm typecheck`、`pnpm test`、`pnpm package`、`pnpm smoke:packaged` 后关闭。 |
| 2026-08-29 | `WP-RF-01` | `Verification`（签名剥离重放） | 本行所在提交；重放后链 `dbadb2c…5c0c97d` | `git diff 69a2e01 5c0c97d` 为空（树逐字节等同已全量门禁通过的原链末端）；`git log` 逐提交比对作者、邮箱、作者/提交时间与信息完全一致；`%G?` 确认 18 个提交均无签名；`git diff --check` | 用户批准：执行环境曾以容器 SSH 密钥自动签署 18 个提交（作者身份不受影响），会在 GitHub 显示 Unverified；按用户选择以 `filter-branch` 原位重放剥离签名，内容、信息、作者与时间零改动，仅提交哈希变更（原链 `64fe18c…69a2e01` 废弃）。执行环境已设 `commit.gpgsign=false` 防复发。 |
| 2026-08-29 | `WP-UI-01` | `— → In Progress`（切片 1） | 本行所在提交 | `tsc --noEmit`（主/测试配置）；全量 `node --test`：677 = 668 通过 / 5 跳过 / 4 已知环境性失败（集合不变，`setup-ui`/`workspace-shell` 的 CSS 契约断言全部保持）；`git diff --check` | 用户报告并授权两项：①窗口控件边框交界白色伪影——根因是半透明 1px 边框 + inset 高光在分数像素尺寸（2.35rem）圆形上的抗锯齿泄漏；改为 38px 无边框实心圆（黄/白/墨），悬停提升、按下缩放。②整体视觉对齐用户提供的参考图——内容卡片由灰色半透明磨砂（`backdrop-filter blur(24px)`）改为暖白近不透明材质 + 柔和暖阴影（用户明确允许更换实现），毛玻璃只保留在标题栏、浮层与对话框；全局令牌暖化（画布渐变、暖色描边、更大圆角、暖阴影）；新增页面进入、状态卡上浮、对话框弹出、背景遮罩淡入动效与按钮悬停/按下反馈，全部受既有 `prefers-reduced-motion` 全局覆盖约束；forced-colors / prefers-contrast / prefers-reduced-transparency 分支同步。仅改 `styles.css`（2,363→2,467 行）。`WP-GA-01` 计划针对的布局属性（body 留白、根滚动、`.workspace-content` 滚动所有权）未触碰。 |
| 2026-08-29 | `WP-UI-01` | `In Progress`（切片 2） | 本行所在提交 | `tsc --noEmit`（主/测试配置）PASS；分块全量 `node --test`：692 项 = 685 通过 / 5 跳过 / 2 已知环境性失败（`ADR-07/TEST-PROTECT-003` 大小写不敏感 FS 假设、`TEST-WORKSPACE-007/PROTECT-007` 的 `ApplicationReleaseDescriptor` 只接受 `darwin`/`win32` 而当前 Linux 容器挂载主机为 `linux`）；`git diff --check` PASS；改动路径无新增超 120 字符行、tab 或尾随空白；以真实组件 SSR + 真实 `styles.css` 在 Chromium `1280×800` 与 `960×640` 两个视口取得布局度量：`documentElement.scrollHeight === clientHeight`（无根滚动条）、`.workspace-frame` 为 `x=0,y=0` 且等于视口尺寸（无外缘）、`.setup-progress-card` `scrollHeight === clientHeight`（无侧栏滚动条）、黑卡底与白卡底间距 `144px`/`36px` | 用户 2026-08-29 逐项授权的七项桌面缺陷修复：①`BrowserWindow` 显式 `1280×800` + 最小 `960×640`（取代 `WP-GA-01` 计划的 `1120×720`，计划文本已同步）；②`.setup-modal` 与 `.migration-dialog-surface` 的半透明白边框 + 白色 inset 高光在圆角处抗锯齿泄漏成白斑，改为暖色 hairline 并去掉 inset 高光；③`.workspace-content`、`.startup-surface`、`.setup-progress-card`、`.setup-workspace`、`.migration-dialog-body`、`.primary-nav` 只隐藏可见 scrollbar，不使用 `overflow: hidden`，滚轮/触控板/键盘滚动保留；`.form-footer` 与 `.setup-modal-footer` 改为可换行并让说明文字可收缩，消除低分辨率下说明与命令按钮重叠；④首次设置对话框头部移除 `WindowControls` 与小 `×` 图标按钮，只保留「保存进度并退出」，窗口控件只留在 Workspace 顶栏与 startup 标题栏（`setup-ui`、`setup-dialog` 契约已反向锁定）；⑤`body` 去外围 padding 与文档滚动，`html`/`body`/`#root`/`.workspace-frame`/`.startup-frame` 受客户区高度约束，frame 去掉自有边框与圆角，成为贴合客户区的唯一视觉外壳；⑥新增 `SettingsDialog`：含左侧分类导航（学期与课程、数据与备份、关于与版本）的独立设置表面，顶栏「设置」改为打开它，原顶栏「数据与备份」入口收入其中，学期设置与数据保护由该表面转交既有对话框；⑦删除进度卡「当前任务 n/3」整行，只保留「设置进度」列，并给黑卡相对白卡底部留出可见间距 | 设置表面按已批准 `UI-SETTINGS-01`「独立页面和左侧分类导航」实现为窗口级模态页，保持五项主导航不变；MVP-A 只交付三个真实分类，`学期与历史`（C1）与`文件资料库`（R11）不建占位。`WP-GA-01` 计划的切片 1/2 代码已在本切片落地，其状态仍为 `—`，剩余的双平台 packaged 人工验收与 A-only G1–G7 未进入。本切片未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，也未做真实 packaged 应用的人工视觉、物理鼠标 Snap/最大化、High Contrast/Reduce Transparency/Reduced Motion 观察；上述截图度量来自 Chromium 无头渲染，不冒充 packaged Electron 证据。 |
| 2026-08-29 | `WP-UI-01` | `In Progress`（切片 3） | 本行所在提交 | `tsc --noEmit`（主/测试配置）PASS；`node --test .test-dist/tests/renderer/**`：89/89 通过；`node --test .test-dist/tests/architecture/**`：48/48 通过（含 `setup-ui` 与 `workspace-shell` 的 CSS 契约断言）；`git diff --check` PASS；以真实组件 SSR + 真实 `styles.css` 在 Chromium `1280×800`（DPR 2）取得改前/改后截图与对比度实测：白字对 `--dark` 12.82:1、`--ink` 对 `--card` 15.98:1、`#c9cac5` 对 `--elevated` 约 7.7:1，均高于 AA | 用户 2026-08-29 直接指令的两项参考图对齐。①卡片不透明度过高：`--card` `0.9 → 0.72`、`--card-strong` `0.97 → 0.88`，卡面重新吸收画布暖色渐变（暖区卡面与画布的蓝通道差由 26 降到 15）；`--card-inner` 保持 `0.66` 以维持卡内层级。②黑色过深：参考图暗面实测为中性 `#303030`（暗像素直方图主峰 25,082 px），`--dark` `#24231f → #303030`、`--elevated` `#26251f → #323232`（保留原有 2 级差），`.data-mode-status` 内联同值边框与 `prefers-reduced-transparency` 分支的两处 `#292a27` 同步为 `#303030`，`tests/renderer/workspace-shell.test.ts` 中锁定该值的断言随之更新。未改动 `--ink` 文本色，也未改动切片 1 已批准的 `.emphasis-layer` / `.setup-current-layer` 黄色径向高光，因此深色卡顶部仍保留暖色渲染。本切片未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，也未做 packaged 应用的人工视觉验收；上述度量来自 Chromium 无头渲染，不冒充 packaged Electron 证据。 |
| 2026-08-29 | `WP-UI-01` | `In Progress`（切片 4） | 本行所在提交 | `tsc --noEmit`（主/测试配置）PASS；`node --test .test-dist/tests/renderer/**`：89/89 通过；`node --test .test-dist/tests/architecture/**`：48/48 通过；`git diff --check` PASS；以真实 `SetupDialog` SSR + 真实 `styles.css` 在 Chromium 三个视口取得布局度量：`1280×800` 与 `960×640` 下 `.setup-progress-card` 与 `.setup-step-panel` 的 top/bottom 完全相等（`1280×800` 改前右栏底边 628.2 对左栏 663.2，改后同为 663.2）；`.setup-current-layer` 底边距卡片底边 13.8px（= 卡片 `0.8rem` 内边距 + 1px 描边）；`.setup-progress-pad` 已不存在于 DOM；`600×800` 单列分支的面板溢出量不劣于改前（body `scrollHeight` 1198 → 1176） | 用户 2026-08-29 逐项指令的四项。①黑卡背景不要黄色：`.setup-current-layer` 与 `.emphasis-layer` 去掉黄色径向渐变只留 `var(--elevated)`；`.setup-step-list li[data-status="当前"]` 底色由 `rgba(250,221,93,.16)` 改为 `rgba(255,255,255,.1)`，黄色只保留为强调色（「当前」小字 `#f7df78`、已完成步骤圆点 `var(--morning)`）——该取舍由用户在两个选项中当场确认。②左右两栏对齐：`.setup-workspace` 的 `align-content` 由 `start` 改为 `stretch`。③黑卡外圈细边框：删除 `.setup-progress-pad` 元素与样式（含三处可访问性分支的选择器列表），`.setup-progress-stack` 简化为无定位的填充容器，`.setup-current-layer` 去掉 `position`/`z-index`；`tests/renderer/setup-dialog.test.ts` 的对应断言改为「stack 直接包住 layer 且不再出现 pad」，测试名同步。④黑卡底部贴近左卡底部：`.setup-progress-card` 改为 `grid-template-rows: auto auto minmax(0, 1fr)`、内边距统一 `0.8rem`，黑卡随 stack 拉伸至距卡片底边一个内边距；四行步骤按用户选择保持顶端对齐（`.setup-current-layer` 加 `align-content: start`）。本切片未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，也未做 packaged 应用人工验收；上述度量来自 Chromium 无头渲染，不冒充 packaged Electron 证据。 |
| 2026-08-29 | `WP-UI-01` | `In Progress`（切片 5） | 本行所在提交 | `tsc --noEmit`（主配置）PASS；`tsc -p tsconfig.test.json` PASS；`node --test .test-dist/tests/renderer/*.test.js`：91/91 通过；`node --test .test-dist/tests/architecture/*.test.js`：48/48 通过；`git diff --check` PASS；以真实 `SetupDialog` / `CoursesPage` SSR + 真实 `styles.css` 在 Chromium `1280×800`（DPR 2）实测：改前完成态首次设置的两块 `.setup-step-panel` 盒子为 `top 10/bottom 280`（内容 384）与 `top 254`——后一块压在前一块溢出内容之上；改后为 `top 143/bottom 565` 与 `top 578/bottom 1002`，无重叠且 `.setup-workspace` 正常滚动；单块面板仍被 `align-content: stretch` 拉满（380/384）。课程页卡片由 `2 × 620px`、高约 700px 变为 `3 × 392px`、高 357px（空课节卡 274px） | 用户 2026-08-29 逐项授权的两项。①完成学期/课程/任务后进入假期步骤时首次设置页错位：根因是 `.setup-step-panel { min-height: 100% }`——百分比 `min-height` 在网格项上会让行尺寸与项尺寸脱钩，两块面板同时出现（完成概览 + 假期表单）时后一块压在前一块的溢出内容上；删除该声明即可，`.setup-workspace` 的 `align-content: stretch` 仍保证单块面板铺满右栏（已用最小复现验证，见上）。②课程页卡片过大：`.workspace-grid--courses` 由固定两列改为 `repeat(auto-fill, minmax(21rem, 1fr))`；`.course-card` 自带更小内边距并 `align-content: start`（左侧色条侧额外留白）；标题 `1.4rem → 1.15rem`；`当前` 徽标加 `align-self: start`，不再被两行表头拉成长圆；课程事实由带底色的方块改为两列「标签—值」行，`教学范围` 独占整行以免半宽折行；课节规则行改为两行两列（类型/时间、地点/生效范围）。仅改 `styles.css`（`.setup-step-panel` 与课程卡片作用域内的规则），未改任何 TSX、DOM 结构或 Workspace 接口。`tests/renderer/workspace-shell.test.ts` 新增两条 CSS 契约断言反向锁定这两项（对 `HEAD` 的旧样式已确认为 RED）。本切片未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，也未做 packaged 应用人工验收；上述度量来自 Chromium 无头渲染，不冒充 packaged Electron 证据。 |
| 2026-08-29 | `WP-UI-01` | `In Progress`（切片 6） | 本行所在提交 | `tsc --noEmit`（主配置）PASS；`tsc -p tsconfig.test.json` PASS；分块全量 `node --test`：`renderer` 95/95、`architecture` 48/48、`data` 181/181、`shared` 99/99、`main` 9/9、`platform` 11/11（1 跳过）、`scripts` 13/13（3 跳过）、`protect` 171/173（1 已知环境性失败：`ADR-07/TEST-PROTECT-003` 大小写不敏感 FS）、`tests/*.test.js` 57/58（1 已知环境性失败：`TEST-WORKSPACE-007/PROTECT-007` 的 `ApplicationReleaseDescriptor` 不接受 `linux`）；`git diff --check` PASS；以真实组件 SSR + 真实 `styles.css` 在 Chromium `1280×800`（DPR 2）实测九个表面：全部 `documentElement.scrollHeight === clientHeight`（无根滚动）；首次设置模态 700px 高、右栏 `520/520` 不溢出；管理模态 760px 高，右栏 `clientHeight 605` 与内容 `605–992` 由 `.settings-panel` 独立滚动 | 用户 2026-08-29 直接指令的两项产品行为变更（先改产品/架构规范再改实现与测试）。①简化首次设置：最低完成条件由 `Current Term + 至少一门 Course` 改为 `Current Term`。`readSetupProjection` 的 `isSatisfied` 改为 `hasCurrentTerm`；`advanceSetupMinimumMilestone` 的 SQL 改为只要求 `plan_state.current_term_id IS NOT NULL`；`everReachedMinimum` 改为「持久里程碑 OR 当前已满足」——当前满足即蕴含曾经达到，避免旧库要等下一次提交才推进；`isSetupProjection` 的交叉校验同步。`SetupState` 去掉 `course`/`activity` 两个分支。②课程/课节/任务/学期/假期改为各自的管理表面：新增 `src/renderer/management-surfaces.ts`（五个表面 + 方向键模型）与 `src/renderer/ManagementDialog.tsx`（复用既有 `TermForm`/`CourseForm`/`MeetingForm`/`TaskForm`/`HolidayForm`，自带 pending-mutation 保留与精确重试，草稿只在窗口内存活且明确告知）。`SetupDialog` 只剩学期一步：删除清单前后导航、`SetupFactReview`、`completeEditor`、`entryIntent` 与 `ActivityStep`（其 Meeting/Task 二选一由管理表面的分类导航取代）。入口：课程页页眉「添加课程 / 添加课节」与空态、任务页页眉「添加任务」（原先打开首次设置的 `onCreateTask` 改为打开任务管理）、设置「学期与课程」五个管理入口。按 `MODULE_CONTRACTS` §1724「抽屉、模态框和 Toast 是既有表面的交互形态」，本管理模态实现的是规格 §9.2/§9.4/§10.2 已批准的创建/编辑抽屉，不新增页面，首发剖面仍为 19 个表面。规范同步：`ARCHITECTURE.md`（WorkspaceLifecycle 行）、`MODULE_CONTRACTS.md` §6、UI 线框规格 §7.2（步骤、布局、最低条件、补充管理表面）、User Flow §UF-A-02、2026-08-28 UX 设计的最低条件段。测试：新增 `tests/renderer/management-dialog.test.ts`（5 项）；`setup-dialog` 删除清单导航与 activity 两项并改写完成态断言；`setup-ui` 的三项改为管理表面断言；`setup-state-minimum`、`workspace-shell`、`task-command-state`、`term-store`、`schema`（level 11 用例改为同时清空 `current_term_id`，仍验证里程碑不被抹除）、`workspace-setup`、`workspace-setup-activity-alternatives` 的最低条件夹具同步。未验证项：本切片未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，也未做 packaged 应用人工验收；上述度量来自 Chromium 无头渲染，不冒充 packaged Electron 证据。管理表面本切片只提供创建与只读列表，未实现编辑/删除。 |
| 2026-08-29 | `WP-UI-01` | `In Progress`（切片 7） | 本行所在提交 | `tsc --noEmit`（主配置）PASS；`tsc -p tsconfig.test.json` PASS；分块全量 `node --test`：`renderer` 96/96、`architecture` 48/48、`shared` 99/99、`data` 181/181、`main` 9/9、`platform` 11/11、`scripts` 13/13、`tests/*.test.js` 57/58（1 已知环境性失败：`TEST-WORKSPACE-007/PROTECT-007` 的 `ApplicationReleaseDescriptor` 不接受 `linux`）；`git diff --check` PASS；以真实 `CalendarPage` SSR + 真实 `styles.css` 在 Chromium `1280×900`（DPR 2）实测：`offset = 1` 时七列为 `2026-09-14…2026-09-20`、控件读作「上一周 / 1 周后 / 下一周 / 回到本周」，且没有任何列带 `calendar-current-label`（该周不含 applicable date）| 用户 2026-08-29 直接指令：日历需要手动切换周。`PlanQueryRequest` 增加可选 `requestedWindow`（`createPlanEvaluationContext` 早已支持该参数），`makePlanQueryRequest` 对非法窗口抛 `TypeError`，`isWorkspaceSetupRequest` 为 `workspace.plan.query` 单列一支，只接受「五个基础键」或「基础键 + 合法 `requestedWindow`」；`queryPlan` 透传该窗口；preload `queryPlan(requestedWindow?)`。Shell 侧新增 `CalendarWeekPresentation`：App 持有 `calendarWeekOffset`/`calendarWeekPlan`/`busy`/`problem`，只有日历页消费另一周的投影，Today、本周摘要与 TBA 仍使用按今天求值的那一份，两者不会对「今天」产生分歧——`CalendarContent` 因此接受显式 `applicableDate`，不再用被查询周的 `applicableDate` 判定今天列。任何提交后的 `refreshPlan` 会把周偏移复位为 0。窗口用 `addCalendarDays`（Renderer 既有工具）从当前周首日推算，Renderer 未新增对 `src/plan` 的依赖。测试：契约层新增窗口信封与非法窗口断言；`workspace-plan` 新增「显式周只移动窗口、不移动今天」；`workspace-pages` 新增「周导航控件 + 忙碌/失败态 + 今天列归属」；`runtime-boundaries` 的 preload 参数计数表把 `queryPlan` 由 0 改为 1。未验证项：未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，也未做 packaged 人工验收；七日网格仍从 00:00 起始、需要手动滚动到上课时段，本切片未改这一点。 |
| 2026-08-29 | `WP-UI-01` | `In Progress`（切片 8） | 本行所在提交 | `tsc --noEmit`（主配置）PASS；`tsc -p tsconfig.test.json` PASS；分块全量 `node --test`：`renderer` 97/97、`architecture` 48/48、`shared` 99/99、`data` 181/181、`main` 9/9、`platform` 11/11、`scripts` 13/13、`tests/*.test.js` 57/58（1 已知环境性失败：`ApplicationReleaseDescriptor` 不接受 `linux`）；`git diff --check` PASS；以真实 `TodayPage` SSR + 真实 `styles.css` 在 Chromium `1280×900`（DPR 2）实测：无根滚动，两栏宽度 `686 / 507`，统计条在两栏之上，`<progress>` 由默认绿灰改为暖色轨道 + `var(--dark)` 填充 | 用户 2026-08-29 从三个方案中选定「Dashboard（最接近参考图 6）」。Today 改为：页眉问候语 + 当前学期名称与起止日期（学期名上首页的直接要求）；新增 `TodayStatBar`（当前学期课程、今日课节、今日待完成、今日已完成、学期日期进度五格，自适应列宽，进度格占两列）；主体改为两栏 `today-column`——左栏今日课节（加 `today-timeline` 时间线导轨）与今日任务，右栏「下一步」、本周摘要、待确定。今日课节空态的主操作由「查看课程」改为「添加课节」，直接打开课节管理表面。「下一步」卡片的截止时间原样显示 UTC Instant（如 `2026-09-11T04:00:00.000Z`），改为按 TermZone 显示 `YYYY-MM-DD HH:mm`，`<time dateTime>` 仍保留精确 Instant；新增 `localInstantLabel`（复用既有 `localInstantParts`，不新增时间语义）。`<progress>` 补 `::-webkit-progress-bar` / `::-webkit-progress-value`（Chromium 对已设样式的 progress 忽略 `accent-color`）。所有数字仍来自 PLAN 投影与 Setup 投影，Renderer 未重新计算分类、选择或汇总。规格 §8 增加 MVP-A 实施说明，声明线框中的 Coursework/Assessment 分区属于 2026-08-28 新任务模型，未进入首发构建。测试：新增「Today opens on one stat bar over a two-column dashboard」（统计条内容、两栏归属、时间线类名、空态动作）。未验证项：未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，也未做 packaged 人工验收。 |
| 2026-08-29 | `WP-UI-01` | `In Progress`（切片 9） | 本行所在提交 | `tsc --noEmit`（主配置）PASS；`tsc -p tsconfig.test.json` PASS；分块全量 `node --test`：702 项 = 695 通过 / 5 跳过 / 2 已知环境性失败（`ADR-07/TEST-PROTECT-003` 大小写不敏感文件系统；`TEST-WORKSPACE-007/PROTECT-007` `ApplicationReleaseDescriptor` 不接受 `linux`）——`renderer` 100/100、`architecture` 48/48、`shared` 102/102、`data` 184/184、`protect` 171/173（1 跳过）、`main` 9/9、`platform` 11/12（1 跳过）、`scripts` 13/16（3 跳过）、`tests/*.test.js` 57/58；`git diff --check` PASS；以真实 `SettingsDialog` 与 `CalendarPage` SSR + 真实 `styles.css` 在 Chromium 实测：设置破坏性区域存在、确认输入为空时按钮 `disabled`、无根横向滚动；日历时段首标签 `07:00`、网格高度 `561px`（17 行 × 33px），09:00 课节 `top=66px`、13:00 课节 `top=198px`、14:00 课节 `top=231px` 均为整数像素 | 用户 2026-08-29 第 1 项与日历时段指令。（a）新增 `A-TERM-006`：设置「学期与课程」分类底部新增破坏性区域「重置当前学期」。新增 PLAN Intent `ResetCurrentTerm`（`plan.reset-current-term` v1）、效果码 `plan.current-term-reset`、请求种类 `workspace.term.reset-current` 与 preload `resetCurrentTerm`；DATA 在 schema level 17 的 receipt CHECK 中登记该 intent/effect，提交时按子表在前的顺序删除 TaskStateHistory/TaskOccurrence override 与 state/TaskSegment/TaskSeries/MeetingOccurrence override/MeetingSegment/MeetingSeries/Course/HolidayRange，再清空 `plan_state.current_term_id` 并删除该 Term。命令必须携带 `expectedRevision`/`expectedPlanVersion`/`expectedTermVersion`，只接受当前 Current Term，且 `confirmedTermName` 必须与 Term 名称逐字相等，否则按 validation 拒绝且数据不变；同一 CommandId 重放返回原 outcome。删除后若库中已无任何 Term 才清零 `ever_reached_minimum`（默认路由回到首次设置）；仍有历史学期时里程碑保持。其他学期、receipt/follow-up 历史、保护水位与本地备份均不受影响。Shell 侧：确认输入与学期名逐字相等前按钮保持 `disabled`，只读模式整区不可执行，结果未确认时保留原命令只做精确重试，成功后用 `querySetup` 刷新并向 App 上抛新投影。（b）日历默认可见时段由 00:00–24:00 改为用户指定的 07:00–22:00，落在该范围外的真实课节/任务把时段扩展到容纳它的整点边界；行高改为 `--calendar-hour-count × 33px`，事件偏移改为整数优先的 `minutes × 33 / 60`（原先 `minutes × 0.55` 会产生 `198.00000000000003px` 这类浮点残差）。文档先行：PRD `A-TERM-006`、MODULE_CONTRACTS §6.2 Term Intent 族与 `ResetCurrentTerm` 规则段、§5 规则 6 的 `everReachedMinimum` 唯一例外、追溯表新增 `A-TERM-006` 行与 62 条计数、UI 规格 §11.1（周导航与可见时段）与 §14.2（重置当前学期）、实施路线图 MVP-A 核心计数 43。测试：新增 3 项契约测试（`ResetCurrentTerm` 规范化与拒绝、digest 绑定 Term 与两个期望版本、请求信封与 validation 分类）、2 项设置表面测试（禁用态与文案、只读与无学期）、1 项日历时段测试；既有 3 项 DATA 重置测试保持通过。未验证项：未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，也未做 packaged 人工验收。 |
| 2026-08-29 | `WP-UI-01` | `In Progress`（切片 10） | 本行所在提交；[已批准修复计划](../superpowers/plans/2026-08-29-wp-ui-01-slice-10-forward-migration-repair.md) | 先取得 RED：新增 `tests/workspace-migration.test.ts` 的「一级之下的工作区按应用自身 open 选项迁移」用例在修复前失败，`workspaceData.kind` 实测 `recovery`（期望 `ready`），与用户机器上应用停在「需要恢复本地工作区」的现象一致。修复后 `tsc --noEmit`（主配置）PASS、`tsc -p tsconfig.test.json` PASS；分块全量 `node --test`：706 项 = 699 通过 / 5 跳过 / 2 已知环境性失败（`ADR-07/TEST-PROTECT-003` 大小写不敏感文件系统；`TEST-WORKSPACE-007/PROTECT-007` 的 `ApplicationReleaseDescriptor` 不接受 `linux`）——`data` 187/187、`shared`+`architecture` 150/150、`renderer`+`main`+`platform` 120/121（1 跳过）、`protect` 171/173（1 跳过、1 已知失败）、`tests/*`+`scripts` 71/75（3 跳过、1 已知失败）。`git diff --check` PASS；改动路径新增行无超 120 字符、tab 或尾随空白 | 修复 2026-08-29 由本包切片 9（schema level 16→17）引入的前向迁移阻塞，根因两处且都在 `openWorkspaceDataWithMigrations`：①接受的源级别仍是止于 `identity.schemaLevel !== 15` 的手写链，源校验梯子的末尾 `else` 仍假定 level 15，因此 level 16 库在进入迁移循环前即被拒绝并返回原 `integrity`/`schema-mismatch`；②`migrationOpenOptions` 只在存在 `migrationRollbackTarget` 时才提供 `migrationSafetyCopy`，而真实入口 `src/workspace.ts` 从不提供该 target，只有测试提供——即使修好①也会停在 `migration-safety-unavailable`，即任何已发布构建都从未能够前向迁移既有工作区。结构性修复：新增 `src/data/schema/ladder.ts`，以 `Record<SchemaLevel, …>` 与 `Record<MigratableSchemaLevel, …>` 登记每级校验器与前向迁移，`MigratableSchemaLevel = Exclude<SchemaLevel, typeof CURRENT_SCHEMA_LEVEL>` 使下一次抬高级别时缺失的一步变成编译错误而非运行期静默错级；`open.ts` 的三处手写梯子改为表查找与 `isMigratableSchemaLevel`。按用户 2026-08-29 决定，安全副本的回退目标改为可选（文档先行：`A-DATA-007`、ADR-10 §1.2-5/§2-10、`MODULE_CONTRACTS` 投影与规则 24、FLOW-07 第 4 步）：副本无条件创建并验证，`rollbackTarget` 为 null 时 PROTECT 不创建 preview、Shell 只显示「未绑定」并只提供删除；新增 `BoundMigrationSafetyCopyProjection` 让回退结构在类型上排除 null，`host.ts` 改为始终提供绑定。`MIGRATION_SAFETY_COPY_SCHEMA` 保持 `-v1`：可空是加宽，既有副本仍可读，`formats.migrationSafetyCopy` 无消费者故保持 `1`。未验证项：未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`；本次修复的最终判据——真实 level 16 工作区在 Windows 开发应用中迁移到 17 且数据完好——尚待 Sirui 在本机重启应用确认，迁移前的字节副本留在 `%LOCALAPPDATA%\CourseFlow Dev\level16-fixture-2026-08-29\`。未做 packaged 人工验收，未涉及 macOS。 |
| 2026-08-30 | `WP-UI-01` | `In Progress`（切片 10 追加证据） | 代码提交 `995dc2932e1e26f44f5990dfc050f05068ba9b0b`；本行所在证据提交 | 用 Sirui 本机真实 level 16 活动数据库的字节副本（`%LOCALAPPDATA%\CourseFlow Dev\level16-fixture-2026-08-29\workspace.sqlite`，335,872 bytes）在设备 Linux VM 的临时 DataSlots 中执行一次 `WorkspaceApplication.open(dataSlotsRoot, appBuildId, {activityControlRoot})` + bootstrap，即 `src/workspace.ts` 使用的同一组选项；原库只读复制，未触碰真实工作区 | 迁移前 `user_version=16`、terms 3、courses 1、task_series 1、meeting_series 1、holiday_ranges 0、revision 13；迁移后 `user_version=17`，五张表计数逐项不变，revision 14（每抬升一级 +1，与 `isSameMigration` 的既有换算一致）。bootstrap `ok=true`，`workspaceData.kind=ready`、`lifecycle.route=today`、`lifecycle.mode=ready`，不再落入 recovery。`inspectMigrationSafetyCopy` 为 `verified`，`sourceSchemaLevel=16`、`targetSchemaLevel=17`、`rollbackTarget=null`、`byteSize=335872`。未验证项：本次仍在 Linux VM 的 Node 运行时执行，不冒充 Windows Electron 证据；Windows 上的 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`、真实开发应用重启后的人工确认与 packaged 验收仍未进行。 |
| 2026-08-31 | `WP-UI-01` | `In Progress`（切片 11） | 本行所在提交 | `tsc --noEmit`（主配置）PASS；`tsc -p tsconfig.test.json` PASS；分块全量 `node --test`：711 项 = 709 通过 / 1 跳过 / 1 已知先存失败 —— `data` 187/187、`shared`+`architecture` 150/150、`renderer`+`main`+`platform` 125/126（1 跳过：`a repository marker link cannot establish CourseFlow ownership`，宿主不允许创建文件链接）、`protect` 173/173、`scripts` 16/16、`tests/*.test.js` 58/59。**同环境（Windows 11 / Node v24.19.0 / Git Bash）clean HEAD `fcd9e83` 基线用 `git worktree` 实测为 706 项 = 704 通过 / 1 跳过 / 1 失败，跳过与失败逐项相同**，因此本切片新增的 5 项测试全部通过、未改变失败/跳过集合。该失败为 `TEST-WORKSPACE-007/PROTECT-007`（`tests/workspace-migration.test.ts:190`）的 `assert.equal(previewSession.binding?.currentData.schemaLevel, '16')` 实测 `'17'`，是切片 9 抬高 schema level 16→17 时遗留的过期断言，先于本切片存在且与本切片无关。切片 10 台账记录的「5 跳过 / 2 失败（`ADR-07/TEST-PROTECT-003` 大小写、`ApplicationReleaseDescriptor` 不接受 `linux`）」属于另一运行环境，本行不沿用该集合。RED 证据：实现前先运行新增/改写测试，7 项失败（`TodayPage renders only real unified...`、`Today opens on a four-column grid...`、`Today collapses the attention slot...`、`Today conflict marks come only from...`、`the dark next-step surface is one card...`、`no Workspace page header renders a kicker...`、`the stylesheet keeps fixed type steps...`），实现后全部通过。`git diff --check` PASS；`src/` 与 `tests/` 改动路径新增行无超 120 字符、tab 或尾随空白（逐行程序化核对）。以真实 `WorkspaceShell` + 真实 `TodayPage` SSR + 真实 `styles.css` 在 Chromium 141 headless（DPR 2）实测：**1280×800** 网格 `293.078px 293.078px 293.078px 293.094px` / `gap 12px`，今日时间轴 `x=35.83 w=598.17 h=622.91`（第 1-2 列跨两行）、下一步 `x=646 w=293.08`、需要注意 `x=951.08 w=293.09`、今日任务 `x=646 w=598.17`、本周 `x=35.83 w=1208.34`，与 UI 规格 §8 逐项一致；`document.scrollingElement` 纵横均 `800/800`、`1280/1280`（无根滚动），唯一纵向滚动仍属 `.workspace-content`（`920/728`），时间轴导轨在卡内滚动（`590/561`）。**960×640** 降为两列 `447.125px` / `gap 12px`，无根滚动（`640/640`、`960/960`），`.workspace-content` `1475/568`。**空日折叠**（逾期 0 / 冲突 0 / TBA 0）：`.attention-card` 不在 DOM 中，下一步扩展为 `w=598.17`，页脚出现「没有逾期任务、本周时间冲突或 TBA 任务，「需要注意」已隐藏。」。**对比度实测**（截两张图，一张全局 `color: transparent` 取每个文本矩形内的真实背景像素均值，与 `getComputedStyle` 前景色按 alpha 合成后算 WCAG 比值；背景图按整页高度重截，避免折叠线以下的探针取到未绘制区域）：1280×800 与 960×640 各 25 个探针、折叠态 22 个，全部达标——正文最低 `5.95:1`（学期胶囊「剩余 n 天」12px，门槛 4.5）、大字最低 `7.05:1`（页眉 32px 数字，门槛 3）。`node .claude/skills/impeccable/scripts/detect.mjs --json --scope layout src/renderer` 以 **DEGRADED** 模式运行（缺 `htmlparser2`/`css-select`/`css-tree`/`domutils`），返回 `[]`；该结果按其自述属于漏报，不作为通过证据 | 按 Sirui 2026-08-31 已冻结的《WP-UI-01 切片 11 交接简报》重排 Today，并执行同批清理 D1/D2/D9-D16。规范先行：PRD `A-VIEW-001`/`A-VIEW-006` 改为首页提升逾期任务/今日待完成/下一节且今日已完成降级为任务完成度；用户流程 §5.4 与 UF-A-03 首页信息层级同步；UI 规格 §8 整节重写（四列网格、槽位与事实绑定、空态降级阶梯、排序与选择的确切读法、Renderer 边界），§4.4 新增与强调黄分离的 critical/warning/success/neutral 状态色词汇，§4.5 记录实装断点 1080/820，§17.2 显式修正「标题区不堆叠统计卡」为允许无卡片页面级事实并把 eyebrow 写为硬禁，§17.8 改为按槽位分级的空状态与学生语言，§17.9 加 severity。实现：`today.tsx` 全页重写为页眉（问候语 + 学期进度胶囊 + 三个行动数字）加四列网格（今日时间轴 / 下一步 / 需要注意 / 今日任务 / 本周七日条）；`shared.ts` 接收从 `calendar.tsx` 迁入的时间几何（`CALENDAR_HOUR_HEIGHT=33`、`calendarMinutePixels`、`calendarHourWindow`、`calendarTimedPlacements`、重叠泳道），使日历与今日共用切片 9 的同一条时间规则，并新增 `termWeekLabel`/`todayHeadlineMeeting`/`taskSeverity`/`conflictingMeetingIds`/`courseColorFor`；`widgets.tsx` 的 `PageHeader` 去掉 `eyebrow` 改为可选 `facts`，`EndedTermState` 由三层嵌套卡拍平为一层深色卡，chip 带 `data-severity`；`styles.css` 收敛为三级圆角 token、固定 rem 字号梯级（`font-size: clamp()` 归零）、`.status-banner` 彩色边 `0.35rem → 1px`、新增 `::selection`/`caret-color`/`tabular-nums`。**Renderer 边界**：三个数字、冲突标记、七日条计数、完成度 meter 全部读现有投影，未新增 Query、未动 schema 与 PLAN/DATA 契约；「本周时间冲突」按 `plan.agenda.warnings` 的真实范围命名为本周而非今日。**一处对简报的更正**：简报 §3 把学期胶囊的假期段绑到 `plan.week.holidayRanges`，该字段只覆盖当前一周，画在学期长度的轨道上会虚构事实（简报自己的红线二）；经 Sirui 2026-08-31 批准改用 `setup.holidayRanges` 按 `termId` 过滤，已写入 UI 规格 §8.1。D15 经 Sirui 批准扩到全 Renderer 20 处 en-dash（简报只点名 6 个文件共 16 处，另 4 处在 `setup/forms.tsx`、`setup/progress.tsx`、`styles.css`）。`styles.css` 拆分未在本切片执行，已按建议登记为 `WP-RF-02`。**未验证项**：未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，未做 packaged 人工验收，未涉及 macOS；Chromium 无头渲染只证明 SSR 后的 DOM 与 CSS 几何，不冒充 packaged Electron 证据。**已知遗留**：`.workspace-content` 在 1280×800 的密集样本下仍需滚动约 192px（920/728），根滚动为 0；`TaskItem` 对 timed deadline 仍打印原始 Instant（`2026-09-11T03:59:00.000Z · America/Toronto`），属 Tasks/Calendar 共用行为，本切片未改 |
| 2026-09-01 | `WP-UI-01` | `In Progress`（切片 12） | 规范提交 `a55868d`；代码与本行所在提交 | `tsc --noEmit`（主配置）PASS；`tsc -p tsconfig.test.json` PASS；分块全量 `node --test`（设备 Linux VM，Node 22）：717 项 = 710 通过 / 5 跳过 / 2 已知环境性失败（`ADR-07/TEST-PROTECT-003` 大小写不敏感文件系统；`TEST-WORKSPACE-007/PROTECT-007` 的 `ApplicationReleaseDescriptor` 不接受 `linux`）： `renderer` 111/111、`architecture` 48/48、`shared`+`main`+`platform` 123/123（1 跳过）、`data` 187/187、`protect` 173（171 通过、1 跳过、1 已知失败）、`tests/*.test.js`+`scripts` 75（71 通过、3 跳过、1 已知失败）；与切片 10 台账同环境的 706 = 699/5/2 相比，跳过与失败集合逐项相同，新增的 11 项全部通过。改动顺序如实记录：先改实现，再把切片 11 的五条 Today 断言改写为新网格断言（改写前它们在新实现上失败，标定了变更范围），再新增六项测试；PLAN 契约测试的新增断言在同一提交里首次运行即通过。`git diff --check` PASS；改动路径新增行经程序化核对无超 120 字符、tab 或尾随空白。以真实 `WorkspaceShell` + 真实 `TodayPage` SSR（`_scratch/ui-check/slice12/render-today.js`，样例学期 5 门课 19 项任务）+ 真实 `styles.css` 在 Chromium 141 headless（DPR 2）实测三档：**1420 画布（视口 1540×1128）** 七槽位 `x/y/w/h` = 时间轴 `60/229/346/626`、现在 `418/229/346/326`、本周课时 `776/229/346/326`、深色卡 `1134/229/346/626`、今日任务 `418/567/704/288`、课程 `60/867/1062/239`、学期任务 `1134/867/346/239`，根无滚动（`1540/1540`、`1128/1128`），`.workspace-content` `1068/1056`；**1280×800** 列宽 293px、`gap 12px`，根无滚动，`.workspace-content` `1055/728`；**960×640** 两列 447px，时间轴独占首行，`.workspace-content` `2083/568`。开学前状态（今天 2026-09-01）同样三档，`.workspace-content` 在 1540×1128 为 `1056/1056`。**对比度实测**（文本透明后截图取每个文本框背景均值，与 `getComputedStyle` 前景色按 alpha 合成）：上课中状态 199 个探针、开学前 164 个探针，正文最低 `5.72:1`、大字最低 `7.05:1`；两个探针（柱图峰值 chip、时间轴任务条上的「今天」chip）因探针框混入了 chip 自身以外的像素而低于门槛，按 token 计算分别为白字对 `#303030` `12.6:1`、`#6f4a06` 对 `#fbe9c4` 约 `7:1`。条件层 `ui-ux-pro-max` 定向检索两次：`--domain chart` 命中「Compare Categories / Bar Chart」（直接标签、不只靠颜色、焦点可读值，已采用：每柱 `title` + 视觉隐藏文本、今天用日标签胶囊而非只换色）；`--domain ux` 检索 accordion 未命中匹配条目，按仓库既有约束回落到原生 `<details>/<summary>`。`node .claude/skills/impeccable/scripts/detect.mjs` 本切片未运行 | 按 Sirui 2026-09-01 在 Cowork 会话中选定的四张新卡片与「学期任务完成度（按课程）」功能，以及他同日逐项确认的四项编排 N1-N4（《WP-UI-01 切片 12 交接简报》，`Documents/`）：①「下一步」与「本周截止」合并为同一张深色卡；②「需要注意」退役，逾期留在页眉与任务列表、时间冲突进「本周课时」卡底部、TBA 进「学期任务」卡底部；③「本周」七日条被「本周课时」柱图吸收；④时间轴改为一列宽日脊跨第 1-2 行，课程 accordion 跨第 1-3 列。规范先行（`a55868d`）：PRD `A-VIEW-001`/`A-VIEW-006`、MODULE_CONTRACTS §5.3 不变量 23 / §8.3 FLOW-02 第 4 步 / §10 `TEST-PLAN-008`、UI 规格 §8 整节重写与 §17.8 次槽位规则、用户流程 UF-A-03 首页信息层级。**PLAN 契约扩展（无新 Query、无 schema 变更）**：`PlanProjection` 新增 `week.days`（七天：`date`、`meetingCount`、`meetingMinutes`、`taskCount`；`cancelled` 不计，跨日课节归开始日取全长）与 `courses`（每门课 `completed`/`pending`/`overdue`/`tba`/`skipped`/`countable`，`countable` 排除 `skipped`），由 `buildWeekDays`/`buildCourseTaskSummaries` 在 `buildPlanProjection` 内从同一批已分类实例汇总；`isPlanProjection` 键集同步，重建比对覆盖两个新字段。**更正**：`readPlanProjectionSource` 按整学期展开，`plan.tasks`/`plan.meetings` 是整学期事实，切片 11 简报「跨周下一节课无法从投影得出」的说法不成立，`plan.meetings` 首个 `upcoming` 即学期内下一节；今日空态阶梯与「现在」卡据此新增跨周一级。**Renderer**：`today.tsx` 七张卡（`NowCard` 五态：上课中 / 课间 / 今天的课上完了 / 今天没有课 / 开学前，环用 `pathLength="100"` 定长、60 刻度表盘轨道，在课比例为与时刻线同类的时间几何；`WeekLoadCard` 读 `plan.week.days`，柱 18px、数据端 `--radius-mark` 4px、基线直角、峰值 chip、任务圆点、冲突 chip；深色卡 `WeekDeadlines` 读 `plan.week.tasks` 最多 5 行 + `n/N`，行不可操作；`CoursesCard` 原生 `<details>` 行、SVG chevron、`:focus-visible` 轮廓、hover 仅在 `(hover: hover) and (pointer: fine)`；`TermTasksCard` 按课程分段条 + 图例 + 逾期/TBA chip，无实例课程补 `0/0`）；`AttentionCard`/`attentionFacts`/`WeekStrip` 与对应 CSS、页脚说明删除；`shared.ts` 新增 `nextTermMeeting`/`durationLabel`/`hoursLabel`/`taskDeadlineRowLabel`/`courseWeekdaySummary`/`sortedCourseMeetings`/`weekdayMarks`/`shortWeekdayOf`。同批清理：D17 `TaskItem` 接收 `termZone`，timed deadline 在 Today/Tasks/Calendar 议程统一显示 TermZone `YYYY-MM-DD HH:mm`（`<time dateTime>` 保留 Instant）；D18 时间轴 60 分钟事件标题被裁（overflow-hidden 网格项自动最小高度为 0）：标题行 `min-height: 1.2em`，短事件裁 meta 行；空态说明块改为覆盖在导轨中央；D19 删除折叠逻辑与 `next-step-card--wide`；D20 hover 门控与 `prefers-reduced-motion`；D21 `forced-colors` 分支覆盖环、柱、分段条与圆点。`:root` 新增 `--radius-mark: 4px`，是三级表面圆角之外唯一的图表数据端半径，CSS 契约测试的白名单同步并锁定该 token。深色卡在「下一步」为空时只保留空态的一个「查看任务」动作（避免同意图 CTA 重复）。设计稿与 SSR 截图在 `_scratch/ui-check/slice12/`。**未验证项**：未在 Windows 运行 `pnpm test`/`pnpm typecheck`/`pnpm package`/`pnpm smoke:packaged`，未做 packaged 人工验收，未涉及 macOS；Chromium 无头渲染只证明 SSR 后的 DOM 与 CSS 几何，不冒充 packaged Electron 证据；`<details>` 的展开交互与环的 220ms 过渡未在真实 Electron 中观察。**已知遗留**：1280×800 下 `.workspace-content` 需滚动约 327px（课程与学期任务是新增的第三行）；`WP-RF-02` 的 `styles.css` 拆分仍未执行，文件已增至 3,885 行。 |


## 7. 拆包与变更规则

- 工作包过大时可以拆成带稳定后缀的子包，但父包在全部子包 `Done` 前不得 `Done`，且 Requirement/TEST 主所有权必须保持唯一。
- 仅实现顺序、资源或证据安排变化时更新本文件和 Roadmap。
- 产品行为变化先更新 PRD/MVP；模块边界/FLOW/Q 变化先更新 Architecture；接口/Problem/TEST 变化先更新 Contracts；技术选择变化先建立或修订 ADR。
- 任何拆包都不得创建“通用框架”“未来扩展”“收尾”或 C1/C2 占位工作包。
