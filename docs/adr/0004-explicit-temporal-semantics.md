# 分别建模本地日期、绝对时刻、时间区间与待定信息

CourseFlow 不把所有日期存成 UTC timestamp。没有具体时刻的截止日保持 `LocalDate`，确定时刻按来源时区解释后保存为绝对 instant，同时保留 IANA 时区；未知或相对日期保持待定和原文。这样避免 UTC 午夜导致日期偏移，也使日历导出、冲突检测和 Evidence 审核拥有一致语义，代价是 contract 和数据库必须显式维护多种时间状态及其约束。
