# CourseFlow

CourseFlow 把用户手工确认的课程事实与原始资料整理成可核对的个人课程计划。所有正式课程记录都来自用户明确提交的表单；Source 只用于预览和人工核对。

## 课程组织

**学期（Academic Term）**：课程计划的顶层时间范围，包含开始/结束日期、课程时区和校历例外。

**课程（Course）**：隶属一个学期的课程身份，包含课程代码、标题、颜色、学分和可选字母等级表。

**周期课节（Meeting Pattern）**：按星期、本地起止时间、类型和有效日期范围定义的重复课节。

**课节例外（Meeting Exception）**：对某个周期课节某一天的取消、保留或改期。

**Reading Week**：学期级校历例外；在派生课节实例时抑制通常课节，不通过删除周期规则实现。

## 正式规划数据

**课程事项（Course Item）**：作业、考试、测验、阅读、项目等用户确认的正式记录。时间可以是待定、纯日期、截止时刻或区间。

**任务标签（Task Label）**：用户在当前学期维护、可用于筛选和分组的正式标签。

**评分方案（Grading Scheme）**：课程的评分组成集合；未知权重保持未知。

**成绩结果（Grade Result）**：用户为评分组成手工录入的 earned/possible。没有结果记录表示未出分，不按零分。

**字母等级表（Letter Grade Scale）**：用户确认的 A/B/C/D/F 边界；只有课程关联后才派生字母等级。

## 课程资料

**来源资料（Source Document）**：用户上传的一组原始课程文件及展示元数据。资料可安全预览、下载或删除，但不会自动改变课程计划。

**来源资源（Source Asset）**：Source Document 中按稳定顺序保存的单个 PDF 或图片对象。

**手工核对（Manual Verification）**：用户在 Source 预览旁打开既有课程事项、课表或成绩表单，对照原文后明确提交。只有该提交动作可以产生正式数据。

## 派生视图

**课节实例（Meeting Occurrence）**：在有界日期范围内由周期课节、学期例外和单次例外计算出的只读实例。

**Schedule Snapshot**：Dashboard、Timeline、Tasks、Calendar、冲突、热力图和 ICS 共同消费的正式只读投影。

**当前成绩**：只基于已出分组成和已知权重派生的结果；不另建第二套真相。

**待定（TBA / Unscheduled）**：原文或用户尚未给出唯一日期/时间时的显式状态，不能补成猜测值。

## 发布模式

**MANUAL_ONLY**：P4 已签署的唯一发布模式。CourseFlow 不配置或调用远程 AI，不提供自动抽取、候选审核或规划助手，也不以其他模型替代。相关研究只作为历史审计材料存在，不是开发入口。
